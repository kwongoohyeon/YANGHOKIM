# 공고 매칭 — 인수인계 (2026-09-16)

브랜치 `kwon-dev`. 설계는 `docs/superpowers/specs/2026-08-30-공고매칭-design.md`,
구현 계획은 `docs/superpowers/plans/2026-08-30-공고매칭-1A.md` 다. 이 문서는 **지금 어디까지 됐고
다음에 뭘 하면 되는지**만 적는다.

## 다른 PC 에서 이어받기

```bash
git clone https://github.com/kwongoohyeon/YANGHOKIM.git
cd YANGHOKIM
git checkout kwon-dev
npm install
```

`.env.development.local` 파일을 직접 만든다(깃에 안 올라간다). 내용 한 줄:

```
DISABLE_ESLINT_PLUGIN=true
```

없으면 `npm start` 가 ESLint 오류 화면을 띄운다(`confirm()` 15건 등). 배포 환경은 `.env.production`
으로 같은 설정을 이미 끄고 있다 — 즉 **로컬만 맞춰 주는 것**이지 코드 문제가 아니다.

```bash
npm start                         # http://localhost:3000
node scripts/test-ann-match.mjs   # 매칭 엔진 검증
CI=true npx react-scripts build   # 빌드(에러 0 이어야 한다)
```

⚠️ 이 앱은 **운영 Supabase 에 직접 붙는다**(주소·키가 `src/App.js` 에 박혀 있다).
로컬에서 고쳐도 실제 고객 데이터가 바뀐다. 화면에서 저장 버튼을 누를 때 조심할 것.

## 끝난 것

| | 내용 | 흔적 |
|---|---|---|
| 1 | `announcements` 테이블 + RLS + 권한 회수 (계획서 Task 5) | `공고매칭_announcements_테이블추가.sql` · `_rollback` · `_검증`. 2026-09-16 운영 DB 에 **실행·검증 7/7 통과** |
| 2 | 지역 정규화 · 지역 조건 판정 (Task 1) | `src/App.js` ANN-ENGINE 블록 |
| 3 | 업종 조건 판정 (Task 2) | 〃 |
| 4 | 규모·매출·업력·근로자수 판정 (Task 3) | 〃 |
| 5 | 종합 판정(유력/애매/제외)·전체 매칭 실행 (Task 4) | 〃 |
| 6 | 최종 리뷰 지적 수정 (지역 해석 불가 → unknown 등) | 같은 블록 |

엔진은 `src/App.js` 의 `// ── ANN-ENGINE-START ──` ~ `// ── ANN-ENGINE-END ──` 사이에 있다.
`scripts/test-ann-match.mjs` 가 **소스에서 그 블록을 떼어내 실행**한다(테스트에 로직을 옮겨 적으면 검증이 아니다).
기존 규모 판정 함수(`judgeSmallBiz` 등)는 `// ── BIZ-SCALE-START ──` 블록이고 **한 줄도 안 고쳤다** —
기업목록 규모 배지가 같은 함수를 본다.

⚠️ `BIZ-SCALE-START` 표식은 마커와 설명이 **두 줄**이다. 한 줄로 합치면 블록 추출이 SyntaxError 로 죽는다.

## 아직 안 한 것

| 차수 | 내용 | 선행조건 |
|---|---|---|
| 1-B | `announce` 화면 — 공고 목록·상세·조건 편집·결과표 | 없음 (엔진 완료) |
| 1-C | 업무노트/일정 보내기 · 마감 D-7 배너 | 1-B |
| 2-B | **기업마당 자동수집** — 브리핑 저장소가 매일 아침 공고를 CRM 으로 보낸다 | Vercel 환경변수(공유 비밀키) 등록 |
| — | Task 6 (`api/parse-announcement.js`, AI 조건 추출) | 사람이 PDF 공고를 붙여 넣는 경로에만 필요 |

### 2-B 가 이번에 새로 정해진 부분

`kwongoohyeon/policy-fund-briefing` 이 이미 매일 아침 08시에 기업마당 공고를 수집해 메일을 보낸다.
그 워크플로 **마지막 단계**에 "공고 전체를 CRM API 로 POST" 를 붙인다. 받는 쪽은
`api/policy-notices-sync.js`(신규) 이고, 기존 `api/assign-db-sync.js` 와 **같은 방식**이다 —
공유 비밀키 헤더 + `SUPABASE_SERVICE_ROLE_KEY` 로 서버에서만 쓴다.

- 기업마당 공고는 `trgetNm`(지원대상 9종) · 지역 해시태그 · 분야 · 신청기간이 **구조화되어 온다**
  → 조건(`conditions`)을 **규칙으로 채운다. AI 호출 0, 비용 0.**
- 실측(2026-09-16): 마감 전 공고 **1,533건** — 전국 459 / 특정 시·도 1,000여 / 태그 없음 56.
  분야는 경영 441 · 기술 303 · 금융 225(융자 167·보증 42) · 수출 193 · 인력 179 · 내수 110 · 창업 67.
  신청기간은 날짜 범위 612 · 예산 소진시까지 627 · 상시 접수 136.
- ⚠️ 0건이 오면 **반영하지 않는다**(공고가 통째로 마감 처리되는 사고 방지 — 배정DB 와 같은 원칙).

## CRM 데이터 실측 (2026-09-16, 휴지통 제외)

| 항목 | 수 | 비율 |
|---|---:|---:|
| 전체 기업 | 413 | |
| 지역 있음 | 408 | 98.8% |
| ↳ 언더바 표기 / 공백 표기 / 고유값 | 272 / 134 / 241 | |
| 업종 있음 | 402 | 97.3% |
| 설립연도 있음 | 397 | 96.1% |
| 매출 있음 | 351 | 85.0% |
| **상시근로자수 있음** | **147** | **35.6%** |

마지막 줄이 매칭 결과를 좌우한다. 소상공인 여부는 근로자수가 있어야 확정되는데 35.6% 뿐이라,
**소상공인 대상 공고(기업마당 213건)에서는 "애매"가 대량으로 나온다.** 엔진 결함이 아니라 데이터 상태다.
화면에서 "이 칸만 채우면 확정된다"를 보여 주면 담당자가 채울 이유가 생긴다.

## 판정 규칙 요약

조건축마다 `pass | unknown | fail` 을 매기고:

> **fail 하나라도 → 제외 / fail 없고 unknown 없음 → 유력 / 나머지 → 애매**

제외는 **확실할 때만** 낸다. 최종 리뷰에서 "판정 불가가 제외로 새는" 경로 4건을 잡았다:

1. 공고 지역을 해석 못 하면(빈 값·문자열 항목·"전국"·별칭표 밖 표기) 전원 제외되던 것 → `unknown`
2. 공고가 `덕양구` 처럼 구 단위를 요구하는데 우리 값은 `경기_고양` 처럼 시 단위일 때 → `unknown`
3. 업종 `exclude:["음식"]` 이 `음식료품 도매업` 을 제외하던 것 → 토큰 단위 비교로 좁힘
4. 업종 키워드로 추정한 규모기준(코드 주석이 스스로 "참고용"이라고 밝힌 값)으로 제외하던 것
   → **수동 보정(`biz_match_override`)에서 온 판정일 때만** 제외한다

## 사람 손이 필요한 것

| 할 일 | 누가 | 비고 |
|---|---|---|
| Vercel 환경변수 `POLICY_NOTICES_SYNC_SECRET` 등록 + 재배포 | 저장소 주인 | 2-B 전에 필요. 무료 요금제는 팀원 초대가 안 된다 |
| PR 머지 | 저장소 주인 | 무료 요금제에서 배포가 멈추지 않게 **"Create a merge commit"** 으로 |
| CRM 로그인 계정 승인 | 저장소 주인 | 화면 확인용 |
| 저장소 비공개 전환 | 저장소 주인 | 고객사 CSV·내부 문서가 공개 상태다. **포크를 먼저 지운 뒤** 전환할 것 |
