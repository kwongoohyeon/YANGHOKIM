# 작업 규칙 (재발방지 체크리스트)

이 저장소는 단일 대형 파일 `src/App.js`(약 18,900줄, React + Supabase)에 대부분의 로직이 모여 있습니다.
공용 함수/표시 필터 하나를 건드리면 여러 기능이 동시에 영향받기 쉽습니다.
기능 추가·수정 요청을 받으면 **아래 절차를 반드시 지킬 것.**

---

## 🚧 보류 중 — 퇴사자 3명(현애·인선·미현) DB 정리 (2026-08-17~)

**화면(App.js)만 정리했고 DB 쪽은 일부러 안 건드린 상태다.** 나중에 확인 후 정리할 예정.
아래 SQL 파일들은 **그때 쓸 것이라 지우지 말 것.**

**끝난 것 — App.js 명단에서 3명 제거, 정원 추가** (커밋 `6e44530`)
- `ASSIGNEES` — 화면의 담당자 선택/필터가 전부 이 배열 하나를 본다(기업상세 담당자 다중선택 포함)
- `TEAM_MEMBERS`(팀 공지 확인 대상) · `CHAT_TEAMS`(팀 채팅 채널) · `WN_SHARE_GROUPS`(→ 빈 배열)
- 중복으로 박혀 있던 명단 2곳("모든 담당자" 필터 · 대시보드 직원별 집계)을 `ASSIGNEES` 참조로 통일했다
  → **이제 명단이 바뀌면 `ASSIGNEES` 한 곳만 고치면 된다.**

**보류 중 1 — 기존 `companies.assignee` 값**
- 3명이 담당으로 들어간 업체가 몇 개인지 **아직 조회 안 했다**(실행 권한이 없어 못 돌렸다).
- 명단에서 빠져도 저장된 `"관호, 현애"` 같은 값은 **화면에 그대로 보인다.** 그 업체를 편집해
  저장할 때만 선택지에 없어 빠진다 → 지금 당장 깨지는 것은 없다.
- 조회용(읽기 전용, 아무것도 수정 안 함):
  - `담당자정리_조회1_전체스캔.sql` — public 전 테이블에서 세 이름이 걸린 컬럼·건수
  - `담당자정리_조회2_companies상세.sql` — 업체별 목록 + `제거 후 남는 담당자` + **`⚠ 담당자 없어짐`** 판정
    (콤마·세미콜론·슬래시 분리 + `김현애`→`현애` 옛 표기 정규화 포함)
- 정리 방침은 **"3명만 빼고 나머지 담당자는 유지"**(`"관호, 현애"` → `"관호"`)로 정해져 있다.
  혼자 담당이던 업체는 빈칸이 되므로, **UPDATE 전에 조회2의 `⚠ 담당자 없어짐` 목록을 먼저 확인할 것.**

**보류 중 2 — App.js 와 한 쌍인 DB 함수 2개 → 1개 해결, 1개 남음**
- ✅ **`chat_can_access()` 는 2026-09-11 에 반영 완료** (짝: `CHAT_TEAMS`).
  이만나미 추가 작업과 **한꺼번에** 돌렸다 → `이만나미_권한정리_채팅RLS_명단반영.sql`.
  ⛔ **`담당자정리_채팅RLS_명단반영.sql` 은 폐기다 — 돌리지 말 것.** 그 파일엔 이만나미가 없어
  지금 실행하면 개인팀 채팅이 다시 막힌다(파일 머리에도 같은 경고를 박아 뒀다).
  최종 명단: corporate = 양호·동일·유진·정원 / individual = 양호·동일·관호·지혜·정원·**이만나미**.
- ⏸ **`담당자정리_업무노트RLS_공유그룹해제.sql` → `wn_visible_names()` (짝: `WN_SHARE_GROUPS`) 는 아직 남았다.**
  DB 함수에 `('미현','인선')` 공유 그룹이 그대로 있어, 두 계정이 살아 있는 동안은
  **DB 로는 서로의 개인노트를 아직 읽을 수 있다**(화면은 `WN_SHARE_GROUPS = []` 로 이미 막혀 있다).
  방향이 **"화면엔 안 보이는데 DB는 아직 허용"** 이라 남은 사람 기능은 안 깨진다.
  퇴사 처리라면 이 파일을 돌려야 짝이 맞는다.
  (즉시 차단이 필요하면 그건 `profiles.status='rejected'` 담당이다. 아래 2-3·2-4 참고.)

**일부러 안 건드린 것 (정리할 때도 건드리지 말 것)**
- `DB_CALL_STAFF` — 배정DB 3~6월 **과거 실적 집계용**. 이름을 빼면 옛 통화가 전부 '기타'로 몰린다.
- `normalizeStaffName` / `DB_NAME_ALIAS` 의 `"김현애"→"현애"` 같은 별칭 — 남은 옛 데이터를 계속 읽으려면 필요하다.

---

## 🔍 통합 업무 화면 사전조사 결과 (2026-08-19)

"기업목록 / 파이프라인 / 기관별현황을 한 화면으로 합치자"의 사전조사.
아래 **1)·2)·3) 은 전부 조사 시점(2026-08-19) 실측**이고, **0단계 1·5번은 같은 날 실행했다**(맨 아래 ✅ 두 절).
다음 세션은 **1단계 3번(대표 단계 정하기)** 부터 이어가면 된다.
0단계는 끝났다 — 이름 일치 미연결 **132건 전부 연결 완료**, 그 과정에서 나온 **"미래 월" 함정도 코드로 해결**했다.

> 재측정 방법: `scripts/run-sql.js` 는 결과를 1개만 찍으므로(2-2 함정) **한 파일에 SELECT 하나씩** 넣어 돌린다.
> 숫자는 매일 변한다 — 인용하기 전에 그날 다시 셀 것.

### 1) 단계가 3곳에 따로 저장된다 — **세 곳은 같은 것의 사본이 아니다**

| | 저장 단위 | 어휘 | 누가 쓰나 |
|---|---|---|---|
| **A** `companies.stage` | **업체당 1값** | `STAGES` 11 + 폐지 2(`추가 진행 예정/중`) | 사람이 기업상세에서만. **자동 갱신 코드가 한 줄도 없다** |
| **B** `pipeline_cards.stage` | **(업체×기관)당 1값** | `STAGES` 11 | 드래그·일괄이동(수동) + `syncPipelineFromCase`(자동) |
| **C** `agency_cases.status` | **(업체×기관×년월)당 1값** | **35종**(일반 23 `ALL_STATUS_OPTIONS` + 구조혁신 12) | 기관현황 화면에서 사람이 입력 |

→ A는 스냅샷 1개, B는 기관별 스냅샷, **C는 월별 시계열.** 애초에 1:1 대조가 성립하지 않는다.

**어긋난 건수 (살아있는 카드 802장 중 살아있는 기업에 연결된 789장 기준)**

| 비교 | 카드 | 기업 |
|---|---:|---:|
| **A ≠ B** | **592장 (75.0%)** | **307개** |
| B ≠ C(매핑 후) | 219장 (C 있는 738장 중 29.7%) | 183개 |
| 세 곳 다 다름 | 135장 | 106개 |

> 옛 기록의 "589건"은 지금 **592건** — 줄지 않았다. 조인 순서를 바꾼 별도 쿼리로 재검증 완료(2-2 규칙).

**유형별** — (가) C 없음 51 / (나) 전부 일치 97 / **(다) B=C 인데 A만 다름 422** / (라) A=B 인데 C만 다름 64 /
(마) A=C 인데 B만 다름 20 / **(바) 전부 다름 135**.
대표 사례: (유)JB특수시스템 A`기타`·B`입금완료/사후관리`·C`완료` (다) / (주)그린준 A=B`기관신청완료/방문완료`·C`시작 전` (라) /
(주)경문도어시스템 A`기관신청대기/방문예정`·B`필수서류 및 인증서요청`·C`부결` (바).

**⚠️ 원인이 유형마다 다르다 — "사람이 안 고쳐서"가 아니다**
- **A는 아무도 자동으로 안 고친다.** → (다) 422장의 원인. B/C는 흘러가는데 A만 등록 당시에 멈춰 있다.
- **A는 애초에 담을 수가 없다.** 카드 있는 기업 391개 중 **235개가 카드 2장 이상**, 그중 **189개는 카드끼리 단계가 다르다.**
  소진공은 `부결/반려`인데 신보는 `심사중`인 기업의 `companies.stage` 에 뭘 넣어도 틀린다.
  → **A 불일치의 절반 이상은 실수가 아니라 컬럼 1개로 N개를 표현할 수 없는 구조 문제다.**
- **B≠C 는 `sync_mode` 가 전부다**: `auto` 533장 중 21장(3.9%)만 어긋남 / `manual` 205장 중 **198장(96.6%)** 어긋남.
  즉 "두 값이 싸운다"가 아니라 **"수동 고정 카드에서는 C가 `시작 전`/`보류`에 방치된다"**이다.
- 매핑 실패(`status_stage_map` 에 없는 상태값)는 **0건** — 1,822행 전부 매핑표를 통과한다.

**⚠️ B로 통일하면 잃는 정보 — 3가지, 전부 실재한다**
1. 🔴 **C의 35종 → 11종 뭉갬(되돌릴 수 없음).** 매핑표는 다대일이다.
   `상담/진단완료` ← **`시작 전`(448) + `상담/진단완료`(312)** ← **가장 뼈아프다. "손도 안 댐"과 "상담 끝냄"이 한 값이 된다.**
   `부결/반려` ← 부결362+진행불가7+중단4+마감진행불가4+반려2+신청취소2 · `실태조사완료/약정완료` ← 승인20+컨설팅최종완료9+…(6종) ·
   `자금집행완료` ← 자금집행완료198+사업전환승인18 · `기타` ← 보류207+예산소진보류7
2. 🔴 **C의 월별 이력 소멸.** (업체×기관) 조합 745개에 1,531행(평균 2.06, 최대 10). **355개 조합이 2행 이상**,
   **207개 조합은 그 안에서 상태가 실제로 변했다.** B는 조합당 1장이라 담을 자리가 없다.
3. 🟡 **`추가 진행 예정/중` 52개 기업.** B의 어떤 단계와도 대응하지 않는다 — 이 52개의 카드 단계는 9개 단계에
   골고루 흩어져 있다(기타29·상담22·부결22·기관신청완료14·입금완료15…). **단계가 아니라 별개 축의 표시**다.
   → `stage` 가 아니라 **별도 플래그로 먼저 옮겨야** 보존된다.

**손실 없음**: A의 나머지 값(B가 같은 어휘 + 기관별로 더 정확) · 카드 없는 기업 6개(㈜디스이즈·롯데제과구미대리점·
비즈팟(일반과세자)·선사현대창의사고력수학교습소·이라기타교습·천씨씨커피(방어점) — 잃는다기보다 카드를 만들어 줘야 할 대상).

### 2) 기업목록 ↔ 기관별현황 중복 저장 — **연결은 전부 "복사"이고 참조가 아니다**

살아있는 기업(397) ⨝ 살아있는 기관현황(company_id 연결분) = **1,525행** 기준.

| 의미 | `companies` | `agency_cases` | 불일치 행 | 불일치 기업 | 판정 |
|---|---|---|---:|---:|---|
| 사업자명 | `name` | `business_name` | **95** | **60** | 🔴 중복 + 오염 |
| 대표자 | `representative` | `representative` | 28 | 14 | 🔴 진짜 중복 |
| 사업자번호 | `business_number` | `business_number` | 13 | 5 | 🔴 진짜 중복 |
| 담당자 | `assignee` | `assignee` | **269** | **121** | 🟡 **의미가 다름** |
| 지역 | `region` | `region` | **227** | **88** | 🟡 의미가 섞임 |
| 업종 | `industry` | `industry` | 0 | 0 | 🔴 중복이나 ac 쪽 70행뿐 |
| 계약일 | `contract_date` | `contract_date` | 1 | 1 | 🔴 중복(384행 일치) |
| 수수료 수령여부 | `fee_status` | `fee_received` | **266** | **57** | 🔴 진짜 충돌 |
| 신용점수·상시근로자수 | `credit_score`/`employee_count` | 동명 | 0 / 1 | 0 / 1 | ⚪ 겹치는 행이 거의 없음 |
| 연락처 | `phone` | `contact_phones` | — | — | ⚪ **`contact_phones` 전 행 빈 배열 = 죽은 컬럼** |

⚠️ **중복으로 착각하기 쉬운 두 쌍 — 통합 대상이 아니다.** 처음엔 세었다가 코드·UI 라벨 확인 후 뺐다.
- `companies.issue`(이슈) ↔ `agency_cases.notes`(비고) — **다른 항목이다.**
- `companies.fee`(**수수료 %율**) ↔ `agency_cases.commission_fee`(**원 금액**) — **단위가 다르다.**

**불일치의 정체 (대표 사례)**
- **사업자명**: `agency_cases.business_name` 은 깨끗한 상호가 아니라 **시트 작업 메모가 붙은 원문**이다.
  `(주)123푸드 (신청 보류)` · `(유)누리관광_인증서 없음` · `(주)경문도어시스템(말일)` ·
  `로컬 ( 폐업관련 자료 홈택스 신청1.14 11시04분 )` · `기동정밀 / 폐업이력 없고, 졸업후보, …`
  분류: 꼬리표 붙음 63행 / 반대 14행 / 아예 다른 이름 18행.
  → **이름을 조인키로 쓰는 새 기능을 만들지 말 것. 조인키는 `company_id` 뿐이다.**
- **담당자**: 표기차(기관담당자가 기업 담당목록에 포함) 104행/59개 vs **완전히 다른 사람 164행/70개**.
  같은 회사 안에서 **기관현황끼리도 담당자가 갈리는 기업이 106개.**
  → `agency_cases.assignee` 는 중복이 아니라 **"이 기관 건은 누가 맡았나"라는 별개 정보다. 합치지 말 것.**
- **지역**: 구분자만 다름 36행 / 시군구 표기차 169행 / **시도부터 다름 12행.**
  후자는 오류가 아니라 **기관 관할 지역본부**가 섞인 것이다((주)엔에스푸드 `충남_논산` ↔ `대전지역본부`).
  전용 `branch` 컬럼이 있지만 16행만 채워져 있다.
- **수수료 수령여부**: `co=수수료수령완료 / ac=false` **251행(42개 기업)**, 반대 15행.
  251행이 나오는 이유도 **1:N** — 기업 단위 플래그 하나로는 "어느 기관 건을 받았나"를 표현할 수 없다.
- **대표자·사업자번호**: 소수지만 **진짜 오류**가 섞여 있다. (주)미러로이드 `유제정`↔`유재정`(오타) ·
  (주)로컬 `나다은`↔`최승철`(다른 사람) · (주)베누스메드 `김태영`↔`김태영(https://www.notion.so010 9476 3569)`(노션 쓰레기) ·
  (주)더푸른식품 `126-86-55980`↔`126 86 59980` · (주)솜씨협동조합 사업자번호에 **법인등록번호 혼입**.
  → **한 규칙으로 밀지 말 것. 건별로 원본 확인**(기대출 단위 정리와 같은 성격).

**두 진입 경로가 하는 일 — 둘 다 그 시점 값을 복사하고 끝난다**
- **「기관별현황에 등록」**(기업상세 하단, `App.js` 의 `기관별현황에 등록` 버튼): `agency_cases` 에 INSERT 하며
  `business_name·assignee·representative·business_number·region·contract_date` 복사 + `status:"시작 전"` 고정.
  ⚠️ **`company_id` 를 안 넣는다** → 이 버튼으로 만든 건은 전부 **미연결로 태어난다.**
- **「사업자명 검색 자동추출」**(기관현황 신규등록 모달 `selectCompany`): 위 6개 + `industry·credit_score`(kcb/nice 폴백)
  ·`employee_count` + **`company_id` 는 넣는다.** 단 복사 후 사업자명을 손으로 고치면 `onBusinessNameChange` 가
  **`company_id` 를 다시 `null` 로 푼다.**
  ⚠️ **모바일판(`MobileApp` 쪽 `selectCompany`)은 `industry`·`region` 2개만 복사하고 `company_id` 를 아예 안 넣는다** — 동작이 다르다.
- 어느 쪽도 **이후 기업정보를 고쳐도 기관현황은 따라오지 않는다.**

**연결 실태**: 살아있는 `agency_cases` 1,822 → 연결 1,531 / **미연결 291**.
그중 **이름 정확일치로 지금 바로 연결 가능한 것이 132건**(`autoLinkCompanies` 버튼을 안 눌렀을 뿐),
기업목록에 없는 이름 159건(업체명 93종). `autoLinkCompanies` 는 **이름 완전일치만** 보므로 위 꼬리표 이름들은 영원히 안 붙는다.

**⚠️ 3번째 사본**: `settlement_manual`(45행)이 `business_name·agency_group·assignee·request_amount·contract_fee·
commission_fee·contract_date·fee_received·settlement_notes` 를 **또** 들고 있다. 정산 통합 시 같이 봐야 한다.

### 3) 결론 — "B로 통일"이 아니라 **축을 셋으로 분리**

지금 문제의 뿌리는 A/B/C가 **틀려서**가 아니라 **1:N을 1:1로 우겨넣어서**다.

| 축 | 원본이 될 곳 | 다른 화면은 |
|---|---|---|
| 업체 신원(상호·대표자·사업자번호·업종) | `companies` | 참조 |
| 기관별 진행 단계 | `pipeline_cards.stage` (B) | `companies.stage` 를 없애고 기업목록은 **카드들의 종합을 계산해** 표시 |
| 기관별 월별 상태 이력 | `agency_cases.status` (C) — **원본 그대로 유지** | B는 C에서 파생된 뷰 |
| 기관별 담당자·수수료 수령 | `agency_cases` (기관 단위가 맞음) | `companies.assignee` 는 "업체 전체 담당", `fee_status` 는 폐기 후보 |

**C가 원본이고 B가 파생이라는 지금 방향(`syncPipelineFromCase`)이 맞다** — 자동 카드 533장 중 96.1%가 이미 일치한다는 게 증거다.

**작업 순서 (앞쪽일수록 안전 / 4번부터가 되돌리기 어려움)**
- **0단계 — 코드 0줄, 지금 확인만**
  1. ✅ **1차 62건 연결 완료 (2026-08-19)** / ⏸ **미래 월 70건은 일부러 남겼다** — 바로 아래 절 참고.
  2. `contact_phones`(전 행 빈 배열)·`companies.credit_score`(397중 1건)·`approved_amount`(0건)가 **죽은 컬럼**인지 확정 후 설계에서 제외.
- **1단계 — 읽기만 바꾸기(회귀 위험 낮음)**
  3. **`companies.stage` 를 "읽는 곳"부터 카드 종합으로 교체.** 기업목록 배지·대시보드 위젯·캘린더(`FOLLOWUP_STAGES`)·
     졸업/부결 판정이 전부 A를 본다(약 52곳). **컬럼을 지우지 않고도 592건 불일치가 화면에서 사라지고 되돌리기도 쉽다.**
     ⚠️ 카드가 여러 장인 기업이 235개(단계가 갈리는 건 189개) → **"대표 단계 1개"를 어떻게 정할지가 이번 설계의 핵심 결정이다**
     (가장 앞선 것? 가장 뒤? 부결 제외 최댓값?). **이걸 먼저 정하지 않고 3번을 시작하지 말 것.**
  4. **`추가 진행 예정/중` 52건을 먼저 별도 플래그로 옮긴다.** 안 하고 3번을 하면 52개 기업 표시가 조용히 사라진다.
- **2단계 — 쓰기 경로 정리(여기서부터 조심)**
  5. ✅ **완료 (2026-08-19)** — 「기관별현황에 등록」 버튼 `insertData` 에 `company_id: data.id || null` 추가. 아래 절 참고.
  6. 자동완성 복사를 **복사 → 참조 표시**로 전환. 대표자·사업자번호·업종·상시근로자수는 기관현황에서 입력칸을 없애고
     `companies` 값을 비춰주기(다르면 배지만). **단 `assignee`·`region`·`status` 는 기관현황에 남긴다 — 위 실측대로 의미가 다르다.**
  7. `fee_status` ↔ `fee_received` 충돌 57개 기업 정리. **`fee_received`(기관 단위)를 원본으로, `fee_status` 는 계산값으로 내린다.**
     1:N 이 원인이라 컬럼을 합치는 걸로는 안 풀린다.
- **3단계 — 데이터 정리(사람이 눈으로. 자동화 금지)**
  8. 대표자 28행·사업자번호 13행 → 건별 원본 확인.
  9. 사업자명 꼬리표 95행 → 꼬리표를 `notes` 로 옮기고 정규화. **단 `company_id` 연결이 끝난 뒤에.**
     이름이 사실상 조인키로 쓰이는 곳(`autoLinkCompanies`·등록 중복체크·`settlement_manual`)이 남아 있는 동안 이름을 고치면 연결이 끊긴다.
  10. 지역 표기 통일 + 정규화 함수 하나. 시도부터 다른 12행은 지역본부인지 실제 오류인지 사람 확인.
- **마지막** — `companies.stage` **컬럼 삭제는 3단계까지 끝내고 몇 주 굴려본 뒤.** 읽는 곳 52군데가 다 계산값으로
  넘어간 걸 확인하기 전에 지우면 화면이 통째로 빈다.

**❌ 절대 하면 안 되는 것 (전부 위 실측 근거 있음)**
- **C를 B로 덮기** → 상태값 35종이 11종으로 뭉개지고(`시작 전` 448 + `상담/진단완료` 312가 한 값), 355개 조합의 월 이력이 사라진다.
- **A를 B로 일괄 UPDATE** → 카드가 여러 장인 235개 기업은 어떤 값을 넣어도 틀리고 `추가 진행 예정/중` 52건이 소멸한다.
- **담당자·지역을 한쪽으로 합치기** → 70개 기업은 기관별로 진짜 담당자가 다르고, 지역엔 기관 지역본부가 섞여 있다.
- **이름으로 두 테이블을 잇는 새 기능** → `business_name` 의 18%가 작업 메모가 붙은 원문이다.

### 🗓 미래 월 행은 "최신"으로 치지 않는다 (2026-08-20, **DB 스키마 변경 0건**)

**이게 이번 조사에서 새로 발견한 가장 큰 함정이다.** 위 0단계-1 을 하다가 드러났다.

`syncPipelineFromCase`/`resyncAutoCards` 는 (회사×기관)에서 **연·월이 가장 뒤인 행**을 진실로 본다.
원래는 "과거 월을 고쳤다고 카드가 되돌아가지 않게" 하려는 장치인데(2026-07-27), **미래 월에는 방어가 없었다.**
실무에서는 **다음 달 신청 건을 미리 `시작 전` 으로 등록**한다(2026-08-18~19 에 2026-09 46행·2026-11 34행 등록).
그 계획 행이 최신이 되면 → `상담/진단완료` 로 매핑 → **이미 진행된 카드가 뒤로 끌려간다.**

**공용 헬퍼 2개** (`App.js`, `isNewerCase` 바로 위)
- `caseYm(c)` = `year*100+month`(없으면 0) · `isFutureCase(c)` = 오늘(KST)이 속한 달보다 뒤인가

**필터를 건 곳은 "최신 행을 고르는" 3곳뿐이다**
| 함수 | 무엇을 바꿨나 |
|---|---|
| `syncPipelineFromCase` | 기준 행(`eff`) 선정에서 미래 월 제외. **`c` 자체가 미래면 `eff=null`** → 카드 단계를 안 건드린다 |
| `resyncAutoCards` | `latest` 집계에서 `if (isFutureCase(c)) return;` |
| `toggleSyncMode`(수동→자동) | 최신 행 고를 때 미래 월 skip |

- ⚠️ **`isNewerCase` 자체는 손대지 않았다.** 그건 "정렬 규칙"이라 시간 개념을 넣으면 회귀 사정권이 넓어진다.
  **필터는 반드시 호출부에서 걸 것** — 최신 행을 고르는 새 경로를 만들면 여기에 같이 추가해야 한다.
- ⚠️ **`eff` 가 null 일 수 있다.** `eff.status` 를 그냥 읽던 3곳을 `eff && eff.status` 로 고쳤다
  (`if (eff && eff.status)` 2곳 · `needs_mapping: !stage && !!(eff && eff.status)`). 안 고치면 **미래 월 행 저장 시 크래시.**
- ⚠️ **카드 신규 생성은 그대로 뒀다.** 새 (회사×기관) 조합이 카드 없이 남으면 보드에서 통째로 사라진다.
- ⚠️ `eff === null` 일 때 **`needs_mapping` 도 세우지 않는다** — 매핑이 없는 게 아니라 아직 판정할 때가 아닌 것이라,
  화면에 ⚠ 를 띄우면 오해를 부른다.
- **화면(기관현황 목록·필터·정산)에는 미래 월 행이 그대로 보인다.** 이 규칙은 **파이프라인 단계 판정 전용**이다.
- 기준이 "오늘"이라 **시간이 지나면 저절로 풀린다** — 9월이 되면 2026-09 행이 정상적으로 최신이 된다.

**[재동기화] 버튼 안전성 — 실측 대조 (2026-08-20, 132건 전부 연결된 상태)**
| | 옛 규칙 | **새 규칙** |
|---|---:|---:|
| 변화 없음 | 492 | **523** |
| **⬅ 뒤로** | **40** | **4** |
| ➡ 앞으로 | 1 | 6 |

→ **미래 월 유령 후퇴는 사라졌다.** 남은 뒤로 4장(뚱이농산·선진무역·유일목형·프로페나인어클락)은
근거가 전부 **2026-07·08 행**이라 유령이 아니라 **카드와 기관현황의 실제 불일치**다.
재동기화의 본래 목적이 "기관현황으로 카드를 다시 유도하는 것"이라 이건 정상 동작이다.
⚠️ 다만 **선진무역**(자금집행완료 → 기관신청대기/방문예정)처럼 낙차가 큰 건은 **기관현황 쪽이 안 갱신된 것**일 수 있으니,
버튼을 누르기 전에 이 4건은 사람이 한 번 보는 게 좋다.

### ✅ 0단계-1 실행 — 미연결 132건 전부 연결 완료 (1차 62건 2026-08-19 / 2차 70건 2026-08-20)
(SQL: `기관현황_미연결_company_id_연결_62건.sql` · `기관현황_미연결_company_id_연결_70건_미래월.sql` / 각 `_rollback.sql`)

**두 번에 나눠 실행했다.** 1차는 현재·과거 월 62건, 2차는 **위 미래 월 규칙을 고친 뒤** 미래 월 70건.
⚠️ **2차 SQL 은 `App.js` 의 `isFutureCase` 변경과 한 쌍이다.** 코드 없이 SQL 만 돌리면 카드가 뒤로 끌려간다.
⚠️ `resyncAutoCards` 는 `company_id` 없는 행을 **일부러 건너뛴다**(`if (!c.company_id) return;`) →
**미연결이 곧 방패였다.** 앞으로 미연결 건을 연결할 때는 그 방패가 사라진다는 걸 알고 할 것.

**실행 방식** — 이름 매칭은 파일을 만들 때 끝내고 `(case_id, company_id)` 쌍을 **명시적으로 박았다.**
`where company_id is null and deleted_at is null` 가드로 그 사이 바뀐 행은 조용히 건너뛴다.
같은 이름의 살아있는 기업이 2개 이상인 건(모호)은 **실측 0건**이라 제외 대상이 없었다.

**검증 (실행 파일과 별도 조회로 재확인 — CLAUDE.md 2-2 규칙)**
- 미연결 291 → 229(1차) → **159**(2차) · 연결됨 1,531 → 1,593 → **1,663** (합계 +132)
- **62/62 · 70/70 이 의도한 기업에 정확히 연결** · 엉뚱한 기업 0 · 이름 불일치 0
- **파이프라인 카드 802장 → 802장, 지문(id+stage+stage_changed_at+sync_mode) `430031e2…`**
  1차 전 · 1차 후 · 코드 수정 후 · 2차 후 **네 번 모두 동일** → **카드는 한 장도 만들어지거나 움직이지 않았다.**
  (연결 전 실측: 132건 전부 이미 해당 (기업×기관) 카드 보유, 휴지통 슬롯 점유 0건이라 unique 위반 위험도 없었다)
- **남은 미연결 159건은 기업목록에 아예 없는 이름**(업체명 93종)이라 자동으로는 못 붙는다.
  꼬리표 정리(3단계 9번) 후에나 가능하다.

**연결했지만 값이 어긋나는 건 4건 (같은 회사가 맞아 그대로 뒀다)**
(주)한산테크 대표자에 노션 URL 쓰레기 · 맥스틸우드 2건 공동대표 중 1명만 기재(사업자번호 동일).
⚠️ **(주)현대아트모아**는 사업자번호가 `133-81-30034`(기업) ↔ `133-81-30346`(기관현황) 로 갈린다.
이름·대표자가 같아 연결은 했지만 **어느 쪽이 오타인지 원본 확인이 필요하다.**

### ✅ 0단계-5 실행 — 「기관별현황에 등록」 버튼이 `company_id` 를 넣는다 (2026-08-19, **DB 변경 0건**)

`CompanyModal` 하단 [기관별현황에 등록] 의 `insertData` 에 **`company_id: data.id || null` 한 줄** 추가.
예전엔 이 줄이 없어 **이 버튼으로 만든 건이 전부 미연결로 태어났고**(미연결 291건의 주 공급원),
이름이 완전히 같을 때만 `autoLinkCompanies` 로 겨우 붙었다 — 꼬리표가 붙는 이름은 영영 안 붙는다.

- ⚠️ `companyName` 은 `nameInput || data.name` 이라 **`data.name` 과 다를 수 있다.** 그래도 지금 열려 있는
  기업의 `data.id` 로 잇는 게 맞다 — 이름보다 정확하다.
- `data.id` 는 항상 있다(신규 등록도 DB insert 결과로 모달을 연다). `|| null` 은 방어용.
- **중복 체크(`business_name`+기관+년월)는 일부러 안 건드렸다.** 판정 기준을 `company_id` 로 바꾸면
  지금까지 등록되던 건이 조용히 "중복"으로 건너뛰어질 수 있다.
- 이 버튼은 **`syncPipelineFromCase` 를 부르지 않는다**(카드를 만들지 않는다) — 예전과 동일하다.
- `CI=true npx react-scripts build` 통과(main.js +4 B).

---

## 🔐 이만나미 계정 권한 정리 (2026-09-11, **새 테이블 0개 · 새 쿼리 0개 · App.js 로직 0줄**)

2026-08-27 에 가입했는데 **하드코딩 명단 어디에도 없어서** 개인팀 채팅·정산 작성이 막혀 있었다.
`profiles` 행은 멀쩡했다(`status='approved'`) — **DB 는 아무것도 막지 않았고 전부 App.js 명단 문제였다.**

### ⚠️ 이 앱에서 `profiles.role='admin'` 은 거의 아무 권한도 주지 않는다
모든 게이트가 **이름 배열**이라, 이만나미는 DB 상 관리자인데 일반 팀원보다 접근이 좁았다.
정원도 admin 이지만 `SETTLEMENT_ADMINS` 에 없어 정산은 본인 담당만 본다 — 같은 계열이다.
→ **새 계정을 만들 때 `role` 만 주면 아무 일도 안 일어난다. 이름 명단에 넣어야 한다.**

### 고친 곳 — 명단 3개 + DB 2건. **판정 함수는 한 줄도 안 건드렸다**
| | 무엇 | 왜 |
|---|---|---|
| `App.js:1621` `ASSIGNEES` | 이만나미 추가 | 담당자 드롭다운에 자기 이름이 없어 **정산에 자기 담당 건을 만들 수조차 없었다** |
| `App.js:1675` `TEAM_MEMBERS.individual` | 〃 | 팀 공지 "확인" 대상 (개인팀 5→6명) |
| `App.js:4338` `CHAT_TEAMS.individual` | 〃 | 개인팀 채널이 `teamChannels` 에서 통째로 빠져 **탭이 렌더조차 안 됐다** |
| DB `chat_can_access()` | 이만나미 추가 + **현애·인선·미현 제거** | App.js 와 한 쌍. 위 🚧 보류 항목을 같이 해소 |
| DB `profiles.role` | `admin` → `member` | 팀원 관리 메뉴 + 남의 계정 수정·삭제 권한 제거 |

- 💰 **`SETTLEMENT_ADMINS` 에는 일부러 넣지 않았다**(사용자 결정: 본인 담당 건만).
  `canViewSettlement` 가 이미 "본인 담당만"이라 **코드 수정이 필요 없었다** — `ASSIGNEES` 에
  이름이 생기니 담당자로 배정만 하면 보인다. ⚠️ 배정 전에는 **정산 화면이 0건으로 보인다**(정상).
- ⚠️ **"전체채팅 접근 불가"는 실재하지 않는 증상이었다.** `canAccessChannel` 은 `general` 이면
  무조건 true 고, DB 도 같다. 실측: 이만나미가 **2026-09-11 에 general 로 메시지 3건을 실제로 보냈다.**
  → 증상 신고를 코드보다 먼저 믿지 말 것. 실제로는 "채널 목록에 전체 하나만 보인다"였다.
- ⚠️ **`role` 변경은 관리자 컨텍스트로 감싸야 먹는다**(2-4). 안 감싸면 `trg_protect_profile` 이
  **에러 없이 조용히 원복**한다. 정원 uuid 로 감쌌다(본인 uuid 로 하면 그 UPDATE 가 성공하는
  순간 `is_admin()` 근거가 사라진다).

### 일부러 안 건드린 것
- `DB_CALL_STAFF`(3406) · `DB_NAME_ALIAS`/`normalizeStaffName` 의 `"김현애"→"현애"` 별칭 —
  퇴사자 이름이 남아 있지만 **과거 실적 집계·옛 데이터 읽기에 필요하다**(맨 위 🚧 절과 같은 이유).
- `SETTLEMENT_ADMINS` · `WN_ADMINS` · `WEEKLY_REVIEW_MEMBERS` — 의미가 다른 명단이라 손대지 않았다.
- `wn_visible_names()` 의 미현↔인선 공유 그룹 — 🚧 보류 항목으로 **아직 남아 있다**(위 절 참고).

### 검증
- `node scripts/test-imannami-access.mjs` **45/45** — 판정 함수·명단을 **App.js 소스에서 떼어내 실행**하고,
  **App.js ↔ SQL 파일 ↔ 라이브 DB 함수 정의 3곳의 명단이 글자 단위로 같은지** 대조한다.
  이름이 `profiles.name` 과 다르면 조용히 실패하므로 DB 와도 대조한다.
  ⚠️ **명단이 바뀌면 이 테스트의 `EXPECT_*` 도 같이 고칠 것** — 그게 이 테스트의 목적이다.
- 별도 검증 SQL(조치 파일에 딸린 SELECT 가 아니라 — 2-2): `이만나미_권한정리_검증.sql` →
  이만나미 권한으로 RLS 를 실제 적용해 **`general` 189건 + `individual` 175건**이 보이고,
  `is_admin()=false` · 퇴사자 3명 팀 채널 전부 false · 함수에 퇴사자 0명.
- 기존 테스트 8종 전부 통과(nocard 5/5 · note-links 52/52 · amount-unit 42/42 · revenue-input 14/14 ·
  growth-roadmap 7/7 · status-comment 37/37 · gujo-reject 16/16 · weekly-members 12/12) ·
  `audit-select-columns` **0건** · `CI=true npx react-scripts build` 통과(main.js +25 B).
- `src/App.js` diff **배열 3줄 + 주석뿐** — 판정 함수(`canAccessChannel`·`canViewSettlement`)는 0줄.
- ⚠️ **화면 클릭 확인은 사람이 해야 한다** — 이만나미 계정으로 로그인해 개인팀 탭 · 정산 작성 ·
  팀원 관리 메뉴 사라짐을 눌러 볼 것. 에이전트가 운영 DB 로 재현하면 안 된다(2026-08-17 사고).

## 1. 수정 전 — 공용 로직 영향 범위 먼저 확인
- 바꾸려는 함수/변수/상태(state)를 **어디서 쓰는지 grep으로 전부 찾고**, 이 변경이
  요청한 기능 외 다른 기능에도 영향을 주는지 먼저 판단한다.
- 특히 아래는 "공용"이라 조심:
  - 체크박스/완료 상태 변경 로직 (`toggleDone`, `markDone`, `is_done`, `status`)
  - 표시 필터 (`displayNotes`, `doneCount`, `showDone` 등 목록 필터/집계 useMemo)
  - 이름 정규화(`normalizeName`), 날짜(`kstDate`), 태그(`MentionField`)
- **상태 값이 다른 의미인지 구분**: 이번 버그의 원인은 `team_notes.status`의
  `taken`(가져감=진행중)과 `done`(완료)을 같은 것으로 취급한 것.
  - `work_requests.status`: `pending` → `read` → `done` (업무 요청 흐름)
  - `team_notes.status`: `open`(대기) → `taken`(진행중) → `done`(완료)
  - 두 흐름은 **다른 테이블·다른 의미**다. 절대 섞지 말 것.

## 2. push 전 — 회귀(regression) 자가 체크리스트
"이번에 요청한 기능"뿐 아니라 "이번 수정으로 영향받을 수 있는 기존 기능"도
스스로 목록을 만들어 코드로 확인한 뒤 push한다. 최소 확인 대상:

- [ ] 업무노트: 개인화 / 자동제목 / 이월 / 대기사유 / @업체 태그
- [ ] 업무 요청: 보내기 · 가져가기 · 완료 체크가 **각각 독립적으로** 동작
- [ ] 빠른 메모 · 빠른 업무
- [ ] 실시간 팀 채팅
- [ ] 팀 업무 공간: 완료 자동 숨김 / 공지 확인 / 우선순위 / 공지 고정
- [ ] 졸업후보기업 판별
- [ ] 기업목록 인라인 편집
- [ ] 대시보드 위젯
- [ ] 모바일 `/m` 탭
- [ ] `CI=true npx react-scripts build` 통과(빌드 에러 0)

### 2-1. 파일 업로드/파싱 기능은 "실제 파일"로 검증
- 엑셀/시트지 파싱을 건드리면 **여러 형식의 실제 파일**로 확인한다:
  - 순정 MS Excel 파일 (기존 정상 동작 회귀 확인 — 폴백 경로를 타면 안 됨)
  - 한셀/한컴오피스로 만든 xlsx (네임스페이스 접두사 + `hs:` 확장 → 정규화 폴백)
- 확인 방법: `node`로 `node_modules/xlsx`를 직접 로드해 실제 파일을 파싱, 추출 행/필드 수를 눈으로 확인.

## ✅ `work_notes` 개인노트 비공개 — 2026-08-05 조치 완료
(2026-08-01 발견 → 2026-08-05 적용. SQL: `업무노트_개인노트_비공개_RLS.sql` / 되돌리기 `_rollback.sql`)

예전 문제: 정책이 `p_work_notes_all`(ALL/`authenticated`/`is_approved()`) 하나뿐이라 사용자 스코프가 없어,
승인된 로그인 사용자면 누구나 남의 노트를 전건 조회할 수 있었다(화면 `wnViewable` 필터로만 가려짐).

지금 상태 — 정책 4개(select/insert/update/delete)로 교체. 열람 범위는 **App.js `wnViewable()`과 동일**:
양호=전원 / 나머지=본인 담당(assignee)만. **예외 하나: `company_id`가 붙은 노트는 팀 공유**
(기업 상세 타임라인용, 2026-08-05 기준 401건 중 5건).

> ⚠️ 2026-08-17: 미현↔인선 공유 그룹은 **없어졌다**(둘 다 담당자 명단에서 제거).
> App.js 는 `WN_SHARE_GROUPS = []` 로 반영 완료. **SQL `wn_visible_names()` 는 아직 옛 상태 — 보류 중.**
> `담당자정리_업무노트RLS_공유그룹해제.sql` 을 돌려야 한 쌍이 맞는다. → 맨 위 **🚧 보류 중** 참고.

**앞으로 work_notes를 건드릴 때 반드시 지킬 것**
- 열람 규칙을 바꾸면 **App.js `wnViewable`/`WN_ADMINS`/`WN_SHARE_GROUPS`와 SQL `wn_visible_names()`/`wn_is_admin()`을
  같이** 고친다. 한쪽만 고치면 "화면엔 있는데 DB가 안 준다"(또는 그 반대)가 된다.
- **남의 노트를 읽어야 하는 새 기능은 만들지 말 것.** 필요하면 숫자만 주는 `security definer` 함수로:
  - `wn_team_unfinished()` — 담당자별 미완료/3일↑방치/최고참 날짜 (대시보드 '담당자별 미완료 현황')
  - `wn_activity_counts(since)` — 담당자별 노트 수정 건수·마지막 활동 (팀 활동 위젯)
  - ⚠ `wn_team_unfinished()`의 미완료 판정 정규식은 `parseUnfinishedItems`를 SQL로 옮긴 것이다.
    **둘 중 하나만 고치면 숫자가 어긋난다.** 고쳤으면 JS↔SQL 집계를 다시 대조할 것(2026-08-05엔 10명 전원 일치).
- **남의 노트에 쓰는 기존 기능**(업무 요청·빠른 업무)은 `wn_append_todo(담당자, 날짜, 줄, 기업id, 제목)` 경유.
  직접 `select` 후 `update` 하면 상대 노트를 못 찾아 **같은 날 노트가 두 장** 생긴다.
- ⚠️ `insert(...).select()`는 **넣은 행이 SELECT 정책에 걸리면 42501 에러**가 난다(0건이 아니라 에러).
  남의 담당으로 노트를 새로 만드는 경로는 지금 **양호 전용(`isWnAdmin`)뿐이라** 문제가 없다.
  일반 사용자도 남에게 노트를 만들게 하려면 `.select()`를 빼거나 전용 함수를 만들 것.
- 함수는 `EXECUTE` 기본값이 PUBLIC이다 → 새로 만들면 **`revoke ... from public, anon` + `grant ... to authenticated`**.

## ✅ `chat_messages` DM 비공개 — 2026-08-10 조치 완료
(2026-08-02 발견 → 2026-08-10 적용. SQL: `채팅_DM_비공개_RLS.sql` / 되돌리기 `_rollback.sql` / 동작검증 `채팅_DM_비공개_동작검증.sql` 26/26 통과)

예전 문제: 정책 3개의 조건이 `is_approved()`뿐이라 **채널 스코프가 없었다**. 채널 판정은 화면 `canAccessChannel()`뿐이라
`fetchChatUnread`가 채널 조건 없이 본문까지 1000건을 받아오면서 **승인된 사람 누구나 남의 DM 본문**을 브라우저로 받았고,
**남의 메시지 수정·삭제도 DB에서는 열려** 있었다(work_notes 개인노트와 같은 계열 — `sender`가 이름 텍스트라 `auth.uid()`와 직접 비교가 안 됨).

**앞으로 chat_messages를 건드릴 때 반드시 지킬 것**
- 채널 규칙은 **App.js `canAccessChannel`/`CHAT_TEAMS`(App.js:3578)와 SQL `chat_can_access(channel, me)`가 한 쌍**이다.
  팀 목록은 `profiles.team`이 아니라 **App.js 하드코딩 배열이 원본**이다.
  한쪽만 고치면 "화면엔 채널이 있는데 메시지가 안 온다"가 된다. 고쳤으면 동작검증을 다시 돌릴 것.
  > ⚠️ 2026-08-17: `CHAT_TEAMS`에서 현애·인선·미현을 뺐다(App.js 반영 완료).
  > **SQL `chat_can_access()`는 아직 옛 명단 — 보류 중.** `담당자정리_채팅RLS_명단반영.sql`을 돌려야 한 쌍이 맞는다.
  > (한쪽만 빠진 지금은 "화면엔 안 보이는데 DB는 아직 허용" 방향이라 남은 사람 기능이 깨지진 않는다.)
  > → 맨 위 **🚧 보류 중** 참고.
- **관리자(양호)도 남의 DM은 못 본다.** work_notes와 달리 전체 열람 예외가 없다(App.js에도 없다).
  `chat_is_admin()`은 "내가 볼 수 있는 채널에서 남의 메시지 삭제"에만 쓴다.
- 읽음표시(`read_by`)는 **남의 메시지에 쓰는 게 정상**이라 UPDATE를 본인 것으로 좁힐 수 없다.
  → 채널까지만 정책으로 막고, 그 안쪽(남의 본문 수정·삭제, sender/channel 바꿔치기)은 **트리거 `trg_chat_protect_update`**가 막는다.
- 트리거는 `auth.uid() is null`이면 통과시킨다 — service_role·`run-sql.js` 같은 관리 작업을 막지 않기 위해서다.

## ⚠️ `team_notes` 는 work_notes·DM 과 **같은 계열이 아니다** — 2026-08-11 확인
(SQL: `팀노트_사칭변조_방지.sql` / 되돌리기 `_rollback.sql` / 동작검증 `_동작검증.sql` 8/8 통과)

**팀 구분은 "접근 권한"이 아니라 "분류"다.** App.js `TeamNotesSection` 은 `activeTab` 으로
법인/개인/전체 탭을 전환해 **누구나 모든 팀 노트를 본다**(App.js:27541). 화면과 DB 가 이미 일치한다.
→ **RLS 를 팀 스코프로 조이면 화면이 통째로 깨진다. 조이지 말 것.**
(`teamRoster`/`TEAM_MEMBERS` 는 "공지 확인 대상 명단"이지 열람 권한이 아니다. `teamKeyOfProfile` 은
팀 업무 저장 팝업의 기본값 전용이다. 셋 다 열람 필터가 아니다 — 헷갈리기 쉬우니 주의.)

열람 쪽 실측: RLS on / 정책은 `p_team_notes_all` 하나(`to authenticated`, using·with_check 둘 다
`is_approved()`) / anon 권한 0건. 비로그인 노출 없음.

그래서 막은 것은 열람이 아니라 **쓰기 쪽 사칭·변조**다(chat 의 `trg_chat_protect_update` 와 같은 계열):
- `trg_team_notes_protect` — INSERT 시 `posted_by` 를 본인으로 강제, UPDATE 시 `posted_by`·`team` 을 조용히 원복.
  ⚠ 확인(`read_by`)·가져가기·완료(`status`)·체크리스트·내용수정·soft delete 는 **전부 통과**한다.
  남의 노트에 쓰는 게 정상인 기능이라 UPDATE 를 본인 것으로 좁힐 수 없다(chat 과 같은 이유).
- `trg_team_notes_no_hard_delete` — 하드 DELETE 차단(앱은 전부 `deleted_at` soft delete). 관리자도 못 지운다.
- 두 트리거 모두 `auth.uid() is null` 이면 통과 — service_role·`run-sql.js` 관리 작업을 막지 않기 위해서다.

**앞으로 team_notes 를 건드릴 때**: `posted_by`·`team` 을 바꾸는 UPDATE 경로나 하드 DELETE 경로를
새로 만들면 트리거에 막힌다. 정말 필요하면 트리거를 같이 고치고 동작검증을 다시 돌릴 것.

### ⚠️ team_notes 에는 "누가 수정했는지"가 없다 — 알림에서 본인 제외를 DB 로 못 한다 (2026-08-16)
`posted_by` 는 **카드를 처음 만든 사람**이고, 항목 추가·수정은 **누구나** 할 수 있다.
채팅(`sender`)·업무요청(`request_from`)처럼 행 값만 보고 "내가 한 일"을 걸러낼 수가 없다.
→ 그래서 **저장하는 브라우저가 저장 직전에 노트 id 를 모듈 전역 `_teamNoteSelfEdit` 에 찍고**
(`markTeamNoteSelfEdit`), 되돌아온 Realtime UPDATE 를 10초간 건너뛴다(`isTeamNoteSelfEdit`).
저장하는 곳(`TeamNotesSection`)과 알림을 내는 곳(App)이 **다른 컴포넌트라** 모듈 전역에 뒀다.
- **항목이 늘어난 저장에만 찍는다.** 제목만 고친 저장까지 찍으면 그 10초 동안 남이 추가한 항목을 놓친다.
- 팀 업무 **항목 추가 알림은 "체크리스트 항목 수가 늘어난 UPDATE"만** 낸다(`handleTeamItemAdded`).
  확인(`read_by`)·가져가기·완료·내용수정은 항목 수가 그대로라 자동으로 걸러진다 — 이게 과다알림을 막는 유일한 장치다.
  판정 근거인 직전 개수는 `teamItemCountRef`(노트 id → 개수)에 있고, **구독 시작 스냅샷과 INSERT 이벤트에서 채운다.**
  둘 중 하나라도 빠지면 그 카드의 **첫 항목 추가를 통째로 놓친다**(비교 기준이 없어 조용히 return).
- `read_by` 는 **일부러 검사하지 않는다** — 이미 확인한 카드라도 새 항목이 붙으면 알려야 한다.
  (INSERT 알림과 다른 점. 거기선 "이미 확인함"이 곧 "이미 본 카드"였다.)
- ⚠️ **이 알림은 실사용 테스트가 팀 전원에게 울린다.** `read_by` 를 안 보므로 CLAUDE.md 의
  "나만 테스트하는 법"(read_by 에 남들 이름 미리 넣기)이 **여기엔 통하지 않는다.**

## 📅 팀 일정 항목 — 2026-08-18 신설 (**새 테이블·새 컬럼 0개**)

실태조사·기관방문 같은 예정 일정을 등록하고, 그 날짜 팀 업무에 떠서, 다녀온 뒤 체크하며 결과를 적는 기능.

**`calendar_events` 를 쓰지 않았다.** 그 테이블은 구글 캘린더 미러라(`sheet`=양호/이사, `color`=구글 colorId,
**하드 DELETE**) 이 용도에 컬럼 7개를 더 붙여야 하고, 무엇보다 팀 업무 화면이 **두 소스를 합쳐 그리게 되어**
밀린 미완료·월별 접기·이월이 전부 `team_notes.checklist` 기준인 지금 구조에 예외 분기가 곳곳에 생긴다.

**대신 체크리스트 항목(`team_notes.checklist`, jsonb)에 필드를 얹었다 — 그래서 SQL 실행이 0건이다.**
```js
{ id, text, taken_by, taken_at, taken_work_note_id, done,      // ← 기존 필드 (그대로 둔다)
  is_sched: true, sched_kind, company_id, company_name, assignee,
  done_at, done_by, result }                                    // ← 일정 전용
```
- **항목이 곧 그 날짜 팀 카드의 항목**이라 "그 날짜 팀 업무노트에 뜬다"가 구조상 자동이다.
- ⚠️ **`text` 를 반드시 채운다**(`schedItemText()` = "실태조사 · (주)입체로").
  이월 표시·밀린 미완료 정렬·항목추가 알림 본문이 전부 `text` 를 읽는다. 비우면 그 셋이 빈칸이 된다.
- ⚠️ 기존 항목엔 `is_sched` 가 없다(undefined) → 일정 분기를 안 타서 **옛 데이터는 그대로** 그려진다.

**저장 경로는 이월(`confirmTeamCarry`)과 같은 것을 쓴다** — (팀 × 날짜)의 살아있는 미완료 카드를 찾아
`checklist` 에 붙이고, 없으면 그 날짜 카드를 새로 만든다. `order(created_at asc).limit(1)` 로 합치기 기준을
하나로 고정한다. **다른 규칙으로 새 경로를 만들면 같은 날 카드가 두 장씩 생긴다.**

**`done`(다녀옴) 과 `taken_by`(가져가기) 는 다른 축이다 — 섞지 말 것.**
- 그래서 일정 항목에는 **가져가기 버튼을 두지 않았다.** `taken_by` 가 붙으면 기존 렌더가 취소선을 씌워
  `done` 체크와 표시가 겹쳐 무엇이 끝난 건지 알 수 없게 된다. 일정에는 **체크박스 + 연기(이월)** 만 둔다.
- ⚠️ `teamOverdue`(밀린 미완료)의 판정 기준은 `taken_by` 였다 → **`if (it.done) return;` 을 넣었다.**
  안 넣으면 "다녀와서 체크까지 했는데 계속 밀린 걸로 뜨는" 상태가 된다.
  일반 항목은 `done` 을 true 로 만드는 코드가 없어(항상 false) 영향이 없다 — 바꾸기 전에 grep 으로 확인했다.
- 미래 일정은 카드의 `work_date` 가 오늘 이후라 **밀린 미완료에 애초에 안 들어간다**(그 구역은 기준일이
  오늘보다 이전인 카드만 본다). 화면에는 파란 `예정` 배지로 구분한다.

**이월(=연기) 하면 종류·업체·담당자를 같이 옮긴다**(`confirmTeamCarry` 의 `newItem`).
안 옮기면 연기한 순간 평범한 글줄이 된다. 단 **결과·완료 표시는 일부러 안 옮긴다** — 연기된 일정은 아직 안 다녀온 것이다.

**알림** (사용자 결정: 그대로 알림 보냄)
- 일정을 만들면 체크리스트 항목이 늘어나므로 기존 `handleTeamItemAdded` 가 팀원에게 "➕ 팀 업무 항목"을 쏜다.
  내 화면만 `markTeamNoteSelfEdit` 로 억제한다.
- **체크·결과 저장은 항목 수가 그대로**라 알림이 안 나간다 → `markTeamNoteSelfEdit` 도 찍지 않는다.
  (찍으면 그 10초 동안 남이 그 카드에 추가한 항목을 놓친다 — 위 team_notes 절 참고.)

**안 만든 것**: 구글 캘린더 연동 · 캘린더 화면 표시 · 일정 전용 모아보기.
완료 기록은 항목 안(`done_at`/`done_by`/`result`)에 남고 월별 접기로 과거 조회가 된다.
나중에 캘린더에도 띄우려면 그때 `calendar_events` 복사를 붙이면 되지만, **두 곳 동기화 규칙부터 정할 것.**

## 📦 업무노트·팀 업무 항목 = 작은 카드 박스 (2026-08-18, **표시만 변경 · 로직 0줄**)

항목이 한 줄로 화면 끝까지 늘어나 이월·가져가기 버튼이 오른쪽 멀리 떨어졌다 → 항목 하나를 작은 박스로 만들어
**버튼을 내용 바로 아래에 붙였다.** 저장·판정 함수는 하나도 안 건드렸다(핸들러 호출 인자까지 동일).

**공용 헬퍼 4개 (`App.js` 최상단, `parseUnfinishedItems` 바로 위)** — 새 항목 목록을 만들면 이걸 쓸 것.
`itemGridStyle(extra)` · `itemBoxStyle(extra)` · `ITEM_BOX_ACTIONS` · `itemGridMaxHeight(count, perRow, rows)`

- ⚠️ **열 수를 숫자로 박지 말 것.** 이 저장소는 인라인 스타일뿐이고 **CSS 미디어쿼리가 없다.**
  그래서 `repeat(auto-fill, minmax(ITEM_BOX_MIN, 1fr))` 로 **컨테이너 폭이 스스로 열 수를 정하게** 했다
  → 데스크톱 3열 / 좁으면 2열 / 모바일 1열이 분기 없이 자동. 폭을 바꾸려면 `ITEM_BOX_MIN`(260) 한 곳만 고친다.
- 박스는 **세로 3단**(① 체크박스+내용 ② 배지 ③ 버튼). 버튼 줄은 `marginTop:"auto"` 라 박스 높이가 달라도
  항상 맨 아래에 붙는다. 그리드는 `alignItems:"stretch"` 로 같은 행 높이를 맞춘다.
- **전체 너비를 유지하는 예외 2가지** — `gridColumn: "1 / -1"`:
  ① 📩 팀원 업무요청 항목(답장 스레드·입력창이 붙어 있어 좁은 박스에선 못 쓴다)
  ② 체크박스가 아닌 일반 글줄(`item.isCheck === false` — 머리글·구분 역할)
- ⚠️ **스크롤 상한은 "항목 개수"가 아니라 "행 수"로 환산한다.** 팀 체크리스트의 옛 기준
  "5개 초과 → 165px" 를 3열에 그대로 쓰면 2행도 안 돼 잘리는 위치가 이상해진다 →
  `itemGridMaxHeight(cl.length, 1, 3)`. ⚠️ 2026-08-18 카드 그리드 복원 후 팀 카드 안은 **1열**이라 3열 가정이면 안 된다.
- 적용된 5곳: 팀 카드 체크리스트 · 팀 일정 항목(`renderSchedItem`) · 팀 밀린 미완료 ·
  개인 노트 체크리스트(`NoteCard`) · 개인 밀린 미완료.
  **모바일은 `MobileApp` 이 `TeamNotesSection` 을 그대로 렌더**하므로 팀 업무가 같이 바뀐다.
  개인 업무노트는 모바일이 자체 UI라 영향 없다(`WorkNotesView` 는 데스크톱 전용).
- ⚠️ JSX 삼항(`checklist ? ( ... ) : ( ... )`) 안 첫 줄에 `{/* ... */}` 를 넣으면 **빌드가 깨진다**
  (단일 표현식 자리라 형제가 못 온다). 그 자리 주석은 `//` 로 쓴다.


### 🗂 2026-08-18 (같은 날, 이어서) — 날짜 카드는 다시 "작은 카드 그리드"로 되돌렸다
위 박스화는 **항목**만 손봤는데, 정작 넓어 보이던 원인은 그 바깥의 **날짜 노트 카드**였다.
`584b1b5`(밀린 미완료 상단 고정)에서 카드 목록 4곳을 `repeat(auto-fill, minmax(...))` → 세로 1열 스택으로
바꿨던 것을 **그 커밋 이전 값 그대로** 되돌렸다(개인 3곳 320px·gap 12 / 팀 1곳 280px·gap 10).

- **되돌린 것은 그 4줄뿐이다.** 밀린 미완료·월별 접기·완료 카드 접힘·이월·일정은 전부 그대로 살아 있다
  (diff 에 style·주석 외 변경 줄 0건).
- ⚠️ **그리드 안에서 전체 너비를 써야 하는 줄에는 `gridColumn: "1 / -1"` 이 필요하다.** 안 붙이면 한 칸으로 쪼그라든다:
  `renderMonthHeader` · `renderCollapsedNote`(개인) · 팀 월 헤더 · 팀 완료 카드 한 줄 — **4곳**.
  카드 목록에 새로 "줄 형태" 요소를 넣을 때도 같이 붙일 것.
- ⚠️ `itemGridStyle` 의 최소폭을 `minmax(min(ITEM_BOX_MIN, 100%), 1fr)` 로 바꿨다.
  `min(...,100%)` 이 없으면 **컨테이너가 260px 보다 좁을 때(팀 카드 280px 안) 트랙이 컨테이너를 넘어** 가로로 삐져나온다.
  전체 너비 구역(밀린 미완료)에서는 `min()` 이 항상 260 이라 동작이 완전히 같다.
- 그래서 지금 열 수는 **구역마다 다르다**: 밀린 미완료 = 3열(전체 너비) / 카드 안 항목 = 1열(카드가 좁다).
  `itemGridMaxHeight` 인자를 볼 때 이 차이를 먼저 확인할 것.
- `alignItems` 는 **일부러 안 넣었다** — 옛 코드에 없어서 같은 행 카드가 서로 높이를 맞춘다(stretch).
  각 카드를 내용 높이대로 두려면 `alignItems: "start"` 를 넣으면 되지만, 그건 복원이 아니라 새 디자인이다.

## 🗂 내 개인 업무노트 — 상시 노출 (2026-08-18, **DB 변경 0건 · 새 쿼리 0건**)

달력에서 날짜를 눌러야만 개인노트가 보여 직원들이 안 쓰던 문제. 업무노트 페이지의
🔴 밀린 미완료(전체) 구역과 **달력 사이**에 파란 테두리 구역을 하나 세워 항상 펼쳐 둔다.
**이미 불러온 `notes` 를 거르고 기존 렌더 함수에 넘길 뿐이다** — supabase 호출이 한 줄도 안 늘었다.

- **열람 범위는 두 겹이다.** `viewableNames`(=`wnViewable`) 위에 **`assignee === 본인`** 으로 한 번 더 좁힌다.
  ⚠️ 관리자(양호)는 `wnViewable` 이 **전원**이라, 이 한 줄을 빼면 이 구역에 남의 개인노트가 섞인다.
  DB 쪽은 `work_notes` RLS 가 이미 같은 범위라 이중 방어가 된다(업무노트 비공개 RLS 절 참고).
- **담당자 칩(`filterAssignee`)·메모/할일 필터를 일부러 안 탄다.** 그 필터는 아래 전체 목록·달력 전용이다.
  여기까지 먹이면 남을 고른 순간 내 구역이 비어 "상시 노출"이 깨진다.
- **접힘 상태를 전체 목록과 섞지 않는다** — `openMyMonths`/`myMonthSeeded` 를 따로 뒀다.
  같이 쓰면 전체 목록에서 접은 달이 내 구역에서도 접힌다.
- 재사용하려고 **기존 코드 두 덩이를 함수로 뽑았다**(본문은 한 줄도 안 바꿨다 — 33줄·17줄 기계 대조 완료):
  · `buildNoteMonthGroups(list, openMap)` ← 옛 `unpinnedRender` IIFE. **전체 목록과 내 구역이 같이 쓴다.**
  · `renderOverdueItemBox(it)` ← 옛 밀린 미완료 map 본문. **두 구역이 같이 쓴다.**
  · `renderNoteCardEl(note)` — NoteCard 프롭이 20개라 복붙하면 반드시 어긋난다. 기존 3곳은 일부러 안 건드렸다.
- 🔵 **예정** = 노트 날짜가 오늘 이후. 파란 배경 + `예정` 배지로 따로 세운다(밀린 미완료의 정반대).
  ⚠️ 팀 업무의 `is_sched` 일정과는 **다른 것**이다 — `work_notes` 에는 `is_sched` 가 없다. 여기선 날짜만 본다.
- ⚠️ **📋 목록 모드에서는 내 노트가 두 번 그려진다**(이 구역 + 아래 전체 목록). `editingId` 가 하나라
  한쪽에서 수정을 누르면 양쪽이 같이 편집 상태가 된다. 데이터는 안 깨지지만 보기엔 어색하다.
  고칠 거면 "목록 모드에서 이 구역 숨기기" 나 "전체 목록에서 내 노트 빼기" 중 하나를 골라야 한다.
- **모바일(`/m`)에는 안 나온다.** `WorkNotesView` 는 데스크톱 전용이고 모바일은 개인 업무노트를 자체 UI로 그린다
  (팀 업무와 다른 점 — 그쪽은 `MobileApp` 이 `TeamNotesSection` 을 그대로 렌더한다).
  좁은 폭에서는 `auto-fill` 이 알아서 1열이 된다.

## 📅 정산관리 연도 선택 (2026-08-19, **DB 변경 0건 · 새 쿼리 0건**)

`SettlementView` 에 **`year === 2026` 이 5곳에 하드코딩**돼 있었다 → 해가 바뀌면 정산 화면이
**통째로 빈 화면**이 되고, 직접 등록한 행도 전부 2026년으로 저장됐다. `activeYear` 상태로 바꿨다.

- UI 는 **좌우 화살표 + 연도**, **월 탭 바로 위**에 둔다(해 → 월 순서). `‹ 2026년 ›` + `올해` 배지 + 건수.
- **이동 범위 = 데이터에 실제 있는 연도 ∪ {올해, 내년}** (`yearRange`).
  올해·내년을 항상 넣는 이유는 **아직 건이 없는 내년에도 미리 등록**할 수 있어야 해서다.
  지난 연도는 데이터가 있을 때만 열린다 — 빈 해로 무한정 거슬러 가지 않게 하기 위함.
- ⚠️ **연도 범위는 화면 목록과 다른 데이터로 판단한다** (`dataYearBounds`, 같은 날 추가).
  `fetchData` 는 `agency_cases` 를 **승인 이상 상태만** 불러오므로, 그 목록만 보면
  승인 전 건만 있는 해가 통째로 안 열린다(2025년: 20건 전부 승인 이전).
  → **status 필터를 푸는 방식으로 해결하면 안 된다.** 풀면 부결·시작 전까지 정산 목록에 들어와
  계약금·수수료 합계가 어긋난다(실측 2026년 **285건 → 1,767건**).
  → 대신 **연도만 따로 확인한다**: `order(year).limit(1)` 두 번으로 최소·최대 연도만 받는다.
  ⚠️ 전체 행을 훑지 않는 이유 — `agency_cases` 는 2,200행이 넘어 **PostgREST 1000행 상한에
  조용히 걸린다**(CLAUDE.md 2-5). 화살표는 min~max 연속 이동이라 경계값 둘이면 충분하다.
- 배지가 `정산 건 없음` 이면 "그 해에 기관 건이 없다"가 아니라 **"승인 이상이 없다"**는 뜻이다.
  그 해에도 **직접 등록은 된다**(`setNewManual` 이 `activeYear` 를 쓴다).
- 연도를 바꾸면 편집·직접등록 팝업을 닫는다(`changeYear`) — 월 탭과 같은 규칙.
  `monthsWithData`(월 탭의 ● 표시)도 선택한 연도 기준으로 다시 센다.
- ⚠️ **같은 하드코딩이 `setNewLead` 에도 남아 있다**(`year: 2026`, 배정DB 계열). 이번 범위가 아니라 안 건드렸다.

## 📅 기관별현황 연도 선택 (2026-08-20, **DB 변경 0건 · 새 쿼리 0건**)

`AgencyView` 가 `var currentYear = new Date().getFullYear()` 를 **13곳에서** 보고 있어
**올해가 아닌 건은 화면에 아예 안 떴다.** 실측: 살아있는 1,822건 중 **2025년 20건이 통째로 안 보이는 상태**였다
(전부 `구조혁신&사업전환` 2·6·7·8월). `activeYear` 상태로 바꿨다. UI 는 **정산관리와 같은 모양**
(좌우 화살표 + 연도, 월 탭 바로 위).

- **범위(`yearRange`) = 데이터에 있는 연도 ∪ {올해, 내년} ∪ {지금 보고 있는 해}.**
  ⚠️ 정산관리와 달리 **별도 조회(`dataYearBounds`)가 필요 없다** — 이 화면의 `cases` 는
  `fetchAllRows("agency_cases","*")` 로 **상태 필터 없이 전건**을 이미 들고 있다.
  (정산관리는 `fetchData` 가 승인 이상만 불러와서 연도만 따로 떠야 했다. 그 차이를 헷갈리지 말 것.)
  · `activeYear` 를 범위에 넣는 이유: 점프로 들어온 해가 조회 전이면 범위 밖이라 **화살표가 양쪽 다 잠긴다.**
- ⚠️ **`monthsWithData` 의 의존성 배열에 연도가 빠져 있었다**(`[cases, activeGroup]`).
  상수일 때는 안 드러났지만 상태로 바꾸면 **연도를 옮겨도 월 탭의 ● 표시가 안 바뀐다.** 같이 고쳤다.
- `assigneesInGroup`(담당자 칩)에 **연도 조건을 새로 넣었다.** 안 넣으면 다른 해 담당자가 칩으로 떠서
  눌러도 0건이 된다. 연도를 바꿀 때 `filterAssignee`·`statusFilter` 를 비우는 것도 같은 이유(월 탭과 같은 규칙).
- **휴지통(`trashedCases`)은 일부러 연도로 안 거른다** — 연도와 무관하게 전부 보여야 복구할 수 있다.
  그래서 `yearRange` 는 살아있는 행만 보고 만든다(삭제분만 있는 해를 열 필요가 없다).
- 신규 등록·붙여넣기 등록·`nextApplyPeriod` 폴백도 `activeYear` 를 쓴다 →
  **2025년을 보는 중에 추가하면 2025년 건으로 들어간다.**

### 🔗 `jumpToYear` — 등록 후 이동에 연도를 같이 싣는다
「기관별현황에 등록」은 월을 **`YYYY-MM` 으로 직접 받는다**(다른 해로 등록될 수 있다).
그런데 `registeredGroups` 가 `{group, month}` 만 담아 **연도를 버리고 있었다** → 다른 해로 등록하면
이동한 화면이 올해로 고정돼 **방금 만든 건이 안 보인다.** 연도를 끝까지 실어 보내도록 고쳤다.

`CompanyModal`(`{group, month, year}`) → `handleAgencyRegistered` → `agencyJumpYear` → `AgencyView jumpToYear`
- URL 도 `?view=agency&month=&group=&year=` 로 맞춘다(새로고침해도 같은 화면). **`year` 는 없으면 생략** — 옛 링크 호환.
- `jumpToMonth`/`jumpToGroup` 과 같은 규칙이다: `useState` 초기값 + `useEffect` 양쪽에 넣어야 한다
  (`agencyJumpKey` 로 다시 마운트되는 경로와 프롭만 바뀌는 경로가 둘 다 있다).
- **모바일은 영향 없다** — `AgencyView` 렌더 지점은 `CRMApp` 한 곳뿐이다(팀 업무와 다른 점).

## 📄 기관별현황 다중선택 복사·이동 (2026-08-20, **DB 스키마 변경 0건 · SQL 실행 0건**)

목록 맨 앞 체크박스 열 → 하단 액션바 → 목적지 팝업(기관·연도·월·상태) → 복사 또는 이동.
실무: 소진공 부결 건을 재단으로 복사(재신청) · 올해 건을 2027년으로 복사(내년 재신청 리스트).

**복사는 insert, 이동은 UPDATE 다 — 이동을 "지우고 새로 만들기"로 바꾸지 말 것.**
이동은 같은 행의 `agency_group`·`year`·`month` 만 바꾼다. id 가 바뀌면
`reapply_from_id` 링크·`reject_checklist`·정산 연결이 통째로 끊긴다.

- 🔗 **복사본은 원본의 `company_id` 를 반드시 가져간다.** 1차 insert 가 실패해 최소 컬럼으로
  재시도할 때도 `company_id` 는 남긴다(`baseData`/`minimal` 양쪽 모두).
  **기존 「📋 복사/붙여넣기」(1건, sessionStorage)도 같은 날 같이 고쳤다** — 아래 절 참고.
- ⚠️ **복사는 결과·정산 필드를 가져가지 않는다.** 가져가면 정산관리가 같은 계약금·수수료를 한 번 더 센다.
  (제외: `result`·`result_reason`·`approval_*`·`contract_*`·`commission_fee`·`fee_received*`·
  `invoice_*`·`received_amount`·`settlement_notes`·`reject_checklist`·`delivered_docs`·
  `script_delivered`·`phone_education_done`·`sort_order`·`notion_page_id`·`apply_date`)
  가져가는 것은 `COPY_FIELDS` 27개 + `company_id`·`reapply_from_id`·`status`.
- 새 기능의 신청상품 컬럼은 `fund_product` 다. `product` 는 **사례집(`case_studies`)** 쪽 컬럼이라
  `agency_cases` 에는 없다(CLAUDE.md 2-6 ②).
- ⚠️ **구조혁신&사업전환만 상태 어휘가 다르다 — `"시작전"`(띄어쓰기 없음), 나머지는 `"시작 전"`.**
  `initialStatusFor(group)` 이 목적지에 맞는 값을 고른다. 어휘가 다른 기관 사이에서는
  "현재 상태 유지"를 **잠그고** 초기화로 고정한다 — 안 그러면 목적지 드롭다운에 없는 상태값이 생긴다.

### 파이프라인 카드 — `syncPipelineFromCase(c, { createOnly: true })`
공용 함수에 **선택 인자 하나**를 더했다. 카드가 이미 있으면 **즉시 return**(단계도 `needs_mapping` 도 안 건드림).
`opts` 를 안 주는 기존 호출부 5곳은 동작이 완전히 그대로다.
- **다른 기관으로 복사/이동** = 새 (회사×기관) 조합 → **카드를 만들어 줘야** 보드에서 통째로 사라지지 않는다.
- **같은 기관·다른 연도로 복사** = 카드가 이미 있다 → **손대면 안 된다.** 최신 행 기준으로 다시 계산하면
  단순 복사가 남의 카드 단계를 조용히 움직인다(재동기화 버튼과 같은 효과가 그 조합에만 걸린다).
- 🗓 **미래 월 규칙과 충돌 없음.** 최신 행을 고르는 3곳(`syncPipelineFromCase`·`resyncAutoCards`·
  `toggleSyncMode`)이 전부 미래 월을 건너뛰므로, 2027년 리스트를 아무리 만들어도 카드가 뒤로 안 끌린다.
- ⚠️ **이동한 "원본 기관" 카드는 일부러 정리하지 않는다.** 남은 행 기준 재계산은 카드를 뒤로 끌 수 있다
  (2026-08-20 실측 "뒤로 4장"과 같은 성격). 원본 기관에 행이 안 남으면 파이프라인 휴지통에서 사람이 지운다.
  결과 화면에 그 안내를 띄운다.

### 선택 규칙 — "보이는 것 = 선택된 것 = 실행되는 것"
`selectedRows = filtered ∩ selectedIds` 로만 쓴다. 검색·담당자 필터가 바뀌어 사라진 행은 **자동으로 빠지고**,
액션바에 보이는 숫자가 곧 실행 대상 수다 → 파이프라인 일괄이동의 "화면 밖 N건 제외"를 애초에 안 만든다.
탭·연도·월을 바꾸면 선택을 **비운다**(`changeYear` 가 필터를 비우는 것과 같은 계열, 5곳).
- Shift+클릭은 **범위 추가만** 한다(해제 안 함). 기준은 `selAnchorRef`.
- 행 체크박스 셀은 `stopPropagation` — 누를 때 상세 모달이 안 열린다.
- **중복 방지**: 목적지에 같은 건(`기관+연+월+company_id`, 없으면 사업자명)이 있으면 실행 전에 세어
  건너뛴다 → 두 번 눌러도 안 불어난다. **조인키는 `company_id` 가 원본이다** — 사업자명은 작업 메모가
  붙은 원문이 섞여 있다(2절).

### 🔧 같은 날 — 기존 「📋 복사/붙여넣기」(1건) 버그 2건도 고쳤다 (2026-08-20)
새 기능과 **완전히 별개 경로**라 따로 손봐야 했다. 둘 다 한 줄짜리다.
1. 🔗 **`company_id` 를 안 실었다** → 이 버튼으로 붙여넣은 건이 **전부 미연결로 태어났다**
   (「기관별현황에 등록」 버튼과 같은 계열의 사고 — 0단계-5 참고. 미연결 159 → 160건으로 늘던 원인).
   → 복사할 때 클립보드에 싣고, 붙여넣을 때 **`baseData` 에** 넣는다(2차 재시도에서도 연결이 살아 있게).
   ⚠️ **옛 클립보드**(이 수정 전에 복사해 세션에 남은 것)에는 `company_id` 가 없어 `null` 이 된다 — 예전과 같다.
   ⚠️ **연결되면 `resyncAutoCards` 의 방패가 사라진다**(그 함수는 `company_id` 없는 행을 일부러 건너뛴다).
   붙여넣은 `시작 전` 행이 그 (회사×기관)의 최신 행이 되면, **[재동기화] 를 눌렀을 때** 카드가 뒤로 갈 수 있다.
   미래 월로 붙여넣으면 해당 없음(미래 월 규칙).
2. **`product` → `fund_product`** — `copyData.product = selectedCase.product` 였는데 `selectedCase` 에
   그 컬럼이 없어 **늘 `undefined`** 였다. 그래서 잘못된 컬럼이 insert 에 실린 적은 **없고**(1차 insert 는
   정상이었다), 증상은 **신청상품이 복사되지 않는다** 하나였다. 나머지(우선도·업종·인증서)는 정상이었다.
- **중복 체크는 지금도 일부러 안 붙였다** — 이 버튼의 기존 동작을 넓히지 않기 위해서다(새 다중선택 기능에는 있다).
  ⚠️ **`syncPipelineFromCase` 는 2026-08-21 에 붙였다**(`createOnly`) — 아래 「🃏 카드 자동 생성」 절 참고.

**검증**: 복사 payload 를 `begin … rollback` 트랜잭션으로 실검증(3건 insert → `company_id` 3/3 유지 ·
`reapply_from_id` 3/3 · 상태 '시작 전' 3/3 → 롤백 후 별도 조회로 흔적 0건 재확인).
컬럼 오타 점검 `node scripts/audit-select-columns.mjs` 0건 · `CI=true npx react-scripts build` 통과(main.js +4.09 kB).

## 🃏 기관현황 → 파이프라인 카드 자동 생성 — 쓰기 경로 전수 점검 (2026-08-21, **DB 변경 0건**)

원칙: **(기업 × 기관) 조합 하나당 살아있는 카드 1장.** 한 업체가 소진공·재단 2곳에 신청했으면 카드도 2장.

### 실측 — 지금 원칙은 이미 지켜지고 있다 (기준일 2026-08-21)
살아있는 `agency_cases` 1,837행 · 살아있는 카드 802장.

| | 조합 | |
|---|---:|---|
| 기업목록과 **연결된** (기업×기관) 조합 | **745** | **카드 없음 = 0건** |
| ↳ 살아있는 카드 있음 | 743 | |
| ↳ **사람이 휴지통에 넣은 것** | **2** | 한우리축산(중진공·신보), 2026-08-12 삭제 — **되살리면 안 된다** |
| 미연결(`company_id` null) (사업자명×기관) 조합 | 104 | 이름으로 카드가 붙는 것 6 |
| ↳ **카드가 전혀 없는 것** | **98** | ⚠️ 아래 |

⚠️ **98건은 "빠진 카드"가 아니다 — 만들지 않기로 했다(2026-08-21 사용자 결정).**
살아있는 기업과 이름이 같은 건 **0건**이다. 61조합(54개 이름)은 **기업목록에서 이미 지운 이름**
(2026-05-18·22 중복 임포트 정리분 — `(주)한기협_ 최관호 신청`·`4/21 cu진부라헬점`), 37조합(36개 이름)은
**기업목록에 아예 없는 이름**(`(주)솔스킨 ( 보성무역 대표님 법인 사업장`·`요가람(인증서만료8월18일)(잠수)`)이다.
전부 2절의 **꼬리표 붙은 원문 이름**이라, 카드를 만들면 `company_id` 없는 유령 카드 98장이 보드에 올라오고
**꼬리표 정리(3단계-9) 때 다시 걷어내야 한다.** → 이름 정리가 먼저다.
(만들었다면: 상담/진단완료 +53 · 부결/반려 +19 · 자금집행완료 +10 · 기타 +9 · 나머지 +7 = 802→900장. 매핑 실패는 0건이었다.)

### 반대 방향 — 카드는 있는데 기관현황 줄이 없는 것 59장. **이것도 오류가 아니다**
- **36장은 `agency_group` 이 null** = 기관 미지정 카드(2026-07-25 수동 생성). 애초에 (기업×기관) 조합이 아니다.
- **20장은 "그 기업의 다른 기관 줄만 있음"** — 전부 `manual`·`입금완료/사후관리`
  (`유성기계`[재단]·`모두의무늬`[기보]). **기관현황에 안 적고 파이프라인에만 올린 과거 완료건**이다.
- 8장은 카드에 `company_id` 가 없는 이름 카드, 1장은 기관현황을 휴지통으로 보낸 것.
→ **지우면 실적이 사라진다. 정리 대상이 아니다.**

### 휴지통·부결·완료를 어떻게 취급하는가 (현행 코드 규칙)
- 🗑 **휴지통 카드는 어떤 경로로도 안 되살아난다.** `syncPipelineFromCase` 의 카드 조회는 **일부러
  `deleted_at` 필터를 안 걸고**(걸면 unique 위반) `if (card.deleted_at) return;` 으로 조용히 끊는다.
- ❌ **부결/반려에도 카드를 만든다.** `부결/반려` 는 STAGES 의 정식 칸이고 `status_stage_map` 에
  부결·반려·중단·진행불가·신청취소·신청못함·마감 진행 불가 7종이 매핑돼 있다.
  보드에서 숨기는 건 사람이 [종결](`closed_at`) 처리했을 때뿐이다(현재 4장).
- ✅ **완료(`입금완료/사후관리`)도 숨기지 않는다**(현재 96장).
- 🗓 **미래 월과 충돌 없다.** 미래 월 규칙은 "최신 행을 고르는 3곳"에서 **단계 이동**만 막는다.
  카드 **신규 생성**은 미래 월도 그대로 하고(`stage || "상담/진단완료"`), `createOnly` 는 애초에
  기존 카드를 안 건드린다.

### ✅ `agency_cases` INSERT 경로 6곳 전수 점검 → 4곳에 `createOnly` 를 붙였다
**새 INSERT 경로를 만들면 반드시 이 표에 추가하고 카드 생성을 같이 붙일 것.**

| 경로 | 함수 | `company_id` | 카드 | 2026-08-21 조치 |
|---|---|---|---|---|
| 기관현황 신규등록 | `saveNewCase` | ✅ | ✅ (opts 없음) | 그대로 |
| 다중선택 복사·이동 | `runBulk` | ✅ | ✅ `createOnly` | 그대로 |
| 기업저장 자동등록(신청예정월+담당기관) | `saveCompany` | ❌ | ❌ | **둘 다 추가** |
| 「기관별현황에 등록」(기업상세) | `CompanyModal` | ✅ | ❌ | **카드 추가** |
| 부결 재신청 자동생성 | `saveRejectChecks` | ❌ | ❌ | **둘 다 추가** |
| 📋 붙여넣기(1건) | `AgencyView` 헤더 | ✅ | ❌ | **카드 추가** |

- **전부 `{ createOnly: true }` 다.** 카드가 있으면(휴지통 포함) 즉시 return → **기존 카드는 단계도
  `needs_mapping` 도 안 움직인다.** 기존 5개 호출부는 인자를 안 주므로 동작이 완전히 그대로다.
- `company_id` 를 같이 넣은 2곳(`saveCompany`·`saveRejectChecks`)은 0단계-5 와 같은 계열의 수정이다.
  안 넣으면 그 경로로 만든 건이 **미연결로 태어나고**, 카드도 이름으로만 붙는다.
  ⚠️ 중복 체크 기준(`business_name`+기관+년월)은 **일부러 안 바꿨다** — `company_id` 로 바꾸면
  지금까지 등록되던 건이 조용히 "중복"으로 건너뛰어질 수 있다.

⚠️ **`autoLinkCompanies`(🔗 미연결 연결)에는 일부러 안 붙였다.** 이름 카드가 이미 있는 상태에서
`company_id` 를 붙인 뒤 카드를 또 만들면 **같은 조합에 카드 2장**(이름 카드 + 연결 카드)이 생긴다.
unique index 는 `(company_id, agency_group)` 이라 `company_id` 가 null 인 이름 카드는 **막지 못한다**
(btree 에서 NULL 은 서로 다르다). 여기는 "이름 카드에 `company_id` 를 승계"하는 별도 설계가 필요하다.

**검증**: 조사 쿼리는 전부 `not exists` ↔ `left join` 두 방식으로 재대조(2-2 규칙, 결과 동일) ·
`node scripts/audit-select-columns.mjs` 0건 · `CI=true npx react-scripts build` 통과(main.js +152 B).

## 🎚 기관현황 상태 → 파이프라인 카드 자동 연동 (2026-08-21, **DB 변경 0건**)

**두 화면의 상태 어휘는 일부러 다르게 유지한다. 통일 금지.**
기관현황 `status` 는 35종의 **월별 시계열**이고 파이프라인 `stage` 는 11종의 **(회사×기관) 스냅샷**이다.
연결은 오직 **번역표(코드) + `status_stage_map`(DB)** 두 겹으로만 한다.

### 📖 번역표 확정본 — `status_stage_map` 보다 **코드가 이긴다**
매핑표는 사람이 화면에서 고칠 수 있어, 확정 규칙을 거기 두면 **조용히 바뀐다.** 그래서 App.js 에 뒀다.

| 기관현황 상태 | 동작 | 상수 |
|---|---|---|
| `시작 전` · `시작전` | 카드 없으면 **생성만**, 있으면 손도 안 댐 | `SYNC_CREATE_ONLY_STATUS` |
| `진행 중` · `보류` | **자동 이동 없음** (⚠ `needs_mapping` 도 안 세운다) | `SYNC_NO_MOVE_STATUS` |
| `승인` | → 실태조사완료/약정완료 | `SYNC_STATUS_OVERRIDE` |
| `완료` | → **자금집행완료** (⚠ 입금완료/사후관리가 **아니다**) | 〃 |
| `부결` | → 부결/반려 | 〃 |
| 나머지 24종 | `status_stage_map` 그대로 + 아래 원칙 3개를 똑같이 적용 | — |

⚠️ `시작전`(띄어쓰기 없음)은 **구조혁신&사업전환 전용 어휘**다. 둘 다 넣어야 한다.

### 원칙 — 전부 `canAutoMoveStage(cur, target)` 한 곳에 있다
1. **앞으로만 전진.** `STAGE_RANK` 로 잰다.
   ⚠️ **`STAGES` 배열 인덱스로 재면 안 된다.** 부결/반려·기타가 배열 **끝**에 있어서,
   인덱스로 재면 부결(10번째)이 입금완료(9번째)보다 '앞'이 되어 규칙이 통째로 뒤집힌다.
   → 그 둘은 **rank 없음(순서 밖)** 이다. 새 단계를 STAGES 에 추가하면 `STAGE_RANK` 도 같이 고칠 것.
2. 💰 **돈 단계 동결.** `수수료대기 및 입금요청`·`입금완료/사후관리` 는 자동으로 **들어가지도 벗어나지도** 않는다.
   생성 시에도 마찬가지(목표가 돈 단계면 `자금집행완료` 로 낮춘다).
3. 🅿️ **`기타` 로는 자동 이동하지 않는다**(사람이 사유를 적는 칸). 기타에서 **나가는** 것은 허용 + `other_reason` 정리.
4. ↩️ **부결 되돌리기** — 카드가 `부결/반려` 인데 상태가 다른 것으로 바뀌면 **복귀 + `closed_at`/`closed_by` 해제.**
   안 지우면 "실태조사완료인데 종결이라 보드에서 숨은 카드"라는 유령이 생긴다.
   ⚠️ `clearOther`/`clearClosed` 는 **분기와 무관하게** 계산한다 — `moveCards`(수동 드래그)와 **같은 규칙**이어야 한다.
5. 🗓 **미래 월 규칙과 충돌 없음.** 최신 행 고르기(미래 월 제외)가 먼저 끝난 뒤 이 게이트가 돈다.

### 어디에 걸려 있나 — **판단 지점은 함수 2개뿐**
| | 적용 | 비고 |
|---|---|---|
| `syncPipelineFromCase` | ✅ | 상태 저장 경로 전부가 지나가는 공용 관문 |
| `resyncAutoCards` ([재동기화] 버튼) | ✅ (2026-08-21 결정) | 안 걸면 버튼 한 번에 전부 되살아난다 |
| `toggleSyncMode` 수동→자동 | ✅ (syncPipelineFromCase 경유) | 확인창 문구도 사실에 맞게 고쳤다 |
| `moveCards` (드래그·일괄이동) | ❌ 그대로 | 사람이 옮기면 `sync_mode='manual'` 이 박혀 **그 뒤로 자동동기화가 영영 안 온다** |
| `advanceCardsToContractPaid` | ❌ 그대로 | 계약금입금완료 버튼 전용 |
| `createOnly` 5경로 | ❌ 해당 없음 | 애초에 기존 카드를 안 건드린다 |

**실제 발동 지점은 2곳뿐이다** — 기관현황 **인라인 편집**(`saveEdit`)과 **상세 모달 상태 버튼**.

### 실측 — 이 변경이 기존 카드를 얼마나 움직이나 (2026-08-21)
자동 카드 546장 기준(manual 256장은 원래 대상 아님).

| | 옛 규칙 | **새 규칙** |
|---|---:|---:|
| 다음 저장/재동기화에서 움직일 카드 | **10장** | **1장** |

- ➡ 움직이는 1장: 행복창고김해가구단지[재단] 상담/진단완료 → 부결/반려
- 🚫 전진만 규칙이 막는 4장: 뚱이농산·선진무역·유일목형·프로페나인어클락 (= CLAUDE.md 에 기록된 "재동기화 뒤로 4장")
- 🚫 `보류`=이동없음이 막는 5장: (주)아이지코리아·기동정밀·에스디케이·한우리축산 등
  (기동정밀은 `자금집행완료 → 기타` 로 끌려갈 뻔했다)
- 🔒 **돈 단계 동결이 막는 52장** — 상태가 `완료` 인데 카드가 이미 `입금완료/사후관리` 다.
  이 규칙이 없으면 번역표대로 52장이 전부 `자금집행완료` 로 **뒤로 끌린다.**

⚠️ **대신 앞으로는 손이 간다.** 새로 `완료` 가 되는 건은 `자금집행완료` 에서 멈추고,
`수수료대기 → 입금완료` 는 사람이 파이프라인에서 밀어야 한다. **의도한 결과다**(원칙 2).

### 화면이 거짓말하지 않게 — 매핑표 모달에 규칙을 적었다
확정 규칙이 코드에 있으면 매핑표 화면은 `완료 → 입금완료/사후관리` 로 **보이는데 실제로는 자금집행완료로 간다.**
→ 상단에 확정 규칙 배너 + 행마다 배지(`생성만` · `🚫 자동 이동 없음` · `📖 확정: …` · `💰/🅿️ 자동 금지`)를 붙였다.
**번역표를 바꾸면 `policyNote()` 도 같이 고칠 것.**

### 검증
- **정책 코드 자체를 떼어내 실행**(`sed -n '1358,1419p' src/App.js` → node) — 손으로 옮겨 적으면 코드 검증이 아니다.
  · 요청 시나리오 `시작 전 → 승인 → 완료 → 부결` 2가지(카드 없음/있음) 전부 번역표대로
  · **11단계 × 6상태 = 66칸 전수**: 후퇴 0 · 돈 단계 관여 0 · 기타 유입 0 · 이동 23칸
- 카드 지문(id+stage+stage_changed_at+sync_mode) **배포 전후 동일** — 코드 변경만으로는 한 장도 안 움직인다.
- `CI=true npx react-scripts build` 통과 · `node scripts/audit-select-columns.mjs` 0건.

## 🃏 보드에 카드가 없는 기업 — 「＋ 카드 없는 기업」 (2026-08-30, **DB 변경 0건 · 새 쿼리 0건**)

### 왜 필요했나 — 임팩트레이드 실측
카드를 만드는 경로는 **6곳 전부 `agency_cases` INSERT 에 붙어 있다**(위 🃏 절). 그래서
**기관현황에 줄이 한 번도 안 생긴 기업은 보드에 나타날 방법이 아예 없다.**
`주식회사 임팩트레이드 (IMPACTRADE)` 실측(2026-08-30): 기업 살아있음 · `companies.stage`=`필수서류 및 인증서요청` ·
**`agency_cases` 0건 · `pipeline_cards` 0장(삭제분 포함)**. 버그가 아니라 구조다.
→ 기업목록엔 진행 중으로 보이는데 보드에선 통째로 빠진다.

**같은 처지 10개 / 살아있는 기업 404**(2026-08-30). 그중 카드 0장인데 **기관현황 줄이 있는 기업은 0개** —
즉 "줄은 있는데 카드가 안 생긴" 진짜 버그는 없다. 단계가 이미 진행 중인 4개:
임팩트레이드·천씨씨커피(방어점)[필수서류 및 인증서요청] · 비즈팟(일반과세자)·선사현대창의사고력수학교습소[기타].

### 만든 것 — 진입점 하나. **저장 경로는 새로 만들지 않았다**
보드 헤더 `＋ 카드 없는 기업 (N)` → `NoCardCompanyModal`(기업 고르기) → 기존 `AgencyPickModal`(기관 고르기)
→ **기존 `addAgencyCard` 를 그대로 호출**.
- ⚠️ **가짜 row 를 만들어 넘긴다**(`openNewCardPick`): `{ co, card: { company_id, business_name, agency_group: null } }`.
  `findDupCard` 도 `company_id`/`business_name` 두 필드만 읽으므로 이걸로 충분하고,
  **같은 조합 카드가 휴지통에 있으면 `dupAlert` 의 "복구해 주세요" 안내가 공짜로 붙는다**
  (unique index 가 `(company_id, agency_group)` 라 삭제분이 슬롯을 쥔 채여서 insert 가 실패한다 — 휴지통 절 참고).
- `AgencyPickModal` 에 **`mode="new"` 는 문구 분기만** 추가했다. `isAdd = mode==="add" || isNew` 라 동작은 "add"와 동일.
  ⚠️ `onPick` 삼항을 `mode==="add"?add:assign` → **`mode==="assign"?assign:add`** 로 뒤집었다(기본값이 add).
  기존 두 모드의 동작은 완전히 그대로다.
- `companiesWithoutCard` memo 는 **`companies`·`pipelineCards` 를 그대로 쓴다 — supabase 호출 0건 추가.**
- ⚠️ **보드 필터(검색·단계·기관·담당자)를 일부러 안 탄다.** 이 버튼은 "보드에 없는 것"을 찾는 장치라
  보드 필터로 또 거르면 정작 찾으려던 기업이 안 보인다.
- ⚠️ **`agency_cases` 줄은 만들지 않는다** — 기존 「새 기관 추가 신청」과 같다. C가 원본이라는 원칙상
  기관현황 등록은 기관현황 화면에서 해야 하고, 여기서 만들면 **쓰기 경로가 7번째로 늘어난다.**
- ⚠️ `companies.stage` 도 안 건드린다(A는 아무도 자동으로 안 고친다는 현행 규칙 유지).
  모달 목록의 단계 배지는 **"사람이 어디까지 적어 뒀나" 힌트로만** 쓴다(STEP1 이 아니면 주황색).

### ⚠️ 같이 발견한 별건 — 보드에서 조용히 사라진 살아있는 카드 13장 (미조치)
`App.js` `pipelineCardData` 가 `companyById.get(card.company_id)` 로 기업을 못 찾으면 **`null` 로 버린다.**
그 버림은 `hiddenByList`("필터로 숨겨진 N장") **집계보다 앞이라 화면에 아무 흔적도 안 남는다.**
실측 13장 = `company_id` 없는 이름 카드 8장 + **기업이 휴지통인 카드 5장**
(진성스포츠·하랑소곱창×2·조은상사×2 — 기업을 soft delete 하면 그 카드가 조용히 사라진다는 뜻).
→ 이번 범위 밖. 고칠 거면 "버려진 장수"를 세어 배너에 띄우는 것부터가 안전하다.

### 검증
- `node scripts/test-nocard.mjs` **5/5** — **App.js 소스에서 memo 콜백을 떼어내** 실제 DB 덤프(기업 1,050행·카드 816장)로 실행.
  JS 결과 10개가 SQL 기대값과 **완전 일치**(건수·목록·임팩트레이드 포함·휴지통 기업 제외·카드 있는 기업 제외).
- **카드 지문(id+stage+stage_changed_at+sync_mode) 배포 전후 동일** `82af5755…` (816장/살아있는 814장)
  → 코드 변경만으로는 한 장도 안 움직인다.
- `node scripts/audit-select-columns.mjs` 0건 · `CI=true npx react-scripts build` 통과(main.js +896 B).

## 🕒 업무노트 @기업 태그 → 기업 타임라인 (2026-09-01, **새 테이블 1개 · 기존 테이블 변경 0건**)

업무노트(팀 체크리스트 항목 · 개인 노트 줄)에 `@기업명` 을 쓰면 그 기업 상세 🕒 타임라인에
항목 내용이 작성 시각으로 뜬다. 소급분 277쌍도 이미 들어가 있다.

### 원칙 — **(항목 × 기업) 하나당 링크 1행.** 저장 위치는 `note_company_links` 하나뿐이다
컬럼 `source('team_item'|'work_line') · note_id · item_key · company_id · item_text · at · author`.
유니크 `(source, note_id, item_key, company_id) where deleted_at is null` → **몇 번을 돌려도 안 불어난다.**
⚠️ `note_id` 에 **FK 를 걸지 않았다** — 원본이 `team_notes` / `work_notes` 두 곳이라 한 컬럼으로 못 건다.
(SQL: `업무노트태그_타임라인_테이블.sql` / `_rollback` / `_검증` — RLS·anon 회수 포함. CLAUDE.md 2-2 체크리스트 전부 통과)

### ❌ `activity_logs` 를 쓰지 않은 이유 — 읽는 곳 11곳 중 **9곳이 오염된다**
기업상세 「이슈·액션」 탭 배지 숫자 · AI 상담 프롬프트 · 소통내역 200건 창 · 팀 활동 위젯 건수가
전부 그 테이블을 센다. 작성 항목을 거기 넣으면 **숫자가 조용히 부풀고 되돌릴 방법이 없다.**
→ **앞으로도 타임라인 표시를 위해 `activity_logs` 에 행을 추가하지 말 것.**

### ⚠️ 매처 — `findTaggedCompanies` 를 쓰지 말고 **`taggedCompanyRefs`** 를 쓸 것
`findTaggedCompanies` 는 **채팅 말풍선 전용**이고 매칭 구간을 소비하지 않아 **접두 과매칭**이 있다
(실측 139쌍 중 17쌍 — `@(주)A상사` 가 `(주)A` 에도 걸린다). 고치면 채팅이 회귀 사정권이라 **안 건드렸다.**
`taggedCompanyRefs` 는 규칙이 둘이다: ① `@` 앞은 줄머리이거나 공백(→ 이메일 `basegilt@gmail.com` 배제)
② 이름 뒤는 공백이거나 끝 + **매칭 구간 소비**(→ 과매칭 제거).

### ⚠️ 텍스트를 다루는 함정 3개 — 전부 실측으로 확인한 것이다
1. **개인 노트는 `decodeItemText` 로 푼 뒤에 매처를 돌린다.** `work_notes.content` 에는
   **리터럴 `\n`(역슬래시+n 두 글자)** 이 섞여 있어, 디코드 없이 경계 검사를 하면
   `…컴퍼니  \n@해광알앤에프` 의 두 번째 `@` 는 **앞 글자가 `n`** 이라 **진짜 태그 5건이 조용히 누락된다.**
   → 그 처리를 모은 것이 `workLineDisplayText(line)`(마커 제거 → 대기사유 분리 → 디코드).
2. **팀 항목(`item.text`)은 디코드하면 안 된다** — 이미 디코드된 상태다.
3. **줄 분리는 `content.split("\n")`(진짜 줄바꿈)만.** 리터럴 `\n` 까지 쪼개면 앱 `parseChecklist` 와
   줄 번호가 어긋나 `item_key` 가 통째로 틀어진다.

### 구조 — **이벤트가 아니라 재조정(reconcile)이다**
`reconcileNoteLinks(source, note, companiesList)` 가 노트 1장의 링크를 통째로 다시 맞춘다:
없으면 insert · 문구/시각이 달라졌으면 update · 사라진 태그는 **soft delete**.
쓰기 경로가 많아 이벤트로 만들면 하나만 빠뜨려도 조용히 어긋나지만, 재조정은 **한 번 늦게 불려도
다음 저장에서 스스로 복구**된다. 순수 계산은 `noteLinkRows(source, note, companiesList)` 가 한다(DB 안 건드림).
- ⚠️ **쓴 사람의 브라우저에서만 부른다.** Realtime 수신자 전원이 부르면 같은 일을 N번 한다.
- ⚠️ **실패해도 노트 저장을 되돌리지 않는다** — 타임라인은 부가 기능이고 다음 저장이 다시 맞춘다.

### 쓰기 경로 전수 표 — **새 경로를 만들면 여기 추가하고 재조정을 붙일 것**
| 테이블 | 경로 | 재조정 | 비고 |
|---|---|---|---|
| `team_notes` | `saveTeamChecklist` | ✅ | 완료·되돌리기·삭제·일정체크·결과메모가 **전부 여기 경유** |
| 〃 | `addSchedule` update / insert | ✅ | insert 에 `.select().single()` 을 붙였다(새 카드 id 가 없으면 재조정 불가) |
| 〃 | `confirmTeamCarry` dest / new / **src** | ✅ | src 는 문구가 `" (→ M/D 이월)"` 로 바뀌므로 포함 |
| 〃 | 새 팀 노트 insert | ✅ | 컬럼 없는 환경 재시도 분기까지 덮는다 |
| 〃 | `saveEditNote` | ✅ | |
| 〃 | `deleteTeamNote`(soft delete) | ✅ | `noteLinkRows` 가 `[]` 를 줘 그 카드 링크가 정리된다 |
| 〃 | 채팅→팀 업무 insert | ❌ | `checklist: []` 고정이라 링크가 생길 수 없다 |
| 〃 | `takeToMyNote` · 가져가기 취소 · 완료/완료취소 · 공지확인 | ❌ | 문구·항목 id 가 안 바뀐다 = 링크가 달라질 수 없다 |
| `work_notes` | `onChecklistChange` | ✅ | ⚠️ 아래 |
| 〃 | 개인 `saveEdit` | ✅ | ⚠️ `setEditingId(null)` **앞**에서 부른다(뒤면 `editNote.id` 가 비어 있다) |
| 〃 | 새 노트 insert | ✅ | |
| 〃 | `saveEditorContent` | ✅ | 자동저장·편집창 체크박스의 **유일한 관문**(호출부 2곳) |
| 〃 | `wn_append_todo` RPC 5곳 | ⚠ 예외 | 아래 절 |

⚠️ **`onChecklistChange` 는 `newContent` 가 아니라 실제 저장된 합본(`savedContent`)으로 재조정한다.**
`saveContentWithRetry` 가 다른 곳에서 늘어난 줄을 합칠 수 있어, 화면 값으로 맞추면 줄 번호가 DB 와 어긋난다.

⚠️ **`saveEditorContent` 는 onBlur 마다 돈다.** `note.content` 는 편집창을 연 시점 값이라 자동저장 뒤에도
안 바뀌므로, 마지막으로 재조정한 본문을 `linkedContentRef`(NoteEditCard 지역 ref)에 남겨
**내용이 실제로 바뀔 때만** 부른다. 안 그러면 칸을 벗어날 때마다 조회를 쏴 편집이 굼떠진다.

### ⚠️ `wn_append_todo` 만 예외 — 재조정이 **불가능**하다
업무요청·빠른업무는 **남의 노트**에 줄을 덧붙이는데, 개인노트 RLS 때문에 내 브라우저가 그 노트를
읽을 수 없어 "원하는 상태"를 계산할 수가 없다. → 그 5곳만 `appendNoteLinksForLine` 으로 **직접 insert** 한다.
`item_key` 는 `"append:<ISO>"` — **노트 주인이 나중에 저장하면 재조정이 정상 줄번호 키로 바꾸고 이 행을 정리한다.**
⚠️ **RPC 호출 자체(인자·순서)는 건드리지 말 것** — 직접 select 후 update 로 바꾸면 상대에게 같은 날 노트가 두 장 생긴다.

### 🕘 시각(`at`) 을 정하는 규칙
`noteLinkAt(dateStr, fallbackIso)` — 업무 날짜 **09:00 KST == 그 날짜의 자정 UTC**(KST=UTC+9)라 변환이 없다.
- 팀 항목: `item.created_at` 우선 → 없으면 카드 `work_date`
- 개인 줄: 노트 `note_date`
- **개인 노트 줄은 "실제 작성 시각"을 쓸 수 없다** — `work_notes.content` 는 통짜 텍스트라 줄마다 시각이 없고,
  포맷을 바꾸면 이월 꼬리표·대기사유·리터럴 `\n` 인코딩이 전부 회귀 사정권에 들어온다.

⚠️ **팀 항목 `created_at` 은 새 항목에만 찍는다.** `saveEditNote` 는 기존 카드를 다시 저장하는 자리라,
`created_at` 없는 옛 항목에 지금 시각을 찍으면 그 항목의 타임라인 위치가 **카드 날짜에서 "수정한 지금"으로
통째로 옮겨간다**(제목만 고쳐도 항목 전부가 오늘로 튄다). 새 항목 판정은 `!it.id` 가 아니라
**`tmp_` 접두 id** 다 — 수정창 `[+ 추가]` 버튼이 항목에 `tmp_xxx` id 를 미리 붙인다(`App.js:32484`).

### ⚠️ 기존 `note_auto` 와 내용이 겹치는 건 **정상이다. 고치지 말 것**
개인 노트 항목을 **체크할 때** `detectCompaniesInText` + `logToActivity` 가 `log_type='note_auto'` 로
이미 기록한다(2026-09-01 실측 **1,098건 · 125개 기업**). 그래서 타임라인에 **"작성"과 "완료"가 나란히** 뜬다.
**의미가 다른 두 사건이라 둘 다 남기고 뱃지만 다르게 했다**(`업무노트항목`=작성 / `업무노트`=완료).
기존 `note_auto` 경로를 건드리면 완료 기록 전체가 회귀 사정권이다.

### 🔄 재동기화 · 미연결 태그
- 기업상세 🕒 탭의 `🔄 재동기화` — 살아있는 노트를 전부 훑어 재조정한다.
  ⚠️ **`fetchAllRows` 로 받는다**(CLAUDE.md 2-5). `.select()` 직접 호출은 1000행 상한에 조용히 걸려
  뒷부분 노트가 통째로 안 맞춰진다. 개인노트는 RLS 때문에 **누른 사람이 볼 수 있는 것만** 맞춰진다.
- `node scripts/migrate-note-links.mjs` (기본 dry-run, `--apply` 로 반영) — 소급 마이그레이션 +
  **연결 안 된 @태그 목록**. **자동 보정하지 않는다** — 유사도로 붙이면 오연결이 난다(위 2절 "이름을 조인키로 쓰지 말 것").
  현재 2건: `@(주)에이원커뮤니케이션코리`(→…코리아) · `@조선제일한우`(→농업회사법인(주)조선제일한우).

### 실측 (2026-09-01) — **인용 전에 그날 다시 셀 것**
| | 값 |
|---|---:|
| 들어가 있는 링크 = 코드로 계산되는 링크 | **295** (팀 103 · 개인 192) · 항목/줄 265 · **기업 71** · 2026-07-18~09-02 |
| 원본 | 팀노트 76 · 개인노트 588 · 기업 406 |

소급분은 두 번에 나눠 넣었다 — 277쌍(2026-08-31) + **18쌍(2026-09-01)**.
뒤 18쌍은 재조정 코드 배포 전에 직원들이 단 태그라 자동으로 안 붙어 있던 것이다.
**앞으로 같은 일이 생기면 `--apply` 나 🔄 재동기화로 메우면 된다** — 유니크 제약 덕에 몇 번을 돌려도 안 불어난다
(실증: 연속 두 번 `--apply` → 두 번째는 `새로 넣을 것 0쌍`, 별도 조회로 `total=295` 불변).

검증(실행 파일에 딸린 SELECT 가 아니라 **별도 조회**로 — CLAUDE.md 2-2):
**잘못 붙은 링크 0** (링크의 기업명이 `item_text` 안에 `@이름` 으로 실제 있는지 전수 대조) ·
**이메일 도메인 기업 0** · **휴지통 기업 링크 0** · **중복 키 0** ·
원본 지문(`team_notes`·`work_notes`·`activity_logs`·`pipeline_cards` id+stage+stage_changed_at+sync_mode)
**반영 전후 동일** `68b99d43…` → 링크만 늘고 원본은 한 줄도 안 움직였다.

재측정: `node scripts/migrate-note-links.mjs` (읽기 전용) · 검증은 `scripts/test-note-links.mjs` **52/52**
(App.js 소스를 떼어내 가짜 supabase 를 주입 — 손으로 옮겨 적지 않는다).

### 🐞 같이 발견한 **별건 버그** (아직 안 고침)
`saveEditNote` 의 `cleanChecklist` 가 항목을 **처음부터 다시 만들어** 6개 필드만 남긴다
→ 팀 카드를 한 번 수정 저장하면 그 안 **일정 항목의 `is_sched`·`sched_kind`·`company_id`·`company_name`·
`assignee`·`result`·`done_at`·`done_by` 가 통째로 사라진다**(일정이 평범한 글줄이 된다).
이 기능 이전부터 있던 버그다. 고치려면 `return { … }` → `return Object.assign({}, it, { … })` 한 줄이지만
**팀 일정 기능 전체가 회귀 사정권**이라 별도 건으로 뒀다.

## 🧭 성장 로드맵 (2026-09-02, **새 테이블 2개 · 기존 테이블 변경 0건 · App.js 기존 코드 수정 0줄**)

업종 7개 × 성장 경로 16건을 보여주는 읽기 전용 참조 화면. 사이드바 `주요 메뉴 → 성장 로드맵`
(`view === "growth-roadmap"`, `src/pages/GrowthRoadmap.jsx`). **「기관 사이트」(`AgencySites.jsx`)와 같은 방식**이다.

- SQL: `성장로드맵_growth_paths.sql` / `_rollback.sql` / `_검증.sql` · 테스트 `scripts/test-growth-roadmap.mjs`
- 원본 패키지는 `growth-roadmap-package/` 에 참고용으로 그대로 뒀다(첨부 SQL·JSX·CSV·README).

### ⚠️ 사이드바 배열과 렌더 분기는 **한 쌍**이다
`App.js:8730` 배열 1줄 + `App.js:9093` 렌더 분기 1줄 + `App.js:5` import 1줄, 전부 **추가만** 했다(삭제 0줄).
한쪽만 넣으면 **메뉴는 뜨는데 화면이 빈 채로 남는다.** 새 메뉴를 만들 때도 같다.
- **이 앱에는 react-router 가 없다.** 첨부 README 의 `<Route path="/growth-roadmap">` 방식은 못 쓴다 — `view` 문자열 상태다.
- 아이콘은 `Icon`(App.js:4271)이 아는 이름만 쓸 것. 모르는 이름은 **에러 없이 `null`** 이라 아이콘 자리만 조용히 빈칸이 된다.
- `view` 는 화이트리스트 없이 `params.get("view")` 를 그대로 받는다 → `?view=growth-roadmap` 이 **공짜로 동작**한다.
- `supabase` 는 모듈 최상단 기존 클라이언트를 넘긴다. 새로 `createClient` 하면 **GoTrueClient 가 둘**이 되어 토큰 갱신이 싸운다.
- **모바일 영향 0** — `MobileApp` 은 이 컴포넌트를 렌더하지 않는다(`AgencySites` 와 같음, `TeamNotesSection` 과 다른 점).

### 🔍 미검증(원문 대조 필요) 표시 — **판정 지점은 `isUnverified` 하나뿐이다**
경로 16건 중 **12건은 원본 영상 판독(`source='video'`)**, **4건은 영상에 없던 업종을 같은 문체로 채운 것(`source='authored'`)**이다.
authored 4건에는 「가맹사업 정보공개서 등록」·「위탁훈련기관 인정」·「원격평생교육시설 신고」처럼 **실제 법적 요건 명칭**이
들어 있어, 공고 원문 대조 전까지 사실로 취급하면 안 된다.

- 판정은 `isUnverified(p) = !!p.source && p.source !== "video"` **한 줄뿐**이다. 배지를 다는 자리가 늘면 이걸 쓸 것.
- **화면에 id 를 박아 두지 않았다. DB 의 `source` 가 유일한 원본이다** → 대조가 끝나 `source` 를 `'video'` 로 바꾸면
  배지·필터·건수가 **코드 수정 없이** 같이 사라진다.
- 표시 3곳 + 필터 1곳: 카드 헤더 배지 · 펼친 본문 안내줄 · **분석 모달 결과 줄** · 상단 `☐ 미검증 N건만 보기`.
  ⚠️ **분석 모달을 빼먹으면 안 된다** — 같은 데이터를 쓰는 자리라 미검증 경로가 1위로 올라올 수 있다.
- 필터를 켜면 **업종 탭을 무시하고 전 업종에서 모아 본다**(대조는 업종을 넘나들며 하므로). 대신 업종 탭을 누르면
  필터가 꺼진다 — 안 끄면 "제조를 눌렀는데 교육이 보이는" 상태가 된다.
- `source` 가 빈 행은 **미검증으로 치지 않는다.** 멀쩡한 경로를 의심하게 만드는 쪽이 더 나쁘다.

### ⚠️ 첨부 SQL 을 그대로 쓰지 않았다 — CLAUDE.md 2-2 위반 3가지
「기관 사이트」때와 **똑같은 문제**라 `기관사이트_agency_sites_보안보정.sql` 을 선례로 삼았다.
**데이터·컬럼·시드 16건은 첨부 원본에서 기계적으로 잘라 붙였다**(옮겨 적기 오류 방지).

| 첨부 원본 | 바꾼 것 | 왜 |
|---|---|---|
| `using (true)` | `using (public.is_approved())` | 미승인·퇴사 계정도 세션만 살아 있으면 읽힌다(2-3·2-4) |
| anon GRANT 회수 없음 | `revoke all ... from anon` | postgres 기본권한 `anon=arwdm` 이라 **새 테이블에 자동으로 붙는다** |
| — | `revoke truncate, references, trigger` | 2-2 필수 |
| — | **`revoke insert, update, delete`** | ⚠️ 아래 |

⚠️ **truncate 회수만으로는 부족했다 — 실측으로 잡았다.** 기본권한이 `authenticated=arwdm` 이라
`INSERT/UPDATE/DELETE` 6건이 그대로 남아 있었다. 쓰기 정책이 없어 RLS 가 막지만, 정책 하나가 느슨해지면
남은 GRANT 로 뚫린다(2026-07-27 사고 패턴). **읽기 전용 참조 테이블이면 쓰기 권한도 회수할 것.**
경로 수정은 Supabase 대시보드(service_role)에서 하므로 앱 사용자에게 쓰기 권한이 필요 없다.

### 실측 (2026-09-02) — **인용 전에 그날 다시 셀 것**
`growth_paths` 16행(전부 `is_active`) · `growth_roadmap_copy` 1행 · **video 12 / authored 4** ·
authored = **id 5(서비스→다점포·가맹) · 7(도소매→수출·해외 판로) · 9(교육→온라인 콘텐츠·구독) · 10(교육→기업·기관 교육)**.

### 안 만든 것
- **기업 상세 드로어 임베드**(첨부 README 5번) — `companies` 에 업종 코드(`food`/`mfg`…)가 없어 텍스트→코드 매핑을
  새로 만들고 `CompanyModal` 을 건드려야 한다. 이번 범위 밖(사용자 결정).
- `roadmap.json`/`roadmap.csv` 를 `src/` 로 복사 — **DB 가 원본**이라 죽은 사본을 만들지 않는다.
  컴포넌트의 `staticData` 폴백 분기는 원본 그대로 남겼지만 현재 호출부는 항상 `supabase` 를 넘긴다.

### 검증
- `node scripts/test-growth-roadmap.mjs` **7/7** — **소스에서 `isUnverified` 를 정규식으로 떼어내** 실제 DB 16행에 먹였다.
  ⚠️ 대조가 끝나 `source` 를 고치면 이 테스트는 **실패하는 게 정상**이다. 기대값을 같이 줄일 것.
- 보안 검증은 **실행 파일에 딸린 SELECT 가 아니라 별도 조회로**(2-2): RLS 꺼진 테이블 0행 · anon GRANT 0행 ·
  authenticated 비-SELECT 권한 0행 · 정책 2행 전부 `is_approved()`.
- ⚠️ `node scripts/audit-select-columns.mjs` 0건 — 다만 이 도구는 **`src/App.js`·`api/ai-search.js` 만 훑고 `src/pages/` 는 안 본다.**
  그래서 컴포넌트가 읽는 컬럼 13개를 `information_schema` 와 **따로 대조했다**(13/13 실재).
- `CI=true npx react-scripts build` 통과(main.js +4.78 kB) · `src/App.js` diff **10줄 추가 · 0줄 삭제**.

## 🎯 파이프라인 카드 「현재 상태」 코멘트 (2026-09-04~05, **새 테이블 0개 · 새 저장 경로 0개**)

카드에 "지금 왜 여기 멈춰 있나"를 한 줄로 띄운다. 사람이 직접 쓰면 그 문구,
안 쓰면 그 기업의 **최신 소통내역 + 최신 @기업 태그 업무노트 항목**이 자동으로 뜬다.
설계서 `docs/superpowers/specs/2026-09-04-pipeline-comment-design.md` · 계획 `docs/superpowers/plans/2026-09-04-pipeline-comment.md`

### 저장 — `companies` 컬럼 3개. **기업 단위다**
`status_comment` · `status_comment_at` · `status_comment_by`.
**`status_comment` 가 비어 있으면 자동표시**, 값이 있으면 그 문구 — 이 한 줄이 전환 규칙 전부다.
- ⚠️ **`companies.issue`(현재 이슈)와 다른 칸이다.** issue 는 카드 있는 기업 401개 중 **237개에
  이미 평균 68자(최대 786자)** 가 들어 있어, 재사용하면 켜는 즉시 카드 237장이 장문으로 덮인다.
- ⚠️ **기업 단위라 카드 2~6장에 같은 문구가 뜬다**(카드 2장 이상인 기업 242개). 의도한 결과다.
  기관별로 다르게 쓰고 싶다면 `pipeline_cards` 쪽 덮어쓰기를 **새로 설계**해야 한다.
- SQL: `파이프라인_현재상태_코멘트_컬럼추가.sql` / `_rollback` / `_검증`

### ⚠️ 자동표시에 넣으면 안 되는 log_type — 이게 이 기능의 핵심 함정
`activity_logs` 는 **사람이 쓴 메모와 시스템 이벤트가 한 테이블에 섞여 있다.**
2026-09-04 실측: 필터 없이 "최신 1건"을 고르면 **카드 기업 145/331(44%)** 에
`"소진공 카드를 '상담/진단완료' → '기관신청대기/방문예정'(으)로 이동"` 이 뜬다.

| 쓰는 것 | 안 쓰는 것 | 왜 |
|---|---|---|
| `manual_memo`·`quick_memo`·`chat_memo` | `pipeline_move`·`stage_change`·`assignee_change` | 시스템 이벤트다 |
| `note_company_links` 전건 | `note_auto` | 항목을 **완료 체크할 때** 남는 기록이라 "지금 상태"가 아니다 |
| | `issue_update`·`status_change`·`action_update` | **`company_id` 가 전 행 비어 있어** 애초에 못 잇는다 |

→ 목록은 **`STATUS_COMM_TYPES` 한 곳**에만 있다. 늘리기 전에 위 표를 다시 볼 것.

### 계산은 순수 함수 — **supabase·React 를 들이지 말 것**
`App.js` 의 `⛳ 현재상태-자동표시 계산 시작/끝` 마커 사이가 전부다
(`STATUS_COMM_TYPES` · `stripLeadingTag` · `statusCommentDate` · `buildStatusAutoMap`).
`scripts/test-status-comment.mjs` 가 **이 구간을 소스째 떼어내 실행**한다 — 무엇이든 참조하는 순간 테스트가 못 돈다.
- ⚠️ **마커 문구를 바꾸면 테스트가 통째로 죽는다.**
- ⚠️ **최신 1건은 JS 에서 고른다.** `fetchAllRows` 의 정렬 기본값(`created_at`→`id` 오름차순)을
  바꾸지 않기 위해서다(CLAUDE.md 2-5).
- ⚠️ **`taggedCompanyRefs`/`findTaggedCompanies` 를 쓰지 않는다.** `chat_memo` 의 선두 `@업체명` 만
  떼면 되는 자리라 `stripLeadingTag` 하나로 충분하고, 후자는 접두 과매칭이 있는 채팅 전용이다.

### 읽기 — `fetchAllRows` 2회 (보드 진입 시 1번)
`activity_logs`(3종만) · `note_company_links`. **합계 약 770행으로 지금은 1000행 미만이지만
`fetchAllRows` 를 쓴다** — 판단 기준은 "지금 넘느냐"가 아니라 "전체를 받아야 하는 조회냐"다(2-5).
실패하면 📛 배너가 뜨고 빈 Map 이 되어 **자동표시만 안 뜬다. 카드·단계에는 영향이 없다.**

### 쓰기 — **새 저장 경로 0개**
편집은 기존 공용 `MemoEditModal`, 저장은 기존 `writeGuarded` + 기존 `onPatchCompany(id, patch)` 패턴.
`companyById` → `pipelineCardData` 가 다시 계산돼 **같은 기업 카드가 한꺼번에 바뀐다.**
- ⚠️ **`✎` 에 `stopPropagation` 필수** — 카드 `onClick` 이 기업상세를 연다. 기관현황 우선도 셀·기업목록 「기타」와 같은 함정.
- ⚠️ **빈칸 저장 = 세 컬럼 모두 `null`** → 자동표시 복귀. 빈 문자열로 두면 "썼는데 안 보이는" 상태가 된다.
- `writeGuarded` 재시도 큐를 그대로 쓴다 — 저장 뒤에 딸린 연동이 없다(2-7 해당 없음).

### ✎ 진입점은 **모든 카드에 있다** — 설계를 뒤집은 곳 (2026-09-05 사용자 결정)
설계서 §4-4 는 "셋 다 없으면 아무것도 그리지 않는다(보드 밀도)"였고 코드도
`if (!manual && !comm && !note) return null;` 이었다. 그러면 **카드 절반(833장 중 423장)에
직접 쓰기 버튼 자체가 안 보인다** — 정작 조용한 카드일수록 적을 말이 있는데 손이 안 닿는다.
→ **그 한 줄을 걷어냈다.** 자동표시 분기가 이미 `✎ 직접 쓰기` 줄을 그리고 있어서,
`comm`·`note` 가 둘 다 없으면 **그 줄만** 남는다(새 UI 코드 0줄).
- ⚠️ **툴팁 문구는 분기한다.** 위에 자동표시가 없는데 "위 자동표시 대신 뜹니다"라고 하면 거짓말이 된다.
- 설계서 §4-4·§7-1 은 옛 결정 그대로 남아 있다 — **이 절이 최신이다.**

### 실측 (2026-09-05) — **인용 전에 그날 다시 셀 것**
살아있는 카드 **833장** / 카드 있는 기업 **404개** ·
자동표시가 뜨는 기업 **162개(40%)** · 카드 **410장(49%)** → **나머지 절반(423장)엔 `✎ 직접 쓰기` 한 줄만 뜬다.**
수동 문구가 들어간 기업 **0개**(아직 아무도 안 썼다 — 이 숫자가 늘기 시작하면 기능이 실제로 쓰이는 것이다).

### 안 만든 것
기업상세에 이 칸 노출 · 기관(카드)별로 다른 문구 · 코멘트 이력 · Realtime 구독 · `note_auto` 포함.
**모바일 영향 0** — `PipelineView` 렌더 지점은 `CRMApp` 한 곳뿐이다(`TeamNotesSection` 과 다른 점).

### 검증 (2026-09-05)
- `node scripts/test-status-comment.mjs` **37/37** — 계산 블록을 소스째 떼어내 실제 DB(소통내역 453행 ·
  태그링크 317행 · 카드 기업 401개)로 실행. 손으로 옮겨 적지 않는다.
- **카드 지문(id+stage+stage_changed_at+sync_mode)** — 장수는 작업 내내 **835장/살아있는 833장 그대로**.
  ⚠️ 해시는 `e2d8db84…` → `74f1d56a…` 로 **바뀌었는데, 원인은 이 작업이 아니라 사람이다** —
  같은 시간대에 양호가 보드에서 카드를 옮겼고(`activity_logs` `pipeline_move` 15건, 16:00~18:28 KST)
  변경 카드마다 그 로그가 남아 있다. **운영 DB 로 지문을 재는 날은 이걸 먼저 확인할 것** —
  "지문이 달라졌다 = 내 코드가 카드를 건드렸다"가 **아니다.**
  이 기능의 쓰기 경로는 `companies` 3컬럼 하나뿐이라 구조적으로 카드를 못 움직인다.
- 기존 테스트 5종 전부 통과: `test-nocard` 5/5 · `test-note-links` 52/52 · `test-amount-unit` 42/42 ·
  `test-revenue-input` 14/14 · `test-growth-roadmap` 7/7.
- `node scripts/audit-select-columns.mjs` **0건** · `CI=true npx react-scripts build` 통과(main.js 442.81 → 443.85 kB).
- **`src/App.js` diff 200줄 추가 · 수정 2줄뿐**(둘 다 `PipelineView` 에 `onPatchCompany` 프롭을 더한 줄) →
  `pipelineCardData`·`moveCards`·`syncPipelineFromCase`·카드 이동/삭제/종결/기타사유는 **한 줄도 안 건드렸다.**
- ⚠️ **화면 클릭 회귀(드래그 이동·일괄 이동·휴지통·종결/복원·동기화 토글)는 사람이 확인해야 한다.**
  이것들은 **운영 DB 를 실제로 바꾼다** — 에이전트가 재현 테스트로 돌리면 안 된다
  (2026-08-17 사고: 카드 2장의 `stage_changed_at` 이 테스트 시각으로 남아 복구 불가).

## 🚫 구조혁신&사업전환 부결 — 재신청 흐름에서 제외 (2026-09-07, **DB 변경 0건 · 새 쿼리 0건**)

**구조혁신&사업전환은 매달 재신청하는 프로그램이 아니다.** 한 번 신청해 최종 결과(승인/부결)가
정해진다. 그래서 부결이어도 **재신청 체크리스트 모달이 뜨면 안 되고 다음 달 건도 만들면 안 된다** —
그냥 `부결` 상태로 남는 것이 맞다. **다른 기관용 기존 재신청 로직은 한 줄도 안 건드렸다.**

### 판정은 `isReapplyExempt` 한 곳뿐이다
`App.js` 의 `⛳ 재신청-제외 시작/끝` 마커 사이(`REAPPLY_EXEMPT_GROUPS` · `isReapplyExempt`).
`scripts/test-gujo-reject.mjs` 가 **이 구간을 소스째 떼어내 실행**한다 — 무엇이든 참조하는 순간 테스트가 못 돈다.
- ⚠️ **마커 문구를 바꾸면 테스트가 통째로 죽는다.**
- ⚠️ **명단이 늘면 여기 한 곳만 고친다.** 판정을 호출부에 흩으면 조용히 어긋난다.
- ⚠️ **모르는 값·빈 값은 `false`** — 기본은 "기존대로 재신청 흐름을 탄다".
- 기존 로컬 `isGujo`(다중선택 복사·이동의 `initialStatusFor` 짝)는 **이 함수를 가리키게 한 줄만 바꿨다.**
  판정을 두 벌로 두면 반드시 어긋난다.

### 막은 자리 — 2곳 (호출부는 안 고쳤다)
| | 무엇 |
|---|---|
| `openReject` **첫 줄** | 모달 자체를 안 연다. 호출부 2곳(인라인 편집 `saveEdit` · 상세 모달 상태 버튼)은 **한 줄도 안 고쳤다** → 앞으로 새 호출부가 생겨도 자동으로 같이 막힌다 |
| `saveRejectChecks` 의 `reapply_intent === "예"` 분기 | **이중 방어.** 판정이 한 곳뿐이면 나중에 조용히 풀린다 |

### 상태값 — `GUJOHYEOK_STATUS_OPTIONS` 에 `"부결"` 하나 추가 (12종 → 13종)
- `GUJOHYEOK_STATUS_COLORS` 에 색도 같이 넣었다. **빠뜨리면 회색 폴백으로 새서** 다른 기관 부결과 색이 달라진다.
- ⚠️ `"반려"` 는 **일부러 안 넣었다**(사용자 결정). 구조혁신에 반려라는 개념이 실무적으로 없다.
- **집계 코드는 손 안 댔다** — `STATUS_GROUPS.rejected` 에 `"부결"` 이 이미 있어 기관별현황 집계·필터가 그대로 동작한다.

### ✅ 승인된 부작용 — 파이프라인 카드는 `부결/반려` 로 이동한다
`SYNC_STATUS_OVERRIDE["부결"] = "부결/반려"` 라 구조혁신 건을 부결로 바꾸면 **카드가 움직인다.**
다른 기관과 같은 취급이 맞다는 사용자 결정(2026-09-07)이다. 전진만·돈 단계 동결 규칙은 그대로 적용된다.
→ **번역표를 건드리는 건 별개 결정이다.** 카드까지 멈추고 싶어지면 그때 다시 정할 것.

### 실측 (2026-09-07) — **인용 전에 그날 다시 셀 것**
살아있는 구조혁신 건 중 **부결·반려·진행불가·신청취소 = 0건** → 이번 변경은 **기존 데이터를 하나도 안 건드린다.**
(가장 많은 상태: `시작 전` 49 · `사업전환 승인` 19 · `시작전` 14 · `컨설팅 최종 완료` 13)

## 🗓️ 주간 정리 자동 팝업 — 명단에 있는 사람만 (2026-09-07, **DB 변경 0건 · 새 쿼리 0건**)

`WorkNotesView`(**데스크톱 전용**)의 주 1회 자동 팝업이 전원에게 떠서, 정리를 실제로 하는 사람만 남겼다.

- 명단은 `App.js` 의 `⛳ 주간정리-대상 시작/끝` 마커 사이 **`WEEKLY_REVIEW_MEMBERS = ["관호","유진","정원"]`** 하나뿐.
  `scripts/test-weekly-members.mjs` 가 소스째 떼어내 실행하고 **`profiles.name` 과 대조**한다.
- ⚠️ **"관리자 제외"로 판정하면 안 된다.** 정원은 `role='admin'` 인데 **대상**이고 양호는 아니다.
  (승인된 admin 은 2026-09-07 기준 양호·정원·권구현 3명 — CLAUDE.md 2-4 의 "양호·정원만"은 옛 정보다.)
- ⚠️ **`ASSIGNEES` 를 재사용하지 말 것.** 담당자 선택·필터 목록이라 의미가 다르고, 거기 사람이 추가되면
  팝업이 조용히 같이 켜진다.
- ⚠️ **이름은 `profiles.name` 과 정확히 같아야 한다.** 틀리면 **아무에게도 안 뜨고 에러도 안 난다** —
  그래서 테스트가 DB 와 대조한다.
- **막은 것은 자동 오픈 `useEffect` 한 곳뿐이다.** 상단 `🗓️ 주간 정리 (N)` 버튼·모달·이월 실행부
  (`openWeeklyManually`·`applyReviewActions`)는 **전원 그대로** — 필요하면 누구나 직접 연다.
- **정보가 사라지지 않는다** — 업무노트 상단 🔴 밀린 미완료 구역은 전원에게 그대로 보인다.
- 모드 결정 `useEffect` 는 안 건드렸다(모달을 안 여니 무해하고, 수동 열기가 자체적으로 모드를 다시 고른다).
- **모바일 영향 0** — `WorkNotesView` 는 `/m` 에서 렌더되지 않는다(팀 업무와 다른 점).

### 검증 (2026-09-07)
- `node scripts/test-gujo-reject.mjs` **16/16** · `node scripts/test-weekly-members.mjs` **12/12**
  (둘 다 App.js 소스를 떼어내 실행 — 손으로 옮겨 적지 않는다)
- 기존 테스트 6종 전부 통과: `test-nocard` 5/5 · `test-note-links` 52/52 · `test-amount-unit` 42/42 ·
  `test-revenue-input` 14/14 · `test-growth-roadmap` 7/7 · `test-status-comment` 37/37
- `node scripts/audit-select-columns.mjs` **0건** · `CI=true npx react-scripts build` 통과(main.js +58 B)
- **카드 지문** — 작업 중 카드가 837 → 838장으로 **늘었지만 이 작업과 무관하다.** 늘어난 1장
  ((주)엘포테크·중진공, `sync_mode=auto`, 17:00 KST)을 빼고 다시 재면 지문이 `9aea1e29…` 로
  **작업 전과 완전히 동일**하다 → 기존 카드는 한 장도 안 움직였다.
  ⚠️ 2026-09-05 절과 같은 교훈이다 — **"지문이 달라졌다 = 내 코드가 카드를 건드렸다"가 아니다.**
  운영 DB 로 지문을 재는 날은 그 사이 사람이 한 일을 먼저 확인할 것.
- ⚠️ **화면 클릭 회귀는 사람이 확인해야 한다** — 운영 DB 를 실제로 바꾸므로 에이전트가 재현 테스트로
  돌리면 안 된다(2026-08-17 사고). 확인할 것: 구조혁신 탭에서 `부결` 선택 → **모달이 안 뜬다** /
  다른 기관에서 `부결` 선택 → **예전처럼 모달이 뜬다** / 관호·유진·정원 계정에 주간정리 팝업이 뜬다 /
  나머지 계정은 안 뜨지만 🗓️ 버튼으로는 열린다.

## 📝 메모 확대 편집 팝업 (2026-08-21, **DB 변경 0건 · 저장 로직 0줄**)

좁은 칸(표 한 줄짜리 input · 사이드패널 rows=3~4 textarea)을 누르면 넉넉한 창이 열린다.
**업무노트의 「체크리스트 항목 편집 모달」과 같은 구조·같은 조작감**으로 맞췄다(새로 디자인하지 않았다).

### 공용 컴포넌트 `MemoEditModal` — 새 메모 칸을 만들면 이걸 쓸 것
오버레이(바깥 클릭으로 안 닫힘) → 흰 카드 `maxWidth 560` → 제목 → 🏢 업체명 → 힌트 → `minHeight 220` textarea → [취소][확인].
`Ctrl(⌘)+Enter` 저장 · `Esc` 취소 · 내용이 바뀐 채로 닫으면 `confirmDiscard`.

- ⚠️ **저장을 하지 않는다.** `onSave(text)` 로 값만 돌려주고 **저장은 호출부의 기존 경로**가 한다.
  **이 컴포넌트에 supabase 를 들이지 말 것** — 들어오는 순간 저장 경로가 두 벌이 된다.
- ⚠️ **`MentionField` 는 일부러 안 쓴다.** @업체 태그는 업무노트 전용이고 이 칸들은 평범한 텍스트다.
  겉모습·조작감만 같게 맞췄다. **업무노트 원본 모달은 한 줄도 안 건드렸다.**
- ⚠️ **Esc 는 `window` capture(`addEventListener(..., true)`) 로 잡고 `stopPropagation` 한다.**
  안 그러면 뒤에 있는 사이드패널·전역 검색(App.js 의 bubble 단계 Esc 핸들러)이 **같은 Esc 로 같이 닫혀**
  작성 중이던 내용이 날아간다. `ZoomSection` 이 쓰는 것과 같은 방식이다.
- `zIndex: 9999` — 사이드패널(900)·재신청 모달(1100)보다 위여야 한다.
- 짝꿍 헬퍼 `memoPreview(v, max)` — 표 한 줄에 보여줄 미리보기(줄바꿈을 공백으로 접고 앞부분만).

### 적용한 4곳 — **저장 위치(테이블·컬럼)는 하나도 안 바꿨다**
| 화면 | 칸 | 저장 | 저장 함수 |
|---|---|---|---|
| 기관별현황 **표** | 비고 | `agency_cases.notes` | `saveCaseMemo` — **편집 모드든 아니든 [확인] 누르면 바로 저장** |
| 기관별현황 **사이드패널** | 📝 이슈 메모 | `agency_cases.notes` | `saveCaseMemo` |
| 기관별현황 **사이드패널** | 추가 메모 | `agency_cases.extra_notes` | `saveCaseMemo` |
| 기업목록 **표** | 기타 | `companies.next_action` | `saveEtc` (기존 함수, 값만 인자로 받게 넓힘) |

- `saveCaseMemo(caseId, field, value)` 는 **사이드패널 「이슈 메모」의 옛 `onBlur` UPDATE 를 그대로 뽑은 것**이다.
  새 저장 경로를 만들지 말고 이걸 쓸 것. `setCases` + `setSelectedCase` 를 같이 맞춘다.
- ⚠️ **인라인 편집(연필) 중에 비고 팝업으로 저장하면 `editData.notes` 도 같이 맞춰야 한다.**
  `saveEdit` 은 `notes: editData.notes` 를 **항상** payload 에 싣기 때문에, 안 맞추면 그 줄의
  [저장]을 누르는 순간 **옛 값으로 방금 저장한 메모가 되돌아간다.**
  (기업목록 「기타」는 `saveRowEdit` 이 `next_action` 을 안 싣기 때문에 이 문제가 없다.)
- ⚠️ **표 셀은 `stopPropagation` 이 필수다.** 기관현황은 행 클릭 → 사이드패널, 기업목록은 행 클릭 → 기업상세가
  열린다. 안 걸면 팝업과 패널이 같이 뜬다. (기관현황 **우선도 셀**이 이미 같은 패턴 — 그걸 따라갔다.)
- ⚠️ 사이드패널 미리보기에 **`maxHeight` + `overflowY:auto`** 를 준다. 옛 textarea 의 `rows` 를 대신하는 것으로,
  안 주면 긴 메모(실측 최대 1,541자)가 패널을 통째로 늘린다.
- 기업목록 「기타」는 **더블클릭 → 클릭**으로 바뀌었다. 인라인 input 이 사라져 죽은 상태
  `editingEtcId`/`editingEtcVal` 도 같이 걷어냈다.

### 일부러 안 건드린 것
- 기업상세 「현재 이슈」·「차기 업무」 — 이미 `minHeight: 220` 이라 충분히 크다. 누를 때마다 팝업이 뜨면 불편하다.
- 기관현황 **신규등록 모달**의 「비고 / 메모」 — 이미 모달 안이라 모달 위 모달이 된다.
- 짧은 칸 전부(신청금액·상태·신용점수·계약금·수수료율) · 업무노트 원본 · 정산관리 비고 · 배정DB 이슈메모.

**모바일 영향 없음** — `AgencyView`·`ListView` 는 `CRMApp`(데스크톱)에서만 렌더된다.

### 📐 기업목록 「기타」 열은 좁은 화면에서도 숨기지 않는다 (2026-08-21)
`LIST_TABLET_HIDE` 에 있으면 **1024px 이하에서 CSS 로 통째로 사라져** 메모 기능에 손이 닿지 않는다.
그래서 「계약」과 같은 이유로 **일부러 뺐다.**
- ⚠️ **짝이 둘이다** — 헤더는 `LIST_TABLET_HIDE` 배열, 본문은 `<td className="lst-hide-tablet">`.
  **한쪽만 고치면 헤더와 본문 열이 통째로 한 칸씩 어긋난다.** 고쳤으면 개수를 세서 맞출 것
  (2026-08-21 기준 양쪽 다 11개, 전체 18열).
- ≤1024px 실측 최소폭: 대표자80+담당60+진행단계175+계약46+**기타140**+작업110 = 611
  ` + ` 업체명(auto, min 180) = **791px**. 1024px 화면에는 여유 있게 들어가고, 그보다 좁으면
  가로 스크롤이 생기지만 업체명이 `sticky left` 라 어느 줄인지는 계속 보인다.
- 더 줄여야 하면 **열 경계를 드래그**하면 된다(`listColWidths` → localStorage 자동 저장). 코드 수정이 아니다.

## 💵 기업현황표 매출액 — 단위 확정 · 입력칸 잘림 (2026-08-26, **DB 변경 0건 · 파서 재작성 0줄**)

### ① 첨부 자동입력이 "단위 확인 필요"로 빠지던 문제
현황표 머리글에 `매출액 (원)` 이라 적혀 있고 값도 `3284875000`(원 단위 전체 자릿수)인데
칸이 비어 들어갔다. 예전 주석("라벨이 (원)이어도 담당자는 28,886백만원이라고 적는다")이 맞는 말이라
**라벨 단위를 아예 안 본 것**이 원인이다.

**해결은 우선순위를 두는 것이다 — `resolveAmountCell(raw, headerLabel)` 한 곳에 있다.**

| | 근거 | 안내문 |
|---|---|---|
| ① 셀에 적힌 단위 | `"28,886백만원"` · `"5억3천만원"` | 없음(사람이 쓴 그대로) |
| ② 표 머리글의 **괄호 안** 단위 | `매출액 (원)` → ×1 · `(백만원)` → ×1e6 | 값이 실제로 환산될 때만 |
| ③ 9자리(1억) 이상이면 원 | `AMOUNT_WON_FLOOR` | 항상 남긴다 |
| ④ 그 외 | `"6,652"` | — **지금처럼 '확인 필요'로 비워 둔다** |

- ⚠️ **`extractLeadingAmount` 는 한 줄도 안 고쳤다.** `resolveAmountCell` 이 그 결과를 받아
  "확인 필요"로 나온 맨숫자만 다시 판정한다 → `parseFinanceNum` 등 다른 호출부는 동작이 그대로다.
- ⚠️ **`parseLoanAmount`(기대출)에는 절대 연결하지 말 것.** 거긴 사람이 자유롭게 적는 칸이라
  같은 규칙을 넣으면 옛 `"1억"→1원` 계열 사고가 재발한다(커밋 `d3df072`).
- ⚠️ **머리글은 괄호 안만 읽는다**(`headerUnitMult`). 라벨 본문을 단위로 오독하지 않기 위해서다.
  구 양식의 `매출 (2025년)` 은 괄호 안이 연도라 `null` 이 나온다 → 구 양식은 ③만 적용된다.
- 적용 범위: `parseHyeonhwangpyoV2` 의 **매출액 + 당기순이익** 두 열, `parseHyeonhwangpyo`(구 양식) 매출 3칸.
  · **당기순이익은 이번에 처음으로 값이 담긴다** — `2025년 당기순이익` 라벨로 `company_info` 에 들어간다.
    ⚠️ `computeFinanceRatios` 의 라벨 조회(`부채총계`·`자본총계`·`영업이익`·`이자비용`)와 겹치지 않는다(부분일치 확인함).
  · `부채총계 (원)` 등 기존 `company_info` 항목은 **원문 문자열 그대로 저장**이고 `parseFinanceNum` 이
    맨숫자를 이미 원으로 읽는다 → **고칠 게 없어 안 건드렸다.**
- 화면: 빨간 `⚠️ 확인 필요`(값 안 넣음) 옆에 파란 `ℹ️ 단위 해석`(값은 넣고 근거를 밝힘) 박스를 새로 뒀다.
  **기업상세 미리보기 모달·신규등록 모달 두 곳 모두**에 있다(`notes` 배열이 파서 → `parseUploadedSheet` → 화면).

### ② 수동 입력 뒷자리 잘림 — **입력칸은 고쳤고, DB 쪽은 확인 대기**
`type="number"` 입력칸은 `"3,002,936,000"` 처럼 **콤마째 붙여넣으면 값을 통째로 빈칸**으로 만들고,
`parseInt` 는 `"3e9"` 를 **3** 으로 읽는다. → `text` + `inputMode="numeric"` + `normalizeRevenueInput`
(숫자만 남기고 앞 0 제거, **12자리=9,999억 상한**)으로 바꿨다. 기업상세·신규등록 **두 칸 모두**.
- `formatRevenue` 가 만 미만 나머지를 버려 `30억 293만 6천` 이 `30억 293만` 으로 보이던 것도 같이 고쳤다
  (천 단위까지만 표시. 대부분 나머지가 0이라 표시는 그대로다).

⏸ **아직 확인 못 한 것 — `companies.revenue_*` 컬럼 타입.**
Supabase UI 로 만든 컬럼은 `integer`(int4, **상한 2,147,483,647 ≈ 21.47억**)인 전례가 있다
(2026-08-15 `companies.fee` 가 integer 라 수수료율 4.8 이 **421번** 재시도 실패 — `수수료율_소수점_fee_numeric.sql`).
매출 30억은 이 상한을 넘어 `22003` 으로 거절되고, CLAUDE.md 2-7 규칙상 **영구 실패로 분류돼 큐에서 빠지므로
사람 눈에는 "저장했는데 옛 값 그대로"**로 보인다. ①의 자동입력도 32.8억이라 같은 상한에 걸린다.
- `.env.local` 의 `SUPABASE_ACCESS_TOKEN` 이 **401** 이라 조회를 못 돌렸다. 토큰을 갱신한 뒤:
  1. `node scripts/run-sql.js 매출액_잘림의심_조회.sql` — 컬럼 타입 + 20억↑ 분포 + 연도 간 100배 차이 나는 잘림 의심 목록
     (⚠️ 한 파일에 SELECT 가 여러 개라 **하나씩 주석 해제해** 돌릴 것 — 2-2 규칙)
  2. ①에서 `integer` 로 나오면 `매출액_bigint_변경.sql` (되돌리기·검증 파일 같이 있음)
- **이미 저장된 값은 손대지 않았다**(사용자 결정). 조회는 목록만 뽑는다.

**검증**: `node scripts/test-amount-unit.mjs` 42/42 · `node scripts/test-revenue-input.mjs` 14/14
(둘 다 App.js 소스를 떼어내 실행 — 손으로 옮겨 적지 않는다) · 가짜 현황표 2종 e2e 15/15 ·
`CI=true npx react-scripts build` 통과.

## 📄 채권자변동정보 조회서(한국신용정보원) 첨부 (2026-08-28, **DB 변경 0건 · 새 쿼리 0건**)

여러 쪽 PDF 전체를 첨부하면 「1. 채무현황」을 마지막 쪽까지 읽어 기대출에 넣고,
「2. 연체채권의 채권자 변동 현황」은 **연체 이력 요약 한 줄**로 따로 남긴다.

### 막혀 있던 진짜 원인 — PDF 지원이 아니라 **문서 종류 목록**이었다
`api/parse-credit.js` 는 처음부터 PDF 를 `document` 블록으로 보내고 있었다(이미지만 되던 게 아니다).
막힌 곳은 셋이다: ① `doc_type` enum 이 `credit_report`/`bank_certificate`/`unknown` 3개뿐이라
조회서는 `unknown` 으로 빠지는데 **담당자가 고를 항목이 없었고** ② `rows` 스키마가
`대과목/소과목/금잔/만기구조` 라 조회서의 `구분/대출종류/기관명/발생일자/금액` 과 열이 안 맞고
③ SYSTEM 프롬프트의 *"연체 섹션은 단 한 줄도 담지 마세요"* 가 2번 표를 **구조적으로 버리고** 있었다.
캡처 이미지는 「채무현황」만 잘려 들어와 여신정보처럼 우연히 읽혔던 것이다.

### 세 번째 변환기를 나란히 뒀다 — **앞 두 변환기는 한 줄도 안 고쳤다**
`debtorChangeToLoans` / `debtorChangeOverdue` (`App.js`, `bankCertToLoans` 바로 아래).
`creditReportToLoans`·`bankCertToLoans`·`parseLoanAmount` 는 **바이트 단위로 동일**함을 배포 전 대조했다.
파서를 손대면 기존 문서 경로가 통째로 회귀 사정권에 들어온다(옛 `"1억"`→1원 사고, 커밋 `d3df072`).

- 💵 **금액 단위는 문서 표기를 따른다.** `(단위: 천원)` → 기존 `parseCreditUnit` 이 이미 ×1,000 으로 읽는다(신설 0줄).
  ⚠️ **단위 표기를 못 읽으면 "조회서니까 천원이겠지"라고 가정하지 않는다** — `확인 필요`로 남긴다.
  앞 두 변환기와 같은 추측 금지 원칙이고, 가정을 넣는 순간 위 사고 계열이 재발한다.
  · 환산 뒤 `parseLoanAmount` 로 **왕복 검증**해 값이 일치할 때만 확정한다(10만원 미만은 비우고 표시).
- 📅 **실행일 = 발생일자**(이 문서엔 적혀 있다) / **만기일은 비운다**(문서에 없다 — 추측 금지).
- 🏷 **구분은 문서의 '구분' 열 그대로** 넣는다: `개인사업자대출`→`biz` · `개인대출정보`→`personal`.
  ⚠️ 이건 추정이 아니라 **문서에 적힌 값을 옮기는 것**이다. 그 열이 없는 여신정보·금융거래확인서는
  지금도 전건 미분류다 — 그쪽에 이 매핑을 옮겨 붙이지 말 것.
  모르는 어휘(`기타채무` 등)는 미분류로 둔다. 자동 지정된 행엔 `kind_src:"문서표기"` 배지가 붙는다.
- 🚫 **연체 행은 기대출(loans)에 절대 안 들어간다.** 연체 금액을 대출로 넣으면 부채가 두 배가 된다
  (금융거래확인서가 담보·연체 섹션을 통째로 버리는 것과 같은 이유). 회귀 테스트에 이 항목이 있다.

### 서버 스키마 — 배열 2개를 새로 두고 금지 규칙에 **예외를 명시적으로 팠다**
`debt_rows`(채무현황) · `overdue_rows`(연체채권 변동). 둘 다 `doc_type=debtor_change` 일 때만 채운다.
⚠️ SYSTEM 의 금융거래확인서 금지 섹션 규칙은 **한 글자도 안 바꿨다.** 대신 제목을
`★★ 금융거래확인서에서 절대 담으면 안 되는 섹션 ★★` 로 적용 범위를 밝히고,
"조회서의 연체표는 `overdue_rows` **에만** 담고 `debt_rows`·`rows`·`bank_rows` 에는 여전히 금지"를
따로 적었다. 이 예외를 흐리게 쓰면 부채 이중계상이 되살아난다.

### 연체 이력은 `company_info` 한 줄 (**새 컬럼 0개**)
라벨 `연체 이력`(`OVERDUE_INFO_LABEL`) — `여신정보 기준일자` 와 **똑같은 upsert 방식**이라
다시 첨부해도 덮어쓰고 쌓이지 않는다.
`2건(미해제 1) · 농협은행 2024-03-11(해제 2024-05-02) · 국민은행 2025-01-20(미해제) · 기준 2026-08-28`
- **0건도 `없음 · 기준 …` 으로 남긴다** — "확인했는데 없더라"는 것도 정보다.
- 기준일자는 기존 `onApply(rows, mode, asOf)` 경로가 이미 `여신정보 기준일자` 에 넣고 있어
  **90일 최신화 배지가 공짜로 붙는다**(신설 0줄).

### 같은 조회서를 다시 첨부해도 안 쌓인다 — 장치가 둘이다
1. **`replace_src` 모드** — 「채권자변동정보로 넣은 N건만 교체」. `src === "debtor_change"` 인 행만
   걷어내고 새 행을 붙인다. **손으로 넣은 행·다른 문서로 넣은 행은 안 건드린다.**
   같은 출처 행이 이미 있으면 이 모드가 기본값이 된다.
2. **`loanDupKey`(기관명 | 발생일 | 금액(원))** — 이미 있는 행에 `이미 있음` 배지를 띄우고
   `행별 선택` 모드에서 체크를 미리 푼다.
   ⚠️ 이건 **힌트**다. 손으로 적은 행은 기관명 표기가 달라 안 걸릴 수 있다 — 확실한 제거는 1번이 한다.
   ⚠️ 이 힌트는 문서 종류와 무관하게 **전 경로에 걸린다**(여신정보·금융거래확인서 포함).
      기본 모드(추가/교체)는 `picked` 를 안 보므로 실제 반영 결과는 예전과 같다.

### 💵 `wonToKorExact` 가 만 미만을 버리던 것도 같이 고쳤다
`419,138,000원` 이 **`4억 1,913만`** 으로 보였다(뒤 `8천` 이 사라짐). 천 단위까지 표시하게 했다
(2026-08-26 `formatRevenue` 와 같은 성격). **표시 전용**이고 호출부 5곳뿐이며 합계·판정 로직은
이 값을 쓰지 않는다. `scripts/test-amount-unit.mjs` 의 안내문 기대 문구도 같이 맞췄다(값은 그대로).

### 일부러 안 한 것
- 남은 미연결·꼬리표 이름 정리 — 이 기능과 무관하다.
- 조회서의 **중복 체크를 `company_id` 기준으로 바꾸기** — 기존 판정 기준을 넓히지 않았다.
- 모바일 — `CompanyModal`·`CreditReportImport` 는 데스크톱 전용이라 영향 없다.

**검증**: `node scripts/test-debtor-change.mjs` **77/77**(App.js 소스를 떼어내 실행 — 손으로 옮겨 적지 않는다) ·
`test-amount-unit.mjs` 42/42 · `test-revenue-input.mjs` 14/14 ·
앞 두 변환기 **바이트 동일** 대조 · `CI=true npx react-scripts build` 통과(main.js +2.33 kB).
⚠️ `node scripts/audit-select-columns.mjs` 는 `SUPABASE_ACCESS_TOKEN` **401** 로 못 돌렸다(2026-08-26부터 알려진 상태).
이번 변경은 **supabase 조회·컬럼을 하나도 안 늘렸다**(`select` 추가 0건).
⚠️ 실제 조회서 PDF 로는 못 돌렸다 — `/api/parse-credit` 은 서버 API 키가 필요하다. 배포 후 화면에서 확인할 것.

## 💰 기대출 금액 입력칸 = "만원" 단위 고정 (2026-08-19, **DB 스키마 변경 0건 · 파서 변경 0줄**)

단위 글자를 안 붙이면 `"12,353"` 이 1만원인지 1,235만원인지 몰라 **합계에서 빠지던** 문제
(`parseLoanAmount` 의 `n < 100000` → `단위 확인 필요`)를, **입력 단위를 만원으로 고정**해서 없앴다.

**⚠️ `parseLoanAmount` 는 한 줄도 안 고쳤다. 앞으로도 고치지 말 것.**
대신 **저장 형식을 `"53000만"`(만 글자를 붙인 문자열)로 맞춰** 이미 검증된 파싱 경로를 그대로 타게 했다.
- 파서를 손대면 **기존 185행이 전부 같은 함수를 통과**하므로 회귀 사정권이 통째로 열린다
  (옛 사고: `"20백만원"`→20원, `"1억"`→1원 — 커밋 `d3df072`).
- **`×10,000` 해서 원 숫자로 저장하는 방식은 쓰면 안 된다.** 8만원 같은 소액이 `80000` 이 되어
  다시 `100000` 미만 → **"단위 확인 필요"가 재발**한다. 지우려는 경고가 되살아난다.

**공용 함수 2개** (`App.js`, `loanAmountOf` 바로 아래)
- `loanAmountToManInput(raw)` — 저장 원문 → 칸에 보일 만원 숫자. ⚠️ **해석 못 한 값
  (`"50+30+19"`, `"67백 -> 57백"` 같은 메모형)은 원문을 그대로 돌려준다.** 숫자로 바꿔 버리면
  사람이 그 메모를 못 고치고 저장된 원문이 화면에서 사라진다.
- `manInputToLoanAmount(s)` — 친 문자열 → 저장 원문. 숫자만 쳤으면 `+"만"`, 사람이 단위를 직접
  쓴 경우(`"5억3천만원"`)는 **그대로 둔다**(하위호환 — 옛 표기를 계속 입력할 수 있다).

- 입력칸은 **앱 전체에서 한 곳뿐**(`CompanyModal` 기대출 표). 모바일에는 기대출 편집 UI가 없다.
- **기존 행은 편집하지 않으면 DB가 안 바뀐다** — 표시만 환산하고 `onChange` 가 안 돌기 때문이다.
  실측: 저장된 185행 전수 왕복 대조(`won → ÷10000 → 재파싱`)에서 **금액이 달라진 행 0건**.
- 여신정보·금융거래확인서 자동입력(`creditReportToLoans`/`bankCertToLoans`)은 **원 단위 저장 그대로 뒀다.**
  문서 값이 딱 떨어지는 만원이 아닐 수 있어(92,938천원=9,293.8만) 원이 더 정확하고,
  `parseLoanAmount` 가 10만원 이상 맨숫자를 원으로 읽어 그대로 맞는다. 건드리면 회귀 사정권만 넓어진다.

### ⚠️ 저장된 옛 맨숫자는 단위가 **업체마다 다르다** — 일괄 환산 금지
2026-08-19 정리 대상 15건을 조사하니 같은 "맨숫자"인데 원래 단위가 제각각이었다.
**만원으로 일괄 적용하면 어떤 업체는 10배 뻥튀기, 어떤 업체는 100배 축소된다.**
판정 근거는 숫자 자체가 아니라 **업체 규모·상품 성격·`agency_cases` 신청금액**이다.
- 스튜디오오도씨·우진종합철거 = **천원** (소진공 1억 신청 중인데 기대출 8.4억은 성립 불가 /
  롯데오토리스 `14084` 는 차량리스라 1,408만원이 맞다)
- 퀸즈PC Cafe = **백만원** (PC방 총부채 88만원일 리 없다. 소진공 1억·신보 5천 신청 중)
- 사투99 = **만원**
→ 앞으로 비슷한 정리를 할 때 **한 규칙으로 밀지 말고 업체별로 원본을 확인할 것.**

**정리 완료 (2026-08-19)** — SQL: `기대출_단위확정_14건.sql` / 되돌리기 `_rollback.sql`
14건을 위 업체별 단위로 환산해 `"1235.3만"` 형태로 저장했다. `update` 마다
`loans->N->>'amount' = <옛값>` 조건을 걸어 **그 사이 사람이 고친 행은 건너뛰도록** 했다.
검증(실행 파일과 별도 조회로 재확인 — CLAUDE.md 2-2 규칙):
저장값 14/14 일치 · 확인 필요 19건 → 5건 · **계획한 14건 외 단 한 행도 안 바뀜(185행 전수 대조)**.

**아직 남은 5건 — 일부러 안 건드렸다.** 숫자 하나로 환산할 수가 없는 값들이다.
`"50+30+19"`(제일F&B) · `"67백 -> 57백"`(에스투코퍼레이션) · `"100백(잔액 27백)"`(비즈팟) ·
`"10건(완제, 거래중 다수)"`(골목식당) · `"26.05"`(보성무역 — **원본 확인 대기**).
앞 4건은 여러 건이 한 칸에 들어 있어 **행을 나눠야** 하고, 보성무역은 원문을 봐야 한다.
→ 자동 변환 대상이 아니다. 사람이 원본을 보고 화면에서 입력해야 한다.

## ✅🗑 밀린 미완료에서 바로 완료·삭제 (2026-08-18, **DB 스키마 변경 0건**)

밀린 미완료에 이월·가져가기밖에 없어 오래 밀린 건을 끝내거나 지울 방법이 없었다.
특히 **체크리스트가 없는 팀 카드 행은 버튼이 하나도 없었다**(`{r.item && (...)}` 가드).

**행이 두 종류라 동작이 다르다 — 섞지 말 것.**
| 행 | 완료 | 삭제 |
|---|---|---|
| 팀 · 체크리스트 **항목** (`r.item` 있음) | `completeTeamItem` → 항목 `done:true` | `deleteTeamItem` → checklist 에서 제거 |
| 팀 · 체크리스트 **없는 카드** (`r.item` 없음) | 기존 `markDone` → `status='done'` | 기존 `deleteTeamNote` → `deleted_at` soft delete |
| 개인 업무노트 항목 | `completeOverdueLine` → `- [ ]`→`- [x]` | `deleteOverdueLine` → 그 줄 삭제 |

- **저장 경로를 새로 만들지 않았다.** 팀은 `saveTeamChecklist` 하나(옛 `patchSchedItem` 의 저장부를 그대로
  떼어낸 것 — `patchSchedItem` 은 이제 이 함수를 부른다), 개인은 기존 `onChecklistChange` 하나만 쓴다.
  개인 쪽에 병합·재시도·활동로그·업무요청 동기화가 전부 붙어 있어 **직접 update 하면 그게 다 빠진다.**
- ⚠️ **팀 항목의 `done` 은 `taken_by`(가져감)와 다른 축이다.** 일반 항목은 지금까지 `done` 을 true 로
  만드는 코드가 없어 옛 데이터는 전부 false/undefined 다 → 새 분기는 **새로 완료한 항목에만** 걸린다.
  카드 렌더에 `doneIt` 분기를 넣어 ✅ + 취소선 + `완료 · 이름` 배지 + ↩️ 되돌리기를 보여준다.
  **안 넣으면 밀린 미완료에서는 사라졌는데 카드에서는 ☐ 그대로**라 무엇이 끝난 건지 알 수 없다.
- ⚠️ **옛 항목은 `id` 가 없을 수 있다.** 그래서 `teamItemHit(i, k, item, idx)` 가 id 가 있으면 id 로,
  없으면 **배열 위치**로 찾는다. id 로만 지우면 `undefined === undefined` 라 **id 없는 항목이 한꺼번에 지워진다.**
  이 때문에 `teamOverdue` 행에 `idx`(체크리스트 안 위치)를 실어 보낸다.
- ⚠️ 개인 삭제는 **줄 수가 바뀐다** → 그 사이 노트가 다른 곳에서 바뀌었으면 `reapplyLineChanges` 가
  null 을 돌려 **저장을 거부**한다(덮어쓰지 않는다). 이게 맞는 동작이다 — 사람에게 알리고 끝낸다.
- **알림은 자동으로 조용하다.** `handleTeamItemAdded` 는 **항목 수가 늘었을 때만** 울린다
  (완료=수 그대로, 삭제=수 감소). 비교 기준 `teamItemCountRef` 는 이벤트마다 가장 먼저 갱신되므로
  삭제해도 다음 추가 알림이 어긋나지 않는다. → 그래서 `markTeamNoteSelfEdit` 를 **찍지 않는다.**
- 권한: 팀 **카드 삭제**만 기존 규칙(양호·admin·등록자)이라 그 사람에게만 버튼이 보인다.
  팀 **항목**은 누구나 추가·수정할 수 있는 기존 규칙을 따라 완료·삭제도 전원 허용.
  개인은 `canEditNote`(본인 노트 또는 WN 관리자)일 때만 버튼이 뜬다.
- 확인창은 **삭제에만** 있다(요청 사항). 완료는 ↩️ 로 되돌릴 수 있어 확인창을 두지 않았다.

### ↩️ 카드 완료 취소 · 진행률에 완료 포함 (2026-08-19, **DB 스키마 변경 0건**)
위 두 개의 "남은 점"을 마저 메운 것이다.

- **`undoMarkDone(note)`** — `status:'done'` 을 되돌린다. ⚠️ `team_notes` 는 **완료 전 상태를 저장하지
  않는다**(`status` 한 칸뿐). 그래서 `taken_by` 로 복귀 지점을 정한다: **가져간 사람이 있으면 `taken`,
  없으면 `open`** — 흐름 `open → taken → done` 과 같은 규칙이라 어긋나지 않는다.
  ⚠️ 전용 컬럼(`prev_status`)을 새로 만들지 말 것. 되돌린 카드는 **밀린 미완료에 다시 나타난다**(의도).
- 버튼은 **2곳**: ① 완료돼 한 줄로 접힌 카드(줄 전체가 "펼치기" 클릭이라 **`stopPropagation` 필수** —
  없으면 취소와 펼치기가 같이 일어난다) ② 펼친 카드 하단 액션 줄.
  되돌릴 때 `expandedDone[note.id]` 를 지운다 — '완료라서 접힌 것을 펴 둔 목록'이라 완료가 풀리면 의미가 없다.
- 권한 게이트를 **일부러 안 걸었다.** `markDone` 은 카드 하단에서만 `isMine` 이고 **밀린 미완료 구역에서는
  누구나** 부른다 → 취소만 좁히면 "남이 완료한 걸 못 푸는" 막다른 길이 생긴다.
- **알림은 자동으로 조용하다** — `handleTeamItemAdded` 는 체크리스트 **항목 수가 늘 때만** 울리는데
  status 변경은 항목 수가 그대로다. 그래서 `markTeamNoteSelfEdit` 도 찍지 않는다(위 절과 같은 이유).
- 진행률 바는 이제 **`taken_by || done`** 을 센다(`progressCount`), 라벨도 "N/M 완료·가져감".
  ⚠️ **두 축을 합쳐 세는 곳은 이 표시 한 곳뿐**이다 — 판정 로직(`teamOverdue`·완료 숨김·카드 렌더)은
  여전히 `done` 과 `taken_by` 를 각각 본다. 여기 규칙을 판정 쪽에 옮기지 말 것.


## 2-3. 세션 수명 제한은 pg_cron 이 대신한다 (2026-08-11)
Supabase 정식 기능(`sessions_timebox`/`sessions_inactivity_timeout`)은 **Pro 플랜 전용**이라
무료 플랜에선 Management API 가 **402** 를 준다. 그래서 `auth.sessions` 를 직접 청소한다.
- cron 작업 `sweep-stale-sessions` — 매일 UTC 18:00(KST 03:00), 생성 30일 초과 또는 14일 미갱신 세션 삭제.
  SQL: `세션수명제한_pg_cron_자동청소.sql` / 해제 `_rollback.sql`
- `auth.refresh_tokens.session_id` 가 **ON DELETE CASCADE** 라 세션을 지우면 리프레시 토큰도 사라진다
  (실측: 세션 30→17 일 때 토큰 822→306). 다만 액세스 토큰은 살아 있어 **최대 1시간 뒤** 로그아웃된다.
- ⚠️ **즉시 차단이 필요하면 이걸 쓰지 말 것** — 그건 `profiles.status='rejected'` 담당이다.
  비활성화하면 세션은 살아 있어도 데이터·API 가 전부 즉시 막힌다(실증 9/9, `퇴사자_세션차단_실증.sql`).
  단 그 UPDATE 는 `trg_protect_profile` 때문에 **관리자 컨텍스트에서만** 먹는다(아래 2-4).

## 2-4. `profiles.role`/`status` 변경은 관리자 컨텍스트에서만 먹는다
`trg_protect_profile` → `protect_profile_privileges()` 가
`if not public.is_admin() then new.role := old.role; new.status := old.status; end if;` 다.
`scripts/run-sql.js` 는 `auth.uid()` 가 없어 `is_admin()`=false → **에러 없이 조용히 되돌린다.**
커밋은 성공하는데 값은 그대로라 "됐다"고 오판하기 딱 좋다. 반드시 이렇게 감쌀 것:
```sql
set request.jwt.claims = '{"sub":"<admin uuid>","role":"authenticated"}';
set role authenticated;
update public.profiles set status = '...' where ...;
reset role;
```
(`is_admin()` = uid 가 `role='admin' and status='approved'`. 현재 양호·정원만 해당.)

## ⚠️ 파이프라인 단계 · 계약금 정산 반영 — 2026-08-11 신설 (2026-08-18 STEP2 폐지로 개정)
(SQL: `파이프라인_단계개편_계약금입금완료.sql` 7/7 · `계약금_수수료율_컬럼추가.sql` 8/8, 각 `_rollback`/`_검증`
 · 폐지 `파이프라인_STEP2_계약금입금완료_폐지.sql` / `_rollback` · 조사 `단계제거_조회_계약금입금완료.sql`)

### 🔻 2026-08-18: STEP2 "계약금입금완료" 칸을 파이프라인에서 없앴다
실무에서 계약금이 들어오면 곧바로 '필수서류 및 인증서요청'으로 넘어가 이 칸을 거치지 않았다(항상 0건).
제거 전 실측: `pipeline_cards.stage` 0건(휴지통 포함) · `companies.stage` 0건(삭제분 포함) ·
`agency_cases.status` 0건 · `status_stage_map` 규칙 0건 → **옮길 데이터가 없어 데이터는 손대지 않았다.**
지운 것은 `stage_stagnation_config` 고아 행 1건뿐(화면에 안 그려져 사람이 지울 수도 없는 행이 된다).

- **STEP 번호는 전부 `STAGES.indexOf()+1` 로 계산한다 — 하드코딩된 번호가 한 곳도 없다.**
  그래서 배열에서 한 칸 빼면 뒤 단계가 자동으로 당겨진다(구 STEP3 필수서류 → **STEP2**).
  보드 헤더·이동 select·상태→STEP 매핑 모달·기업상세 카드 배지가 전부 같은 계산을 쓴다.
  → 앞으로도 STEP 번호를 글자로 박지 말 것. 안내문이 필요하면 `advanceStageLabel()` 처럼 계산해서 쓴다.

**`contract_status` 와 `stage` 의 이름 중복은 이걸로 사라졌다. 연동은 이제 한 방향뿐이다.**
- `companies.contract_status` (`CONTRACT_STATUSES`) — 서명 절차가 어디까지 갔나. **정산 반영의 유일한 발동 조건.**
- `companies.stage` / `pipeline_cards.stage` (`STAGES`) — 파이프라인 단계. 정산과 무관하다.

- 계약 상태를 켜면 → `advanceCardsToContractPaid()` 가 그 업체의 열린 카드를
  `CONTRACT_PAID_ADVANCE_STAGE`(= "필수서류 및 인증서요청") 로 **앞으로만** 옮긴다.
  이미 그 단계를 지난 카드는 안 건드린다(뒤로 끌면 진행이 후퇴한다). 부결/반려·기타는 STAGES 뒤쪽이라 자동 제외.
  드래그 이동과 같은 의미가 되도록 `sync_mode='manual'` 로 고정한다 —
  **안 그러면 다음 기관상태 자동 동기화(`status_stage_map`)가 단계를 되돌려 놓는다.**
- ⚠️ **반대 방향(카드를 끌면 계약 상태가 켜짐)은 일부러 없앴다.** 옛 `applyContractPaid()` 는 제거했다.
  "필수서류 및 인증서요청" 으로 옮겼다고 계약금이 입금된 것은 아니기 때문이다.
  **계약 상태를 켜는 곳은 기업상세의 [계약금입금완료] 버튼 하나뿐**이고, 정산 반영도 거기서 함께 실행된다.
  → 파이프라인 드래그·일괄 이동에는 이제 정산 부작용이 **하나도 없다**(단계만 바뀐다).

**정산 자동 반영(`syncSettlementFromCompany`)에서 지킬 것**
- 조건은 **OR** 다: `contract_status='계약금입금완료'` **또는** `fee_status='수수료수령완료'`.
- 반영하는 건 **계약금(`contract_fee`)·수수료율(`commission_rate`) 둘뿐**이다.
  ⚠️ **`settlement_manual.commission_fee`(수수료 "금액")는 절대 건드리지 말 것.** 사람이 직접 넣는 칸이다.
  함수는 그 키를 아예 쓰지 않는다 — 새 기능을 붙일 때도 넣지 말 것.
- 대상은 **같은 업체명의 수동 정산행 중 최신 1건**. 없으면 새로 만든다.
  자동행(`agency_cases`)은 대상이 아니다 — 수수료율 컬럼 자체가 없다.
- **매 저장마다 밀어넣지 않는다.** 조건이 새로 참이 됐거나 계약금·수수료율 값이 실제로 바뀐 경우만 보낸다.
  (안 그러면 정산관리에서 사람이 고쳐 둔 계약금을 저장할 때마다 덮어쓴다.)
- 계약 상태 버튼은 즉시 저장이라, 켤 때 **계약금·수수료율도 같은 UPDATE 에 실어 보낸다.**
  안 실으면 "정산엔 반영됐는데 기업정보엔 저장 안 된" 상태가 만들어진다.

**폐지된 "스크립트 전달 완료"** — 카드 0건·기업 0건·기관상태 0건이라 데이터 이동 없이 지웠다.
같이 지운 것: `status_stage_map` 규칙 1건(남기면 그 상태가 되는 순간 보드에 없는 단계로 카드가 날아가 **화면에서 사라진다**),
`stage_stagnation_config` 행 1건. 새 단계 정체 기준은 `enabled=false` 로 넣었다(알림을 임의로 늘리지 않기 위해).

## ⚠️ 배정DB — 구글시트 조회 전용 화면 — 2026-08-12 신설 (1단계)
DB 변경 0건. 사이드바 `더보기 → 배정DB`(`view === "assigndb"`, `AssignDbView`).

**시트가 원본이고 CRM 은 거울이다.** 수정 기능은 **일부러 안 만들었다**(사용자 결정 — 이사님은 시트에서만 작성).
쓰기 기능을 붙이자는 요청이 오면 "시트 ↔ CRM 어느 쪽이 최신인가" 충돌 규칙부터 정해야 한다.

- 시트: `배정DB_김동일 이사님` / `ASSIGN_DB_SHEET_ID = 1LQ_BF9vU-4J-PpX-r-HWj213ftZA4GjZkbFcrnU8A-g`
- 읽는 법: **Drive API 로 xlsx export → 기존 `readUploadedWorkbook()`**.
  Drive API·`drive.readonly` 스코프를 이미 쓰고 있어 **추가 구글 동의창도, 콘솔 설정도 없다.**
  · Sheets API 를 쓰려면 Cloud 콘솔에서 API 사용설정을 켜야 한다(안 켜면 403) — 그래서 안 썼다.
  · CSV export 는 쓰지 말 것 — 메모·진행방향 칸에 줄바꿈과 쉼표가 섞여 있어 파싱이 깨진다.
  · ⚠️ export 는 **첫 번째 탭만** 준다. 탭이 늘면 첫 탭만 보인다(화면 하단에 안내 있음).
- **컬럼을 코드에 박지 말 것.** 이 화면의 존재 이유가 "시트를 고쳐도 CRM 은 안 고친다"이다.
  헤더 행을 `findAssignDbHeaderRow()` 로 매번 찾아 그 순서 그대로 그린다.
  ⚠️ 헤더 판정에서 **찬 칸 수만 세면 안 된다** — 헤더 행은 A열(순번)이 비어 있어 데이터 행보다
  한 칸 적고, 그러면 첫 데이터 행이 헤더로 잡혀 **표가 통째로 한 줄 밀린다**(실제로 그렇게 틀렸다).
  그래서 `assignDbHeaderScore()` 가 "짧은 글자 칸 +2 / 숫자·날짜·금액 칸 −3 / 힌트 이름 ×20"으로 채점한다.
  힌트(`ASSIGN_DB_HEADER_HINTS`)가 하나도 안 맞아도(=컬럼명을 전부 바꿔도) 동작한다.
- 토큰은 기존 `getDriveToken()`/`connectGoogleDrive()` 그대로 — 브라우저 localStorage, 약 1시간 만료.
  서버에 refresh token 을 저장하지 않는다(2026-07-27 사고 이후 `google_oauth_tokens` 잠긴 채 유지).

### 2단계 — Apps Script → Supabase 캐시 (2026-08-12, 지금 이게 기본 경로)
(SQL: `배정DB_스냅샷_테이블.sql` / `_rollback` / `_검증` 8/8 · 엔드포인트 `api/assign-db-sync.js` 검사 19/19)

1단계는 **브라우저가 본인 구글 계정으로 직접 읽어서**, 시트 소유자 계정이 아니면 막혔다.
2단계는 그 제약을 없앤다 — 팀원은 **CRM 로그인만 하면 되고 구글 연결이 아예 필요 없다.**

```
구글시트 → Apps Script(설치형 onChange + 10분 안전망) → /api/assign-db-sync → assign_db_snapshot → 화면(Realtime)
```

- **테이블은 `assign_db_snapshot` 1행뿐이고 `grid jsonb` 에 시트 2차원 배열을 가공 없이 넣는다.**
  ⚠️ 컬럼을 고정 스키마로 펼치지 말 것 — 펼치는 순간 "시트를 고쳐도 CRM 은 안 고친다"가 깨진다.
  헤더 판정은 **Apps Script 도 서버도 아니고 App.js `buildAssignDbTable()` 한 곳**에서만 한다.
  1단계 직접 읽기와 2단계 스냅샷이 **같은 함수를 통과**하므로 결과가 갈라지지 않는다.
- **쓰기 정책을 만들지 않았다.** `authenticated` 는 SELECT 권한만 있다(INSERT/UPDATE/DELETE 회수).
  쓰기는 service_role 로 서버리스 함수만 한다 → 사람은 아무도 못 쓴다.
- ⚠️ **`api/assign-db-sync.js` 는 다른 5개 엔드포인트와 인증 방식이 다르다.**
  기계(Apps Script)가 부르므로 사람 JWT 가 없어 `denyUnauthorized()` 를 쓸 수 없다.
  대신 공유키(`ASSIGN_DB_SYNC_SECRET`) 상수시간 비교. **이 키로 할 수 있는 일은 스냅샷 1행 갱신뿐**이다.
  service_role 키를 구글에 두지 않는 이유: 시트 편집 권한자가 스크립트를 열면 RLS 를 통째로
  우회하는 DB 마스터키를 보게 된다. service_role 은 Vercel 환경변수에만 둔다.
- 안전장치: **행 0건은 반영하지 않는다**(시트 사고로 데이터가 날아가도 스냅샷은 보존 — 시트가 원본이라
  다음 정상 동기화 때 복구된다). 40만 칸 초과는 413.
- Apps Script 는 내용이 그대로면 전송을 생략한다(md5 지문) → 10분 안전망이 돌아도 헛일을 안 한다.
- 필요한 환경변수(Vercel): `ASSIGN_DB_SYNC_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`.
  **둘 중 하나라도 없으면 500 + 어느 값이 없는지 알려준다** — 설치 중 원인을 못 찾는 게 더 위험해서 일부러 그렇게 했다.
- 시트 쪽 코드 원본: `apps-script/배정DB_동기화.gs` (저장소엔 참고용, 실제로는 시트 안에서 돈다).
  트리거를 고치면 `setupAssignDbSync()` 를 다시 실행해야 반영된다.
- 구글 직접 읽기는 **비상용 버튼으로만** 남겼다(동기화가 멈췄을 때 권한 있는 사람이 확인). 이때는
  "🆘 구글에서 직접 읽음 (이 화면에만)" 배지가 뜬다 — 남에게는 안 보이는 내용이라는 표시다.

## ⚠️ 파이프라인 카드 일괄 이동 · 삭제(휴지통) — 2026-08-12 신설
(SQL: `파이프라인_카드삭제_휴지통.sql` / `_rollback` / `_검증` 8/8)

**카드 1행 = (회사 × 기관) 조합**이다. 카드를 지워도 `companies`(기업)도 `agency_cases`(기관현황 원본)도
건드리지 않는다 — 그 기업의 그 기관 신청 건 하나만 사라진다. 반대 방향만 연결돼 있다
(`company_id ... on delete cascade` → 기업을 **영구삭제**하면 카드도 같이 사라짐).

**⚠️ 최대 함정: 삭제한 카드가 되살아난다.**
`syncPipelineFromCase()`(App.js:1196)는 기관현황을 저장할 때마다 (회사×기관) 카드를 찾고 **없으면 새로 만든다.**
그래서 카드 조회에 **`deleted_at` 필터를 걸면 안 된다** — "카드 없음"으로 오판해 insert 로 내려가는데,
삭제된 카드가 unique 슬롯을 쥐고 있어 **unique 위반 에러**가 난다.
→ 조회는 삭제분까지 포함해서 하고, `if (card.deleted_at) return;` 으로 **조용히 끊는다**(`sync_mode==='manual'` 과 같은 자리).
실측으로 확인함: 삭제 후 `deleted` 포함 조회 1건 / 제외 조회 0건 / 그 상태 insert → `unique_violation`.

**unique index 는 일부러 완화하지 않았다.** `pipeline_cards_company_agency_uniq` 를 `where deleted_at is null` 로
바꾸면 같은 조합 카드가 보드와 휴지통에 동시에 생겨 **복구 시점에 충돌**한다. 지금은 삭제 카드가 슬롯을
계속 점유해서 복구가 **항상 성공**한다. 대신 같은 조합을 새로 만들려 하면 막히므로,
`findDupCard` 가 삭제분도 찾아 "휴지통에서 복구하세요"로 안내한다(`dupAlert`).

**카드를 상태에 담는 모든 경로는 `.is("deleted_at", null)` 로 거른다** — 보드·정체알림·대시보드가 전부
`pipelineCards` 상태 하나를 본다. 거른 지점: 메인 로드(6100)·파이프라인 진입(10405)·재동기화(10153·10810)·
대시보드 단계 카운트(9291)·기업상세(13689)·`resyncAutoCards`·`advanceCardsToContractPaid`.
`pipelineCardData`·정체 계산에도 이중 방어를 넣었다. **예외는 `syncPipelineFromCase` 하나뿐**(위 이유).

**단계 이동 로직은 `moveCards()` 하나뿐이다.** 개별 드래그(`handleDrop`)와 일괄 이동(`doBulkMove`)이
같은 함수를 부른다. 두 벌로 두면 반드시 어긋나므로 **새 이동 경로를 만들 때도 이 함수를 경유할 것.**
- 카드마다 출발 단계가 달라 정리 필드(기타 사유/부결 종결 해제/재배치 확인)가 제각각이라 **카드별 UPDATE** 다.
  한 방 UPDATE 로 묶으면 안 된다. 실패 건은 건너뛰고 끝에 모아 보고한다.
- **STEP2 연동은 회사 단위로 중복 제거**해 회사당 1회만 실행한다(`applyContractPaid`).
  안 그러면 같은 회사 카드가 여러 장일 때 정산에 같은 값을 여러 번 민다.
- 일괄 대상은 **화면에 보이는 카드(`visibleRows`)로 한정**한다. 선택 후 필터가 바뀌어 사라진 건은
  대상에서 빼고 "화면 밖 N건 제외"로 알린다 — 안 보이는 카드가 움직이면 사고다.

**삭제 권한은 승인된 팀원 전원**(기업목록 휴지통과 동일). 복구·영구삭제도 같다.

⚠️ **남은 흔적 — 카드 2장의 정체 기준시각이 조사용 테스트 시각으로 남아 있다** (2026-08-17, 원복 SQL 실행 완료 후)
`a6bd3d0`(다중 선택 드래그 버그) 조사 때 운영 카드 6장을 실제로 옮겨 재현했고, 그 뒤 전부 원래 단계로
되돌렸다(6/6 확인 · 테스트 `pipeline_move` 로그 6건도 soft delete 완료). 다만 **아보르시스템**(소진공) ·
**아이디어스푼**(기타) 2장은 이동 전 `stage_changed_at` 을 미처 떠 두지 못해 **단계만 복구했고 시각은
`2026-08-17 10:03:30+00`(테스트 이동 시각) 그대로**다 → 이 둘은 정체일수가 실제보다 짧게 계산돼
정체 알림에 늦게 뜬다. **원래 값은 어디에도 남아 있지 않아 복구 불가**(activity_logs memo 는 단계만 적는다).
`sync_mode` 도 근거 추정으로 `manual` 로 뒀다 — 틀렸다면 화면에서 🔄/📌 토글로 바꾸면 된다.
→ 앞으로 운영 DB 로 재현 테스트를 할 때는 **바꿀 컬럼 전체를 먼저 SELECT 로 떠 둘 것**(단계만으로는 부족하다).

## ⚠️ 업무요청·요청현황 버튼은 "신호"로 연다 — 2026-08-11
사이드바(데스크톱)·상단바(모바일)의 📩📋 버튼은 **상태를 전역으로 끌어올린 게 아니다.**
`pendingWnAction`(App) → `WorkNotesView` 의 `openAction` prop → useEffect 가 `setShowRequest`/`setShowReqStatus`
를 호출하고 **소비 후 신호를 지운다**(안 지우면 업무노트로 돌아올 때마다 모달이 다시 뜬다).
요청 보내기·답장·완료 처리는 업무노트의 노트 상태와 얽혀 있어 전역으로 옮기면 업무노트 전체가 회귀 사정권이다(1절).
- 배지는 App 이 이미 들고 있는 `reqAlertList`(받은 미확인)·`sentReplyAlertList`(새 답장)를 그대로 쓴다 — 새 쿼리·새 폴링 없음.
- **모바일은 `WorkNotesView` 를 렌더하지 않는다**(자체 UI). 그래서 모바일 전용 모달을 따로 만들었다.
  단 **보내기 경로는 데스크톱과 같아야 한다**: `wn_append_todo` RPC → `work_requests` → 푸시.
  직접 select 후 update 하면 남의 노트를 못 찾아 같은 날 노트가 두 장 생긴다.

## 2-2. DB 테이블을 새로 만들면 — RLS를 같은 커밋에서 켠다
Supabase는 테이블 기본값이 **"열림"**이라, RLS를 켜지 않으면 비로그인 anon key로 전건이 조회된다.
과거 `보안_RLS_승인제_SETUP.sql`이 테이블을 **이름으로 나열**하는 방식이라, 그 뒤에 만든 테이블마다
구멍이 생겼다(2026-07-27 사고: 10,801건 노출). 목록 방식에 기대지 말고 **테이블을 만든 그 커밋에서** 처리한다.

- [ ] `alter table public.<t> enable row level security;`
- [ ] `is_approved()` 기반 정책 부여(승인된 로그인 사용자만). **`to public` / `to anon` 정책은 만들지 않는다.**
- [ ] anon 권한 회수: `revoke all on public.<t> from anon;`
- [ ] **불필요 권한 회수**: `revoke truncate, references, trigger on public.<t> from authenticated, anon;`
      앱(PostgREST)은 이 셋 중 어느 것도 쓰지 않는다. 반면 위험은 실재한다:
      **TRUNCATE 는 RLS 도 행 트리거도 우회**해 승인된 토큰 하나로 테이블을 통째로 비울 수 있고,
      **TRIGGER 는 이미 있는 `security definer` 함수를 호출하는 트리거를 남의 테이블에 달 수 있어**
      조건이 갖춰지면 권한 상승 경로가 된다.
      2026-08-11에 26개 테이블에서 셋 다 일괄 회수했다(각각 26→0 확인).
      SQL: `authenticated_TRUNCATE_회수.sql` · `authenticated_REFERENCES_TRIGGER_회수.sql`(+ `_검증`/`_rollback`)
      회수 후 `authenticated` 에 남은 건 **SELECT/INSERT/UPDATE/DELETE 뿐**이다(앱에 필요한 것만).
      기존 FK 18건·보호 트리거 4개는 영향 없다(REFERENCES 는 제약 생성 시점에만 필요).
      `postgres` 소유 기본권한도 `arwdDxtm` → **`arwdm`** 으로 줄여, `run-sql.js` 로 만든
      테이블엔 다시 안 붙는다.
      ⚠ 다만 **`supabase_admin` 소유 기본권한은 아직 `arwdDxtm` 그대로**다 —
      `postgres` 가 그 롤의 멤버가 아니라(`pg_has_role`=false) 여기서 못 고친다.
      supabase_admin 이 만든 테이블에는 다시 붙을 수 있으니
      **새 테이블마다 위 한 줄을 실행하고 카탈로그로 확인할 것.**
      (아직 남은 것: `MAINTAIN` 26개 — VACUUM/REINDEX 계열이라 데이터 노출은 아니고 부하 위험만 있다.)
- [ ] 옛 정책이 남아 있지 않은지 확인 — PERMISSIVE 정책은 **OR로 합쳐져서**,
      `{anon} USING(true)` 하나만 남아도 새 정책이 무력화된다.
      **같은 역할(`authenticated`)끼리도 마찬가지다.** 정책을 "추가"하면 조이는 게 아니라 **푸는** 결과가 된다.
      새 정책을 만들 때는 **반드시 옛 정책을 `drop`하고** 만들 것.
      (2026-07-29 사고: `profiles`에 INSERT 정책이 2개 — 엄격한 `p_profiles_insert`
      (`role='member' AND status='pending'`)와 느슨한 `profiles insert own`(`auth.uid()=id`)이 OR로 합쳐져
      실효 조건이 `auth.uid()=id` 하나뿐이었다. 신규 가입자가 자기 프로필을
      `role='admin', status='approved'`로 INSERT하면 **승인 절차를 건너뛰고 관리자**가 됐다.
      트리거 `trg_protect_profile`은 `BEFORE UPDATE` 전용이라 INSERT 경로를 막지 못했다.
      → 구 정책 3개 제거 + `BEFORE INSERT` 트리거(`protect_profile_insert`) 추가로 봉쇄.
      **정책은 언제든 다시 느슨해질 수 있으니, 값 강제는 트리거로 이중 방어할 것.**)
- [ ] 배포 전 점검(결과가 **0행**이어야 정상):
      ```sql
      select c.relname from pg_class c
       where c.relnamespace='public'::regnamespace and c.relkind='r' and c.relrowsecurity = false;
      ```
- Storage 버킷도 같다: `public=false` + 정책은 `to authenticated`.
  공개 버킷이 아니므로 **`getPublicUrl()`을 쓰면 안 된다** → `createSignedUrl()`(App.js `StorageAudio`).
- `/api/*` 서버리스 함수를 새로 만들면 **첫 줄에 `denyUnauthorized(req, res)`**(`api/_auth.js`).
  검사가 없으면 누구나 호출해 `ANTHROPIC_API_KEY` 요금을 태울 수 있다.
  프런트는 반드시 `callApi()`(App.js)로 호출한다 — 토큰·anon key 헤더를 붙여준다.
- ⚠️ 판정 함정: RLS는 "거부"가 아니라 "필터링"이라 **`200 OK` + 0건이 정상 차단 상태**다.
  또 PostgREST는 RLS가 막았을 때와 0행 매칭일 때 **둘 다 204**를 준다 →
  "없는 id로 UPDATE 찔러보기"로는 쓰기 권한을 판정할 수 없다. 카탈로그(`pg_policies`·GRANT)로 확인할 것.
- ⚠️ `revoke`는 **grantor가 다르면 에러 없이 아무것도 안 한다.** 권한을 부여한 롤과 회수하는 롤이
  다르면(예: `supabase_admin`이 준 것을 `postgres`로 회수) 조용히 통과하고 권한은 그대로 남는다.
  **"성공했으니 회수됐다"고 믿지 말 것** — 실행 후 반드시 카탈로그로 재확인한다:
  ```sql
  select table_name, privilege_type, grantor from information_schema.role_table_grants
   where table_schema='public' and grantee='anon';   -- 0행이어야 정상
  ```
- ⚠️ `scripts/run-sql.js`는 여러 SELECT가 든 파일을 실행해도 **결과를 하나만** 출력하고,
  그게 **파일 마지막 검증 쿼리가 아닐 수 있다.**
  (2026-07-29: `anon_GRANT_회수_16개테이블.sql` 실행 후 파일 **상단의 사전 스냅샷**(회수 전 112행)이 찍혀
  회수가 실패한 것처럼 보였다. 실제로는 정상 회수된 상태였다.)
  → **검증은 조치 파일에 딸린 SELECT를 믿지 말고, 실행 후 별도 조회로 다시 확인할 것.**

## 2-5. 전체 조회는 `fetchAllRows` 로 — `.limit(1000 초과)` 는 금지어다 (2026-08-12)

PostgREST 는 서버 설정(max-rows)으로 **한 번에 1000행까지만** 준다.
`.limit(10000)` 을 걸어도 **에러도 경고도 없이 조용히 무시**된다.
→ 화면은 멀쩡한데 숫자만 틀리는, **가장 알아채기 어려운 방식**으로 고장난다.

2026-08-11 에 AI검색에서 한 번 잡았지만(`09b4482`), 고친 함수가 **지역 함수라 재사용이 안 돼서**
같은 버그가 앱 곳곳에 그대로 남아 있었다. 2026-08-12 에 공용 함수로 올리고 전부 교체했다.

- 공용 함수는 **`src/App.js` 최상단 `fetchAllRows(table, cols, opts)`** 하나뿐이다.
  반환값이 supabase 와 **같은 모양(`{ data, error }`)** 이라 기존 호출부를 그대로 두고 바꿔 낄 수 있다.
  - `opts.build` — 필터 콜백 `(q) => q.is("deleted_at", null)`
  - `opts.orderBy`/`ascending`/`tieBreak` — 페이지 경계 고정용. **기본값(created_at→id 오름차순)을 바꾸지 말 것.**
    AI검색 스냅샷은 매번 같은 순서여야 JSON 바이트가 같아 **프롬프트 캐시가 산다.**
  - 실패하면 `data: null` 을 준다 — **반쪽 데이터를 성공처럼 쓰면 1000행 절단과 똑같은 사고**가 되므로 일부러 그렇게 했다.
- **판단 기준은 "지금 1000행이 넘느냐"가 아니라 "전체를 받아야 하는 조회냐"**다.
  지금 안 넘어도 넘는 순간 조용히 틀리기 시작한다. 넘을 수 있으면 지금 `fetchAllRows` 로 써 둔다.
- `.limit(N)` 은 **"최근 N건만 보여준다"는 의도일 때만** 쓴다. 그때도 N ≤ 1000 이어야 의도대로 동작한다.
  (남아 있는 `.limit(1000)` 4곳은 전부 이 의도다 — 채팅 최근 목록·배지 카운트.)

**실측 행 수 (2026-08-12, `scripts/run-sql.js` 로 확인)**
`activity_logs` 2,323 · `agency_cases` 2,222(살아있는 것 1,734) · `companies` 1,039(살아있는 것 397) ·
`pipeline_cards` 796 · `db_leads` 720 · `work_notes` 582 · `chat_messages` 366 · 나머지 100 미만.
→ **1000을 넘는 건 `activity_logs`·`agency_cases` 둘뿐**이다. `pipeline_cards` 가 다음 차례다.
숫자가 틀린 것 같으면 추측하지 말고 위 스크립트로 다시 세어 볼 것.

⚠️ `companies` 는 총 1,039행이지만 `.is("deleted_at", null)` 이 **서버에서 먼저 걸리므로** 397행만 온다.
필터는 1000행 상한보다 **먼저** 적용된다 — 그래서 "총행이 1000을 넘는다"만으로 판단하면 안 되고,
**실제로 전송되는 행 수**로 판단해야 한다.

## 2-6. PostgREST 조회를 손으로 조립할 때 — 따옴표·컬럼명 두 함정 (2026-08-15)

`api/ai-search.js` 처럼 supabase-js 를 안 쓰고 **URL 을 직접 만드는** 코드에서만 해당한다.
둘 다 **에러가 아니라 200 + 0건**으로 나타나서, 화면상 "원래 없는 것"과 구분이 안 된다.

**① 큰따옴표는 쓰는 자리가 정해져 있다.**
- 최상위 조건 `col=op.값` → **따옴표 금지.** 붙이면 따옴표째 값이 되어 **항상 0건**이다.
- `in.(...)` / `or=(...)` 안 → 콤마·괄호가 구분자라 **감싸야** 한다(감싸도 정상 동작).
- 인코딩(`encodeURIComponent`)은 따옴표와 무관하게 **항상** 한다 — 안 하면 ilike 의 `%` 가 깨진다.

실측(2026-08-15, 같은 토큰으로 PostgREST 직접 질의):
```
name=eq."(주)엘케이네스트코리아"          →   0건 ❌      name=eq.(주)엘케이네스트코리아        →   1건 ✅
agency_group=eq."소상공인시장진흥공단"    →   0건 ❌      agency_group=eq.소상공인시장진흥공단  → 767건 ✅
status=eq."부결"                          →   0건 ❌      status=eq.부결                        → 301건 ✅
in.("부결","진행 중") → 416건 ✅          or=(status.eq."부결",result.eq."부결") → 303건 ✅
```
이 버그로 **AI 상담의 모든 문자열 필터가 늘 0건**이었다. 특정 업체를 물으면 모델이 헛돌다
글 없이 끝나 "(빈 응답)"이 떴고, 집계도 0건을 받아 09b4482 "집계를 DB 에 위임"이 사실상
동작하지 않았다(모델이 스냅샷 눈대중으로 답을 만들어 겉보기엔 정상이라 더 위험했다).
→ 검증: `AI_TEST_JWT=<토큰> node scripts/test-ai-search-query.mjs` (실제 DB 에 쏴서 0건이 아닌지 확인)

**② 없는 컬럼을 select 하면 400 이고, 그 실패는 조용하다.**
`agency_cases` 의 상품명은 `fund_product` 다. `product` 는 **사례집(case_studies)** 의 컬럼이라
`agency_cases` 에는 없다. 2026-08-15 에 이 오타로 기업목록 기관 배지가 통째로 비어 있었다.
`fetchAllRows` 는 실패 시 `data:null` 을 주는데 호출부 대부분이 `if (!r.error && r.data)` 라
아무 일도 없던 것처럼 지나간다.
→ 이제 `fetchAllRows` 가 **콘솔에 table·select·사유를 다 찍고 화면에도 📛 배너**를 띄운다.
→ 배포 전 점검: `node scripts/audit-select-columns.mjs` (0건이어야 정상. 커버리지도 같이 출력)

## 2-7. 재시도 큐에 넣으면 안 되는 저장 (2026-08-15)

`writeGuarded` 의 재시도 큐는 **payload 를 그대로 다시 보내는 순수 UPDATE** 다.
그래서 **저장 뒤에 딸린 연동이 있는 경로는 큐에 넣으면 안 된다** — 연동이 통째로 빠진 채
"저장은 됐다"가 되어 더 찾기 어려운 불일치가 생긴다.
- 실제 사고: 계약 상태 저장이 큐에서 뒤늦게 성공 → `contract_status` 만 켜지고
  **정산 반영(`syncSettlementFromCompany`)은 실행되지 않아** 정산이 빈 채로 남았다.
  게다가 3일 전 payload 라 `contract_status_at` 도 과거 시각이라 정체일수가 어긋났다.
- → `setContractStatus` 는 `retry:false`. 실패하면 사람이 **버튼을 다시 누르게** 해야
  정산 반영까지 한 묶음으로 실행된다. 같은 성격의 저장을 새로 만들면 똑같이 할 것.

⚠️ 값의 형식이 틀려서 거절된 실패(22P02·22003·PGRST204·check/not-null 위반)도 재시도 대상이
아니다. 값이 안 바뀌니 영원히 실패한다 — 실제로 **421번** 재시도한 항목이 있었다
(`companies.fee` 가 integer 인데 수수료율 4.8 을 보냄 → 2026-08-15 numeric 으로 변경).
`classifyWriteFail` 이 `permanent` 로 표시해 큐에 안 넣고, 옛 항목도 걷어낸다.

## 3. 완료 보고 시 — "회귀 확인 완료" 여부 명시
매 기능 추가/수정 완료 보고에 **반드시 "회귀 확인 완료" 여부와 점검한 항목**을 같이 적는다.
깨진 게 있으면 목록으로 보고하고 "고칠 것 vs 이번엔 보고만 할 것"을 구분한다.

## 참고: 알려진 함정
- 팀 업무 표시 필터는 반드시 `status === "done"`만 완료로 취급.
  `status !== "open"`으로 집계하면 `taken`(진행중)이 완료로 오인된다. (2026-07-21 수정)
- ⚠️ 모바일 `MobileApp`은 **`TeamNotesSection`을 렌더한다**(`tab === "team"`, 데스크톱 컴포넌트 재사용).
  예전에는 안 그랬으나 지금은 아니다 — **팀 업무 관련 수정은 모바일에도 그대로 영향**을 준다. (2026-08-01 확인)
  단 `WorkNotesView`는 데스크톱 전용이라, 업무노트(work_notes) 화면 수정은 모바일에 영향이 없다.
  모바일은 업무노트를 자체 UI로 그린다(`newItems` 등).
- 한셀/한컴오피스로 만든 xlsx는 표준 엑셀과 달리 ①모든 요소 태그에 `x:` 네임스페이스 접두사(`<x:row>`,`<x:c>`,`<x:si>`),
  ②리치텍스트/스타일에 haansoft 전용 확장(`<hs:size>` 등)을 `mc:AlternateContent`로 섞는다.
  SheetJS는 이 둘 때문에 **에러 없이 0행**을 반환하거나 `"Unrecognized rich format"` 예외로 실패한다.
  → 업로드 xlsx 읽기는 반드시 `readUploadedWorkbook()`(App.js) 경유. 표준 읽기로 셀이 0개면 fflate로 XML을 정규화 후 재시도한다.
  정상 엑셀은 첫 시도에서 끝나 fflate를 로드하지 않는다(동작·성능 무변경). (2026-07-24 수정: (주)에이치앤와이상사 파일 0개 → 7필드)
